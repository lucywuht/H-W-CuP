import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface ChipGroupProps extends React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The content for the chip group.
     */
    children: React.ReactNode;
}
/**
 * Chip group displays multiple related chips in a horizontal row to help with arrangement and spacing.
 *
 * - {@link https://livingdesign.walmart.com/components/checkbox/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/checkbox/react/ React documentation}
 *
 * ### Content
 *
 * The Chip Group accepts content as Chip components through a required `children` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import {
 *   Chip,
 *   ChipGroup
 * } from "@walmart-web/livingdesign-components";
 *
 * export default () => {
 *   return (
 *     <ChipGroup>
 *       <Chip>One</Chip>
 *       <Chip>Two</Chip>
 *       <Chip>Three</Chip>
 *     </ChipGroup>
 *   );
 * }
 * ```
 */
export declare const ChipGroup: React.FunctionComponent<ChipGroupProps>;
