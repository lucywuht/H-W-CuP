import * as React from "react";
/**
 * Locks the `body` from scrolling except for provided element.
 * @private
 *
 * @see {@link https://github.com/willmcpo/body-scroll-lock}
 *
 * @example
 * const [setScrollableContent] = useLockBodyScroll();
 *
 * return (
 *  <div ref={setScrollableContent} ....>...</div>
 * );
 */
export declare const useLockBodyScroll: () => React.Dispatch<React.SetStateAction<HTMLElement | null>>[];
