/**
 * Handle debounced `window` resize events.
 * @private
 *
 * @param {Function} onResize The function called when the window is resized.
 * @returns {undefined}
 */
export declare const useDebouncedWindowResize: (onResize: (...args: unknown[]) => void) => void;
