import * as React from "react";
/**
 * Menu focus management hook.
 *
 * @example
 * ```tsx
 * const MyComponent = () => {
 *   const menuFocus = useMenuFocus(children, isOpen, menuRef, triggerRef);
 *
 *   // Get the currently focused item's index:
 *   menuFocus.focusedItemIndex;
 *
 *   // Change the focused item:
 *   menuFocus.focusFirstItem();
 *
 *   // ...
 * };
 * ```
 *
 * @param {React.ReactNode} children The `children` of the menu element.
 * @param {boolean} isOpen If the menu is open.
 * @param {React.RefObject} menuRef The ref for the menu element.
 * @param {React.RefObject} triggerRef The ref for the trigger element.
 * @returns {Object}
 */
export declare const useMenuFocus: (children: React.ReactNode, isOpen: boolean, menuRef: React.RefObject<HTMLElement>, triggerRef: React.RefObject<HTMLElement>) => {
    focusFirstItem: () => void;
    focusedItemIndex: number;
    focusLastItem: () => void;
    focusNextItem: () => void;
    focusPreviousItem: () => void;
};
