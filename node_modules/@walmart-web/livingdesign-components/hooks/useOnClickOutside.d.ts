import * as React from "react";
/**
 * Trigger click event when clicking outside of a given ref.
 * @private
 *
 * @see {@link https://github.com/gragland/usehooks/blob/master/src/pages/useOnClickOutside.md}
 *
 * @example
 * const ref = useRef();
 *
 * useOnClickOutside(ref, onClick);
 *
 * @param {React.RefObject | React.RefObject[]} ref Ref to manage outside click
 * @param {Function} onClick Click event handler
 * @param {Boolean} [useCapture=false] Whether to dispatch before any events beneath it in the DOM tree
 * @returns {undefined}
 */
export declare const useOnClickOutside: (ref: React.RefObject<HTMLElement> | React.RefObject<HTMLElement>[], onClick: (event: PointerEvent | MouseEvent | TouchEvent) => void, useCapture?: boolean) => void;
