import * as React from "react";
/**
 * Height transitions for the `Collapse` component.
 * @private
 *
 * @note This hook is separate from the `Collapse` component to permit mocking
 * such that `setTimeout` doesn't fire during testing.
 *
 * @param {boolean} isOpen Whether the collapse is open
 * @param {React.RefObject} ref The collapse's element's React ref
 * @returns {Object}
 */
export declare const useCollapseTransition: (isOpen: boolean, ref: React.RefObject<HTMLElement>) => {
    height: number | "auto";
    isTransitioning: boolean;
    onTransitionEnd(): void;
};
