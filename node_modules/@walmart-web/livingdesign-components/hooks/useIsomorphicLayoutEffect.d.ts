import { useEffect } from "react";
/**
 * Suppress the warnings for useLayoutEffect.
 * @private
 *
 * We need useLayoutEffect to perform layout calculations. Else, async/queued React rendering to DOM will cause non-deterministic behavior.
 * React currently throws a warning when using useLayoutEffect on the server about how its effect cannot be encoded into the server renderer's output format. This
 * will lead to a mismatch between the initial, non-hydrated UI and the intended UI.
 * To get around it, we can conditionally use useEffect on the server (no-op) and useLayoutEffect in the browser. This way, the UI will not appear broken before hydration.
 * It matches logic in React's `shared/ExecutionEnvironment` file.
 * The package react-layout-effect does the same internally, hence there is no need to install additional package.
 * @see {@link https://jira.walmart.com/browse/LD-2031}
 * @see {@link https://reactjs.org/docs/hooks-reference.html#uselayouteffect}
 * @see {@link https://github.com/facebook/react/issues/14927#issuecomment-572717051}
 * @see {@link https://github.com/facebook/react/issues/14927#issuecomment-552205874}
 * @see {@link https://www.npmjs.com/package/react-layout-effect}
 */
export declare const isDomEnvironment: boolean;
export declare const useIsomorphicLayoutEffect: typeof useEffect;
