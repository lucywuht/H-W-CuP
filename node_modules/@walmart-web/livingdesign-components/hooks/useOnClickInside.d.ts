import * as React from "react";
/**
 * Trigger click event when clicking inside of a given ref.
 * @private
 *
 * @see {@link https://github.com/gragland/usehooks/blob/master/src/pages/useOnClickOutside.md}
 *
 * @example
 * const ref1 = useRef();
 * const ref2 = useRef();
 *
 * useOnClickInside(ref1, onClick);
 * useOnClickInside([ref1, ref2], onClick);
 *
 * @param {React.RefObject | React.RefObject[]} ref Ref to manage inside click
 * @param {Function} onClick Click event handler
 * @param {Boolean} [useCapture=false] Whether to dispatch before any events beneath it in the DOM tree
 * @returns {undefined}
 */
export declare const useOnClickInside: (ref: React.RefObject<HTMLElement> | React.RefObject<HTMLElement>[], onClick: (event: PointerEvent | MouseEvent | TouchEvent) => void, useCapture?: boolean) => void;
