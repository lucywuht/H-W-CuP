import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export declare type SkeletonVariant = "rectangle" | "rounded";
export interface SkeletonProps extends React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The height for the skeleton.
     */
    height?: number | string;
    /**
     * The variant for the skeleton.
     *
     * @default "rectangle"
     */
    variant?: SkeletonVariant;
    /**
     * The width for the skeleton.
     */
    width?: number | string;
}
/**
 * A skeleton is a low-fidelity shape that approximates a user interface element and indicates loading of content. A group of skeletons roughly matching a loaded screen can improve perceived responsiveness when loading data is slow.
 *
 * @see {@link https://livingdesign.walmart.com/components/skeleton/react/}
 */
export declare const Skeleton: React.FunctionComponent<SkeletonProps>;
