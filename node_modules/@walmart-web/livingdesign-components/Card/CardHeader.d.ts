import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface CardHeaderProps extends Omit<React.ComponentPropsWithoutRef<"div">, "title">, CommonProps {
    /**
     * The leading icon for the card header.
     */
    leadingIcon?: React.ReactNode;
    /**
     * The title for the card header.
     */
    title: React.ReactNode;
    /**
     * The trailing content for the card header.
     */
    trailing?: React.ReactNode;
}
/**
 * - {@link https://livingdesign.walmart.com/components/card/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/card/react/ React documentation}
 *
 * ### Leading icon
 *
 * The Card Header accepts a leading icon through th optional `leadingIcon` prop.
 *
 * ### Title
 *
 * The Card Header accepts a title through a required `title` prop.
 *
 * ### Trailing content
 *
 * The Card Header accepts trailing content through  optional `trailing` prop.
 *
 * ```tsx
 * <CardHeader
 *   leadingIcon={<Icon.Spark size="medium" />}
 *   title="Welcome to Walmart!"
 *   trailing={<Button variant="tertiary">Here</Button>}
 * />
 * ```
 */
export declare const CardHeader: React.FunctionComponent<CardHeaderProps>;
