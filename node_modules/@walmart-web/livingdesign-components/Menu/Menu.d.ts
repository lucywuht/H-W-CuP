import * as React from "react";
import { CommonProps } from "../utility/commonProps";
import { MenuPosition } from "./Menu.service";
export type { MenuPosition };
export interface MenuProps extends React.ComponentPropsWithoutRef<"span">, CommonProps {
    /**
     * The content for the menu.
     */
    children: React.ReactNode;
    /**
     * If the menu is open.
     *
     * @default false
     */
    isOpen?: boolean;
    /**
     * The callback fired when the menu requests to close.
     */
    onClose: (event: React.KeyboardEvent<HTMLElement> | React.MouseEvent<HTMLElement> | MouseEvent | PointerEvent | TouchEvent) => void;
    /**
     * The callback fired when the menu requests to open.
     */
    onOpen: (event: React.KeyboardEvent<HTMLElement> | React.MouseEvent<HTMLElement>) => void;
    /**
     * The position for the menu.
     *
     * @default "bottomLeft"
     */
    position?: MenuPosition;
    /**
     * The trigger for the menu.
     */
    trigger: React.ReactElement;
    /**
     * The trigger ref for the menu.
     */
    triggerRef: React.RefObject<HTMLElement>;
}
/**
 * A Menu is a container with items for user-triggered actions.
 *
 * - {@link https://livingdesign.walmart.com/components/menu/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/menu/react/ React documentation}
 *
 * The `Menu` must be configured as follows to work properly:
 *
 * - Must provide menu items to `children`
 * - Must provide a single, button-like element to `trigger`
 * - Must provide the trigger element's `ref` to `triggerRef`
 * - Must provide `isOpen` and manage its state with `onClose` and `onOpen` handlers
 *
 * ```tsx
 * import * as React from "react";
 * import * as Icon from "@livingdesign/icons";
 * import {
 *   IconButton,
 *   Menu,
 *   MenuItem
 * } from "@walmart-web/livingdesign-components";
 *
 * export default () => {
 *   const [isOpen, setIsOpen] = React.useState(false);
 *   const triggerRef = React.useRef<HTMLElement>();
 *
 *   return (
 *     <Menu
 *       isOpen={isOpen}
 *       onClose={() => setIsOpen(false)}
 *       onOpen={() => setIsOpen(true)}
 *       trigger={
 *         <IconButton a11yLabel="Options" ref={triggerRef}>
 *           <Icon.More />
 *         </IconButton>
 *       }
 *       triggerRef={triggerRef}
 *     >
 *       <MenuItem>One</MenuItem>
 *       <MenuItem>Two</MenuItem>
 *       <MenuItem>Three</MenuItem>
 *     </Menu>
 *   );
 * };
 * ```
 *
 * ### Menu item selection
 *
 * Pass an `onClick` handler to `MenuItem` to handle selection.
 *
 * ```tsx
 * <Menu
 *   // ...
 * >
 *   <MenuItem onClick={() => console.log("Menu item 'One' selected")}>
 *     One
 *   </MenuItem>
 * </Menu>
 * ```
 */
export declare const Menu: React.FunctionComponent<MenuProps>;
