import * as React from "react";
import { MenuPosition } from "./Menu.service";
export interface MenuLayoutContainerProps extends React.ComponentPropsWithoutRef<"div"> {
    /**
     * The content for the menu layout container.
     */
    children: React.ReactNode;
    /**
     * The callback fired when the menu layout container requests to close.
     */
    onClose: (event: MouseEvent | PointerEvent | TouchEvent) => void;
    /**
     * The position for the menu layout container.
     */
    position: MenuPosition;
}
/**
 * @private
 */
export declare const MenuLayoutContainer: React.FunctionComponent<MenuLayoutContainerProps>;
