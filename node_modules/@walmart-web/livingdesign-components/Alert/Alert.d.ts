import * as React from "react";
import { AlertActionButtonProps } from "./AlertActionButton";
import { CommonProps } from "../utility/commonProps";
export declare type AlertVariant = "error" | "info" | "success" | "warning";
export interface AlertProps extends React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The accessibility label for the alert's icon.
     */
    a11yIconLabel?: string;
    /**
     * The props spread to the alert's action button.
     */
    actionButtonProps?: AlertActionButtonProps;
    /**
     * The content for the alert.
     */
    children: React.ReactNode;
    /**
     * The variant for the alert.
     *
     * @default "success"
     */
    variant?: AlertVariant;
}
/**
 * Alerts provide brief information and feedback to a user.
 *
 * - {@link https://livingdesign.walmart.com/components/alert/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/alert/react/ React documentation}
 *
 * ### Action button
 *
 * The Alert has an optional action button that allows users to interact. Set the `actionButtonProps` prop to enable the action button:
 *
 * ```tsx
 * <Alert
 *   actionButtonProps={{
 *     children: "Action button",
 *     onClick(event) {
 *       console.log(event);
 *     }
 *   }}
 * >
 *   Alert
 * </Alert>
 * ```
 *
 * You can also change the action button to an `HTMLAnchorElement` by setting `href`:
 *
 * ```tsx
 * <Alert
 *   actionButtonProps={{
 *     children: "Go to account",
 *     href: "https://www.walmart.com/account",
 *     target: "_blank",
 *   }}
 * >
 *   Alert
 * </Alert>
 * ```
 *
 * ### Variant
 *
 * The Alert comes with four variants:
 *
 * - `error`: Conveys a negative message to the user.
 * - `info`: Provides general information in a highlighted way.
 * - `success`: Conveys a positive message to the user.
 * - `warning`: Draws the users attention to something that may affect their experience and maybe require their attention.
 *
 * Set the Alert's variant with the `variant` prop:
 *
 * ```tsx
 * <Alert variant="error">Error</Alert>
 * <Alert variant="info">Info</Alert>
 * <Alert variant="success">Success</Alert>
 * <Alert variant="warning">Warning</Alert>
 * ```
 */
export declare const Alert: React.FunctionComponent<AlertProps>;
