import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export declare const BOGLE_BASELINE_RATIO = 0.21739130434782605;
export interface LineClampProps extends React.ComponentPropsWithoutRef<"span">, CommonProps {
    /**
     * The content for the line clamp.
     */
    children: React.ReactNode;
    /**
     * The number of visible lines.
     *
     * @default 2
     */
    lines?: number;
}
/**
 * A utility component that limits the number of visible lines of text.
 *
 * - {@link https://livingdesign.walmart.com/utilities/line-clamp/react/ React documentation}
 *
 * ### Content
 *
 * The Line Clamp accepts optional content through the `children` prop:
 *
 * ```tsx
 * <LineClamp>Content</LineClamp>
 * ```
 *
 * ### Lines
 *
 * The Line Clamp has an optional `lines` prop to limit the number of visible lines to display:
 *
 * ```tsx
 * <LineClamp lines={3}>
 *   Lorem ipsum dolor sit amet, consectetur adipiscing elit.
 * </LineClamp>
 * ```
 */
export declare const LineClamp: React.FunctionComponent<LineClampProps>;
