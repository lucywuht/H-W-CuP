import * as React from "react";
import FocusLock from "react-focus-lock";
import { CommonProps } from "../utility/commonProps";
export interface FocusTrapProps extends React.ComponentProps<typeof FocusLock>, CommonProps {
    /**
     * The content for the focus lock.
     */
    children: React.ReactNode;
    /**
     * If focus should be returned to the triggering element.
     *
     * @default true
     */
    hasFocusReturn?: boolean;
}
/**
 * The Focus Trap is a utility component that isolates browser focus for accessibility purposes.
 *
 * - {@link https://livingdesign.walmart.com/utilities/focus-trap/react/ React documentation}
 *
 * ### Content
 *
 * The Focus Trap accepts content for the focus lock through the `children` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import {
 *   Button,
 *   ButtonGroup,
 *   FocusTrap
 * } from "@walmart-web/livingdesign-components";
 *
 * const [disabled, setDisabled] = React.useState(true);
 *
 * <FocusTrap disabled={disabled}>
 *   <ButtonGroup>
 *     <Button>it's</Button>
 *     <Button>a</Button>
 *     <Button>trap</Button>
 *     <Button
 *       onClick={() => setDisabled(!disabled)}
 *       variant="tertiary"
 *     >
 *       {disabled ? "Arm" : "Disarm"}
 *     </Button>
 *   </ButtonGroup>
 * </FocusTrap>
 * ```
 *
 * ### Focus Return
 *
 * Whether or not focus should be returned to the triggering element is configurable through the `hasFocusReturn` prop.
 *
 * <FocusTrap hasFocusReturn={false}>
 *   {...}
 * </FocusTrap>
 */
export declare const FocusTrap: React.FunctionComponent<FocusTrapProps>;
