import * as React from "react";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableRowProps extends React.ComponentPropsWithoutRef<"tr">, CommonProps {
    /**
     * The content for the row.
     */
    children: React.ReactNode;
    /**
     * If the row is selected.
     *
     * @default false
     */
    selected?: boolean;
}
/**
 * Data Table Row contains a row of cells in a Data Table.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Content
 *
 * The Data Table Row accepts content (Data Table Cells) through the `children` prop.
 *
 * ```tsx
 * <DataTableRow>
 *   <DataTableCell>Chuck</DataTableCell>
 *   <DataTableCell>Norris</DataTableCell>
 * </DataTableRow>
 * ```
 *
 * ### Selection
 *
 * The Data Table Row accepts the optional `selected` prop which indicates whether or not the row is selected.
 */
export declare const DataTableRow: React.FunctionComponent<DataTableRowProps>;
