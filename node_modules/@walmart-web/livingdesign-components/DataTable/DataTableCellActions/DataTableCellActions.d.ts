import * as React from "react";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableCellActionsProps extends Omit<React.ComponentPropsWithoutRef<"td">, "align">, CommonProps {
    /**
     * The content for the data table cell actions.
     */
    children: React.ReactNode;
}
/**
 * Data Table Cell Actions is a container for interactive elements that perform actions on a selected cell.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Content
 *
 * Data Table Cell Actions accepts content through the `children` prop.
 *
 * ```tsx
 * import { Gear, Pencil, TrashCan } from "@livingdesign/icons";
 * import { DataTableCellActions, IconButton } from "@walmart-web/livingdesign-components";
 *
 * <DataTableCellActions>
 *   <IconButton a11yLabel="Edit" onClick={...}>
 *     <Pencil />
 *   </IconButton>
 *   <IconButton a11yLabel="Delete" onClick={...}>
 *     <TrashCan />
 *   </IconButton>
 *   <IconButton a11yLabel="Settings" onClick={...}>
 *     <Gear />
 *   </IconButton>
 * </DataTableCellActions>
 * ```
 */
export declare const DataTableCellActions: React.FunctionComponent<DataTableCellActionsProps>;
