import * as React from "react";
import { CheckboxA11yProps } from "../../Checkbox";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableHeaderSelectProps extends Omit<React.ComponentPropsWithoutRef<"td">, "onChange">, CommonProps {
    /**
     * The accessibility label for the data table header select.
     *
     * @default "Toggle all rows"
     */
    a11yCheckboxLabel?: string;
    /**
     * The props spread to the data table header select's input element.
     *
     * @default {}
     */
    checkboxProps?: CheckboxA11yProps["checkboxProps"];
    /**
     * If the data table header select is checked.
     *
     * @default false
     */
    checked?: CheckboxA11yProps["checked"];
    /**
     * If the data table header select is disabled.
     *
     * @default false
     */
    disabled?: CheckboxA11yProps["disabled"];
    /**
     * If the data table header select is indeterminate.
     *
     * @default false
     */
    indeterminate?: CheckboxA11yProps["indeterminate"];
    /**
     * The name for the data table header select.
     */
    name?: CheckboxA11yProps["name"];
    /**
     * The callback fired when the data table header select requests to change.
     */
    onChange: CheckboxA11yProps["onChange"];
    /**
     * The value for the data table header select.
     */
    value?: CheckboxA11yProps["value"];
}
/**
 * Data Table Header Select contains a checkbox to select/deselect Data Table Rows in bulk.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Checkbox
 *
 * The Data Table Header Select controls the state of the checkbox through the `checked` and `indeterminate` props.
 *
 * The Data Table Header Select requires a callback be provided through the `onChange` prop to handle interaction with the checkbox.
 *
 * The Data Table Header Select accepts additional control of checkbox state and configuration through the `disabled`, `name`, `value`, and `checkboxProps` props.
 *
 * ```tsx
 * import * as React from "react";
 * import { DataTableHeaderSelect } from "@walmart-web/livingdesign-components";
 *
 * const DATA = [{id: 1, color: "blue"}, {id: 2, color: "green"}, {id: 3, color: "purple"}]
 *
 * const MyComponent = () => {
 *   const [selected, setSelected] = React.useState([])
 *   const allSelected = selected.length === DATA.length;
 *   const indeterminate = selected.length > 0 && selected.length < DATA.length;
 *
 *   const handleChange = () => {
 *     const newSelectedState = allSelected || indeterminate ? [] : DATA.map((datum) => datum.id)
 *     setSelected(newSelectedState)
 *   }
 *
 *   return <DataTableHeaderSelect
 *     checked={allSelected}
 *     indeterminate={indeterminate}
 *     onChange={handleChange}
 *   />
 * }
 * ```
 *
 * ### A11y Label
 *
 * The Data Table Header Select accepts a custom accessibility label through the `a11yCheckboxLabel` prop.
 *
 * ```tsx
 * <DataTableHeaderSelect
 *   a11yCheckboxLabel="Select all rows"
 *   ...
 * />
 * ```
 */
export declare const DataTableHeaderSelect: React.ForwardRefExoticComponent<DataTableHeaderSelectProps & React.RefAttributes<HTMLInputElement>>;
