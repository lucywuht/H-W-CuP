import * as React from "react";
import { CheckboxA11yProps } from "../../Checkbox";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableCellSelectProps extends Omit<React.ComponentPropsWithoutRef<"td">, "onChange">, CommonProps {
    /**
     * The accessibility label reference IDs for the data table cell select.
     */
    a11yLabelledBy: string;
    /**
     * The props spread to the data table cell select's input element.
     *
     * @default {}
     */
    checkboxProps?: CheckboxA11yProps["checkboxProps"];
    /**
     * If the data table cell select is checked.
     *
     * @default false
     */
    checked?: CheckboxA11yProps["checked"];
    /**
     * If the data table cell select is disabled.
     *
     * @default false
     */
    disabled?: CheckboxA11yProps["disabled"];
    /**
     * The name for the data table cell select.
     */
    name?: CheckboxA11yProps["name"];
    /**
     * The callback fired when the data table cell select requests to change.
     */
    onChange: CheckboxA11yProps["onChange"];
    /**
     * The value for the data table cell select.
     */
    value?: CheckboxA11yProps["value"];
}
/**
 * Data Table Cell Select contains a checkbox to select/deselect the containing row.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Checkbox
 *
 * The Data Table Cell Select controls the state of the checkbox through the `checked` prop.
 *
 * The Data Table Cell Select requires a callback be provided through the `onChange` prop to handle interaction with the checkbox.
 *
 * The Data Table Cell Select accepts additional control of the checkbox state and configuration through the `disabled`, `name`, `value`, and `checkboxProps` props.
 *
 * ```tsx
 * import * as React from "react";
 * import { DataTableCellSelect } from "@walmart-web/livingdesign-components";
 *
 * const MyComponent = () => {
 *   const [checked, setChecked] = React.useState(false)
 *
 *   return <DataTableCellSelect
 *     a11yLabelledBy="aria-label-reference"
 *     checked={checked}
 *     onChange={() => setChecked((previousState) => !previousState)}
 *   />
 * }
 *
 * ### A11y Label
 *
 * The Data Table Cell Select requires an accessible label that references a unique identifier for the containing row that is visible on the screen. A reference to the labelling element can be provided through the `a11yLabelledBy` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { DataTableCellSelect } from "@walmart-web/livingdesign-components";
 *
 * const MyComponent = () => {
 *   const [checked, setChecked] = React.useState(false)
 *
 *   return (<DataTableRow>
 *     <DataTableCellSelect
 *       a11yLabelledBy="label-cell-id"
 *         checked={checked}
 *         onChange={() => setChecked((previousState) => !previousState)}
 *       />
 *     <DataTableCell id="label-cell-id">
 *       This is the Label
 *     </DataTableCell>
 *   </DataTableRow>)
 * }
 * ```
 */
export declare const DataTableCellSelect: React.ForwardRefExoticComponent<DataTableCellSelectProps & React.RefAttributes<HTMLInputElement>>;
