import * as React from "react";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableCellActionsMenuProps extends React.ComponentPropsWithoutRef<"span">, CommonProps {
    /**
     * The content for the data table cell actions menu.
     */
    children: React.ReactNode;
    /**
     * If the data table cell actions menu is open.
     *
     * @default false
     */
    isOpen?: boolean;
    /**
     * The callback fired when the data table cell actions menu requests to close.
     */
    onClose: (event: React.KeyboardEvent<HTMLElement> | React.MouseEvent<HTMLElement> | MouseEvent | PointerEvent | TouchEvent) => void;
    /**
     * The callback fired when the data table cell actions menu requests to open.
     */
    onOpen: (event: React.KeyboardEvent<HTMLElement> | React.MouseEvent<HTMLElement>) => void;
    /**
     * The trigger for the data table cell actions menu.
     */
    trigger: React.ReactElement;
    /**
     * The trigger ref for the data table cell actions menu.
     */
    triggerRef: React.RefObject<HTMLElement>;
}
/**
 * Data Table Cell Actions Menu is a dropdown menu containing interactive elements that perform actions on a selected cell.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Content
 *
 * The Data Table Cell Actions Menu accepts content through the `children` prop.
 *
 * ### Open/Close
 *
 * The Data Table Cell Actions Menu is set to open or close through the `isOpen` prop.
 *
 * The logic for opening and closing the Data Table Cell Actions Menu is controlled through the `onOpen` and `onClose` props.
 *
 * ### Trigger
 *
 * The Data Table Cell Actions Menu requires that a triggering element be provided through the `trigger` prop.
 * A ref to the triggering element is also required to be provided through the `triggerRef` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { More } from "@livingdesign/icons"
 * import { DataTableCellActionsMenu, DataTableCellActionsMenuItem, IconButton } from "@walmart-web/livingdesign-components"
 *
 * const MyComponent = () => {
 *   const [isOpen, setIsOpen] = React.useState(false);
 *   const triggerRef = React.useRef<HTMLButtonElement>(null);
 *
 *   return (<DataTableCellActionsMenu
 *     isOpen={isOpen}
 *     onClose={() => setIsOpen(false)}
 *     onOpen={() => setIsOpen(true)}
 *     trigger={
 *       <IconButton a11yLabel="Toggle menu" ref={triggerRef}>
 *         <More />
 *       </IconButton>
 *     }
 *     triggerRef={triggerRef}
 *   >
 *     <DataTableCellActionsMenuItem>Action 1</DataTableCellActionsMenuItem>
 *     <DataTableCellActionsMenuItem>Action 2</DataTableCellActionsMenuItem>
 *     <DataTableCellActionsMenuItem>Action 3</DataTableCellActionsMenuItem>
 *   </DataTableCellActionsMenu>);
 * }
 * ```
 */
export declare const DataTableCellActionsMenu: React.FunctionComponent<DataTableCellActionsMenuProps>;
