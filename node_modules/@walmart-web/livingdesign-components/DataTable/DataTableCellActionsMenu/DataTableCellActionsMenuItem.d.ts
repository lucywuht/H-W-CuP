import * as React from "react";
import { CommonProps } from "../../utility/commonProps";
export interface DataTableCellActionsMenuItemProps extends React.ComponentPropsWithoutRef<"button">, CommonProps {
    /**
     * The content for the data table cell actions menu item.
     */
    children: React.ReactNode;
    /**
     * The leading icon for the data table cell actions menu item.
     */
    leadingIcon?: React.ReactNode;
}
/**
 * Data Table Cell Actions Menu Item.
 *
 * - {@link https://livingdesign.walmart.com/components/data-table/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/data-table/react/ React documentation}
 *
 * ### Content
 *
 * The Data Table Cell Actions Menu Item accepts content (text label) through the `children` prop.
 *
 * ```tsx
 * <DataTableCellActionsMenuItem>Delete</DataTableCellActionsMenuItem>
 * ```
 *
 * ### Leading Icon
 *
 * The Data Table Cell Actions Menu Item accepts an optional leading icon through the `leadingIcon` prop.
 *
 * ```tsx
 * import { TrashCan } from "@livingdesign/icons";
 * import { DataTableCellActions } from "@walmart-web/livingdesign-components";
 *
 * <DataTableCellActionsMenuItem leadingIcon={<TrashCan />}>Delete</DataTableCellActionsMenuItem>
 * ```
 */
export declare const DataTableCellActionsMenuItem: React.ForwardRefExoticComponent<DataTableCellActionsMenuItemProps & React.RefAttributes<HTMLButtonElement>>;
