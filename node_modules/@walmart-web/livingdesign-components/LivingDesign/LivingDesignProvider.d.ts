import * as React from "react";
export interface LivingDesignProviderProps {
    /**
     * The content for the living design provider.
     */
    children?: React.ReactNode;
}
/**
 * A utility component that contains all the required React providers.
 *
 * - {@link https://livingdesign.walmart.com/utilities/living-design-provider/react/ React documentation}
 *
 * ### LivingDesignProvider
 *
 * Add the provider to your application's root. The provider must be in the DOM on page load to provide all the required React providers. It cannot be added dynamically.
 *
 * This provider adds [React Aria](https://react-spectrum.adobe.com/react-aria/ssr.html), [A11y Announcement](https://livingdesign.walmart.com/utilities/a11y-announcement/react/), and [Snackbar](https://livingdesign.walmart.com/components/snackbar/react/) to your application.
 *
 * ```tsx
 * import * as React from "react";
 * import * as ReactDOM from "react-dom";
 * import {LivingDesignProvider} from "@walmart-web/livingdesign-components";
 *
 * import {App} from "./App";
 *
 * ReactDOM.render(
 *   <LivingDesignProvider>
 *     <App />
 *   </LivingDesignProvider>,
 *   document.getElementById("app"),
 * );
 * ```
 */
export declare const LivingDesignProvider: React.FunctionComponent<LivingDesignProviderProps>;
