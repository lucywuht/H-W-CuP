import React from "react";
import { CommonProps } from "../utility/commonProps";
export declare type ProgressIndicatorVariant = "error" | "info" | "success" | "warning";
interface ProgressIndicatorBaseProps extends React.ComponentPropsWithoutRef<"span">, CommonProps {
    /**
     * The maximum value for the progress indicator.
     *
     * @default 100
     */
    max?: number;
    /**
     * The minimum value for the progress indicator.
     *
     * @default 0
     */
    min?: number;
    /**
     * The value for the progress indicator.
     *
     * @default 0
     */
    value?: number;
    /**
     * The value label for the progress indicator.
     */
    valueLabel?: string;
    /**
     * The variant for the progress indicator.
     *
     * @default "info"
     */
    variant?: ProgressIndicatorVariant;
}
/**
 * Progress Indicators display a visual representation of the completion of a task or process.
 *
 * - {@link https://livingdesign.walmart.com/components/progress-indicator/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/progress-indicator/react/ React documentation}
 *
 * ### Max
 *
 * Set the maximum value for the Progress Indicator through the `max` prop:
 *
 * ```tsx
 * <ProgressIndicator max={50} />
 * ```
 *
 * ### Min
 *
 * Set the minimum value for the Progress Indicator through the `min` prop:
 *
 * ```tsx
 * <ProgressIndicator min={10} />
 * ```
 *
 * ### Value
 *
 * Set the value for the Progress Indicator through the `value` prop:
 *
 * ```tsx
 * <ProgressIndicator value={30} />
 * ```
 *
 * ### Value label
 *
 * The Progress Indicator accepts an optional `valueLabel` prop to provide a label for the Progress Indicator:
 *
 * ```tsx
 * <ProgressIndicator valueLabel="Label" />
 * ```
 *
 * ### Variant
 *
 * The Progress Indicator comes with four variants:
 *
 * - `error`: Conveys a negative message to the user.
 * - `info`: Provides general information in a highlighted way.
 * - `success`: Conveys a positive message to the user.
 * - `warning`: Draws the users attention to something that may affect their experience and maybe require their attention.
 *
 * Set the Progress Indicator's variant with the `variant` prop:
 *
 * ```tsx
 * <ProgressIndicator variant="error" />
 * <ProgressIndicator variant="info" />
 * <ProgressIndicator variant="success" />
 * <ProgressIndicator variant="warning" />
 * ```
 */
export interface ProgressIndicatorA11yProps extends ProgressIndicatorBaseProps {
    /**
     * The accessible label reference IDs for the progress indicator (Required if omitting `label`).
     */
    a11yLabelledBy: string;
    label?: never;
}
export interface ProgressIndicatorLabelProps extends ProgressIndicatorBaseProps {
    /**
     * The label for the progress indicator (Required if omitting `a11yLabelledBy`).
     */
    label: React.ReactNode;
    a11yLabelledBy?: never;
}
export declare type ProgressIndicatorProps = ProgressIndicatorA11yProps | ProgressIndicatorLabelProps;
/**
 * Progress Indicators display a visual representation of the completion of a task or process.
 *
 * @see {@link https://livingdesign.walmart.com/components/progress-indicator/react/}
 */
export declare const ProgressIndicator: React.FunctionComponent<ProgressIndicatorProps>;
export {};
