import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface DividerProps extends React.ComponentPropsWithoutRef<"hr">, CommonProps {
    /**
     * The title for the divider.
     *
     * If the divider is used as decoration, this property should be omitted.
     */
    title?: string;
}
/**
 * Dividers create visual breaks in content.
 *
 * - {@link https://livingdesign.walmart.com/components/divider/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/divider/react/ React documentation}
 *
 * ### Title
 *
 * The Divider accepts a title through the `title` prop to label the element for screen-readers.
 *
 * ```tsx
 * <Divider title="Descriptive Title" />;
 * ```
 */
export declare const Divider: React.FunctionComponent<DividerProps>;
