import * as React from "react";
import { CommonProps } from "../utility/commonProps";
import { PopoverLayoutManagerProps } from "./PopoverLayoutManager";
export type { PopoverPosition } from "./PopoverLayoutManager";
export interface PopoverProps extends PopoverLayoutManagerProps, React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The trigger for the popover.
     */
    children: React.ReactElement;
    /**
     * If the popover is open.
     *
     * @default false
     */
    isOpen?: boolean;
}
/**
 * Popovers are overlays that display contextual content on demand.
 *
 * - {@link https://livingdesign.walmart.com/components/popover/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/popover/react/ React documentation}
 *
 *
 * The `Popover` component must be configured as follows to meet accessibly requirements:
 *
 * 1. Must provide a single button-like element as its `children`
 * 1. Must provide the button-like element's `ref` as its `triggerRef`
 * 1. Must provide at least one focusable element within its `content`
 *
 * ```tsx
 * import * as React from "react";
 * import {Button, Popover} from "@walmart-web/livingdesign-components";
 *
 * export const MyComponent = () => {
 *   const [state, setState] = React.useState(false);
 *   const ref = React.useRef();
 *
 *   return (
 *     <Popover
 *       content={
 *         <div>
 *           <Checkbox
 *             label="I must contain at least one focusable element."
 *             name="status"
 *             onChange={() => {}}
 *           />
 *         </div>
 *       }
 *       onClose={(event) => setState(false)}
 *       isOpen={state}
 *       triggerRef={ref}
 *     >
 *       <Button
 *         ref={ref}
 *         onClick={(event) => {
 *           setState((state) => !state);
 *         }}
 *       >
 *         Open Popover
 *       </Button>
 *     </Popover>
 *   );
 * };
 * ```
 *
 * ### Position
 *
 * The Popover has eight possible positions. The possible positions are `bottomCenter`, `bottomLeft`, `bottomRight`, `left`, `right`, `topCenter`, `topLeft`, or `topRight`. Set the position through the `position` prop:
 *
 * ```tsx
 * <Popover position="bottomCenter">
 *   <Trigger />
 * </Popover>
 * ```
 *
 * ### Has nubbin
 *
 * Give the Popover a nubbin through the `hasNubbin` prop:
 *
 * ```tsx
 * <Popover hasNubbin={true}>
 *   <Trigger />
 * </Popover>
 * ```
 */
export declare const Popover: React.FunctionComponent<PopoverProps>;
