import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface SideNavigationProps extends React.ComponentPropsWithoutRef<"nav">, CommonProps {
    /**
     * The content for the side navigation.
     */
    children?: React.ReactNode;
}
/**
 * Side navigation provides a list of primary links that navigate users to a URL. Links should be grouped together in sections based on related content or categories. A section title can proceed a list of links.
 *
 * @see {@link https://livingdesign.walmart.com/components/side-navigation/react/}
 */
export declare const SideNavigation: React.FunctionComponent<SideNavigationProps>;
