import * as React from "react";
import { CommonProps } from "../utility/commonProps";
import { NudgeCloseButtonProps } from "./NudgeCloseButton";
export interface NudgeProps extends Omit<React.ComponentPropsWithoutRef<"div">, "title">, CommonProps {
    /**
     * The actions for the nudge.
     */
    actions?: React.ReactNode;
    /**
     * The content for the nudge.
     */
    children: React.ReactNode;
    /**
     * The props spread to the nudge's close button.
     */
    closeButtonProps?: NudgeCloseButtonProps;
    /**
     * The leading content for the nudge.
     */
    leading?: React.ReactNode;
    /**
     * The callback fired when the nudge requests to close.
     */
    onClose?: (event: React.MouseEvent<HTMLButtonElement>) => void;
    /**
     * The title for the nudge.
     */
    title: React.ReactNode;
}
/**
 * Nudges are used to remind, provide instructions or prompts for the next-step in a process, or as a mechanism to upsell or cross-sell. The information they provide should not be required for a user to complete a task.
 *
 * - {@link https://livingdesign.walmart.com/components/nudge/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/nudge/react/ React documentation}
 *
 * ### Actions
 *
 * The Nudge accepts actions through the `actions` prop:
 *
 * ```tsx
 * <Nudge
 *   actions={
 *     <ButtonGroup>
 *       <Button>Action1</Button>
 *       <Button>Action2</Button>
 *     </ButtonGroup>
 *   }
 *   title="Title"
 * >
 *   Content
 * </Nudge>
 * ```
 *
 * ### Content
 *
 * The Nudge accepts content through the `children` prop:
 *
 * ```tsx
 * <Nudge title="Title">Content</Nudge>
 * ```
 *
 * ### Leading
 *
 * The Nudge accepts optional leading content through the `leading` prop:
 *
 * ```tsx
 * <Nudge
 *   leading={<LeadingIcon />}
 *   title="Title"
 * >
 *   Content
 * </Nudge>
 * ```
 *
 * ### On close
 *
 * The Nudge has a close button. Add an `onClose` prop with a callback function to display the close button and handle user interaction:
 *
 * ```tsx
 * <Nudge
 *   onClose={(event) => console.log(event)}
 *   title="Title"
 * >
 *   Content
 * </Nudge>
 * ```
 *
 * ### Title
 *
 * Set the title for the Nudge with the `title` prop:
 *
 * ```tsx
 * <Nudge title="Title">Content</Nudge>
 * ```
 *
 */
export declare const Nudge: React.FunctionComponent<NudgeProps>;
