import * as React from "react";
import { CommonProps } from "../utility/commonProps";
interface CheckboxBaseProps extends Omit<React.ComponentPropsWithoutRef<"label">, "onChange">, CommonProps {
    /**
     * The props spread to the checkbox's input element.
     *
     * @default {}
     */
    checkboxProps?: React.ComponentPropsWithoutRef<"input">;
    /**
     * If the checkbox is checked.
     *
     * @default false
     */
    checked?: boolean;
    /**
     * If the checkbox is disabled.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * The id for the checkbox.
     */
    id?: string;
    /**
     * If the checkbox is indeterminate.
     *
     * @default false
     */
    indeterminate?: boolean;
    /**
     * The name for the checkbox.
     */
    name?: string;
    /**
     * The callback fired when the checkbox requests to change.
     */
    onChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /**
     * The value for the checkbox.
     */
    value?: number | string;
}
export interface CheckboxA11yProps extends CheckboxBaseProps {
    /**
     * The accessible label reference IDs for the checkbox (Required if omitting `label`).
     */
    a11yLabelledBy: string;
    label?: never;
}
export interface CheckboxLabelProps extends CheckboxBaseProps {
    /**
     * The label for the checkbox (Required if omitting `a11yLabelledBy`).
     */
    label: React.ReactNode;
    a11yLabelledBy?: never;
}
export declare type CheckboxProps = CheckboxA11yProps | CheckboxLabelProps;
/**
 * Checkboxes are used for a list of options where the user may select multiple options, including all or none.
 *
 * - {@link https://livingdesign.walmart.com/components/checkbox/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/checkbox/react/ React documentation}
 *
 * ### A11y labelled by
 *
 * The Checkbox accepts an optional `a11yLabelledBy` prop that sets the accessible label IDs (Required if omitting `label`).
 *
 * ### Label
 *
 * The Checkbox accepts a checkbox label through an optional `label` prop (Required if omitting `a11yLabelledBy`).
 *
 * ### Name
 *
 * The Checkbox accepts a checkbox name through an optional `name` prop.
 *
 * ### Selection
 *
 * The Checkbox requires a callback to be provided to the `onChange` prop to handle interaction with checkbox.
 *
 * ### States
 *
 * The Checkbox states can be managed through the following optional props `checked`, `indeterminate` and `disabled`.
 *
 * ### Value
 *
 * The Checkbox accepts a checkbox value through an optional `value` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { Checkbox } from "@walmart-web/livingdesign-components";
 *
 * export default () => {
 *   return (
 *     <>
 *       <Checkbox
 *         checked={checked}
 *         disabled
 *         indeterminate
 *         label="apple"
 *         onChange={(e) => console.log(e.target.value)}
 *         value="apple"
 *       />
 *       <Checkbox
 *         disabled
 *         label="banana"
 *         onChange={(e) => console.log(e.target.value)}
 *         value="banana"
 *       />
 *       <Checkbox
 *         indeterminate
 *         label="orange"
 *         onChange={(e) => console.log(e.target.value)}
 *         value="orange"
 *       />
 *     </>
 *   );
 * }
 * ```
 */
export declare const Checkbox: React.ForwardRefExoticComponent<CheckboxProps & React.RefAttributes<HTMLInputElement>>;
export {};
