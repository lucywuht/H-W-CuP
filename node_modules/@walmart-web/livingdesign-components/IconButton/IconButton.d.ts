import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export declare type IconButtonSize = "large" | "medium" | "small";
interface IconButtonBaseProps extends CommonProps {
    /**
     * The accessible label for the icon button.
     */
    a11yLabel: string;
    /**
     * The content for the icon button.
     */
    children: React.ReactNode;
    /**
     * The size for the icon button.
     *
     * @default "small"
     */
    size?: IconButtonSize;
}
export declare type IconButtonAnchorProps = IconButtonBaseProps & JSX.IntrinsicElements["a"] & {
    /**
     * The href for the icon button (Anchor only).
     */
    href: string;
};
export declare type IconButtonButtonProps = IconButtonBaseProps & JSX.IntrinsicElements["button"] & {
    /**
     * If the icon button is disabled (Button only).
     *
     * @default false
     */
    disabled?: boolean;
};
export declare type IconButtonProps = IconButtonAnchorProps | IconButtonButtonProps;
export declare type IconButtonPolymorphicType = {
    (props: IconButtonAnchorProps): JSX.Element;
    (props: IconButtonButtonProps): JSX.Element;
    displayName?: string;
};
/**
 * The Icon Button triggers an action (similar to a button) and relies solely on an icon to convey its purpose.
 *
 * - {@link https://livingdesign.walmart.com/components/icon-button/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/icon-button/react/ React documentation}
 *
 * ### Content
 *
 * The Icon Button accepts an icon to display through the `children` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { IconButton } from "@walmart-web/livingdesign-components";
 * import Icon from "@livingdesign/icons";
 *
 * <IconButton a11yLabel="icon">
 *   <Icon.Cart />
 * </IconButton>
 * ```
 *
 * ### A11y Label
 *
 * The Icon Button requires an accessible label be provided through the `a11yLabel` prop.
 *
 * ### Size
 *
 * The Icon Button comes in three sizes: small (default), medium and large. Set the Icon Button's size using the `size` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { IconButton } from "@walmart-web/livingdesign-components";
 * import Icon from "@livingdesign/icons";
 *
 * <IconButton a11yLabel="icon" size="large">
 *   <Icon.Cart />
 * </IconButton>
 * ```
 */
export declare const IconButton: IconButtonPolymorphicType;
export {};
