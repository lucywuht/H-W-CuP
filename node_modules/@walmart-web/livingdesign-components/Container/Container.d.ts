import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface ContainerProps extends React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The content for the container.
     */
    children: React.ReactNode;
}
/**
 * A utility component that sets a maximum width for layouts.
 *
 * - {@link https://livingdesign.walmart.com/utilities/container/react/ React documentation}
 *
 * ### Content
 *
 * The Container accepts content through a required `children` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { Container } from "@walmart-web/livingdesign-components";
 *
 * export default () => {
 *   return (
 *     <Container>
 *       <p>
 *         Leverage agile frameworks to provide a robust
 *         synopsis for high level overviews. Iterative
 *         approaches to corporate strategy foster
 *         collaborative thinking to further the overall
 *         value proposition. Organically grow the holistic
 *         world view of disruptive innovation via workplace
 *         diversity and empowerment.
 *       </p>
 *     </Container>
 *   );
 * }
 * ```
 */
export declare const Container: React.FunctionComponent<ContainerProps>;
