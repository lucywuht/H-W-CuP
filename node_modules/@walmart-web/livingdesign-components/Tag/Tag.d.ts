import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export declare type TagColor = "red" | "spark" | "green" | "blue" | "purple" | "gray";
export declare type TagVariant = "primary" | "secondary" | "tertiary";
export interface TagProps extends React.ComponentPropsWithoutRef<"span">, CommonProps {
    /**
     * The content for the tag.
     */
    children: React.ReactNode;
    /**
     * The color for the tag.
     *
     * @default "blue"
     */
    color?: TagColor;
    /**
     * The leading content for the tag.
     */
    leading?: React.ReactNode;
    /**
     * The variant for the tag.
     *
     * @default "secondary"
     */
    variant?: TagVariant;
}
/**
 * Tags are used to draw a customer’s focus to item traits such as availability, status, or media rating. Items may be products, fulfillment slots, or something else. They are visual only and non-interactive.
 *
 * @see {@link https://livingdesign.walmart.com/components/tag/react/}
 */
export declare const Tag: React.FunctionComponent<TagProps>;
