import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export interface GridProps extends React.ComponentPropsWithoutRef<"div">, CommonProps {
    /**
     * The content for the grid.
     */
    children: React.ReactNode;
    /**
     * If the grid has a gutter.
     *
     * @default false
     */
    hasGutter?: boolean;
}
/**
 * The Grid provides a framework for the rhythmic and consistent positioning of elements onscreen.
 *
 * The Grid contains 12 columns. There are no rows.
 *
 * - {@link https://livingdesign.walmart.com/components/grid/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/grid/react/ React documentation}
 *
 * ### Content
 *
 * The Grid accepts content (GridColumns) through the `children` prop.
 *
 * ```tsx
 * <Grid>
 *   <GridColumn>Column 1</GridColumn>
 *   <GridColumn>Column 2</GridColumn>
 *   <GridColumn>Column 3</GridColumn>
 * </Grid>
 * ```
 *
 * ### Gutters
 *
 * Spacing between columns in the Grid can be added through the `hasGutter` prop.
 *
 * ```
 * <Grid hasGutter>
 *   <GridColumn>Column 1</GridColumn>
 *   <GridColumn>Column 2</GridColumn>
 *   <GridColumn>Column 3</GridColumn>
 * </Grid>
 * ```
 */
export declare const Grid: React.FunctionComponent<GridProps>;
