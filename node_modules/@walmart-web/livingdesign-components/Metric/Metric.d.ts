import * as React from "react";
import { CommonProps } from "../utility/commonProps";
export declare type MetricVariant = "negativeDown" | "negativeUp" | "neutral" | "positiveDown" | "positiveUp";
export interface MetricProps extends Omit<React.ComponentPropsWithoutRef<"div">, "title">, CommonProps {
    /**
     * The accessible label for the metric's trend indicator.
     */
    a11yTrendIndicatorLabel?: string;
    /**
     * The text label providing a description of the metric.
     */
    textLabel?: React.ReactNode;
    /**
     * The timescope for the metric.
     */
    timescope?: React.ReactNode;
    /**
     * The title for the metric.
     */
    title: React.ReactNode;
    /**
     * The unit for the metric.
     */
    unit?: React.ReactNode;
    /**
     * The value for the metric.
     */
    value: React.ReactNode;
    /**
     * The variant for the metric.
     *
     * @default "neutral"
     */
    variant?: MetricVariant;
}
/**
 * Metric displays the value of a significant data point.
 *
 * - {@link https://livingdesign.walmart.com/components/metric/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/metric/react/ React documentation}
 *
 * ### A11y trend indicator label
 *
 * The Metric accepts an optional accessible trend indicator label through the `allyTrendIndicatorLabel` prop:
 *
 * ```tsx
 * <Metric
 *   allyTrendIndicatorLabel="Trend Indicator Label"
 *   title="Title"
 *   value="Value"
 * />
 * ```
 *
 * ### Text label
 *
 * The Metric accepts an optional `textLabel` prop to provide a description of the metric:
 *
 * ```tsx
 * <Metric
 *   textLabel="Text Label"
 *   title="Title"
 *   value="Value"
 * />
 * ```
 *
 * ### Timescope
 *
 * Set the timescope for the Metric with the `timescope` prop:
 *
 * ```tsx
 * <Metric
 *   timescope="Timescope"
 *   title="Title"
 *   value="Value"
 * />
 * ```
 *
 * ### Title
 *
 * Set the title of the Metric with the `title` prop:
 *
 * ```tsx
 * <Metric
 *   title="Title"
 *   value="Value"
 * />
 * ```
 *
 * ### Unit
 *
 * Set the unit for the Metric with the `unit` prop:
 *
 * ```tsx
 * <Metric
 *   title="Title"
 *   unit="Unit"
 *   value="Value"
 * />
 * ```
 *
 * ### Value
 *
 * Set the value for the Metric with the `value` prop:
 *
 * ```tsx
 * <Metric
 *   title="Title"
 *   value="Value"
 * />
 * ```
 *
 * ### Variant
 *
 * The Metric comes with five variants:
 *
 * - `neutral`: Used to show data at a point in time without trend.
 * - `positiveUp`: Used to show an upward trend (an increase in value) that indicates a positive change.
 * - `positiveDown`: Used to show a downward trend (a decrease in value) that indicates a positive change.
 * - `negativeUp`: Used to show an upward trend (an increase in value) that indicates a negative change.
 * - `negativeDown`: Used to show a downward trend (a decrease in value) that indicates a negative change.
 *
 * Set the Metric's variant with the `variant` prop:
 *
 * ```tsx
 * <Metric
 *   title="Title"
 *   value="Value"
 *   variant="neutral"
 * >
 *   Neutral
 * </Metric>
 *
 * <Metric
 *   title="Title"
 *   value="Value"
 *   variant="positiveUp"
 * >
 *   PositiveUp
 * </Metric>
 *
 * <Metric
 *   title="Title"
 *   value="Value"
 *   variant="positiveDown"
 * >
 *   PositiveDown
 * </Metric>
 *
 * <Metric
 *   title="Title"
 *   value="Value"
 *   variant="negativeUp"
 * >
 *   NegativeUp
 * </Metric>
 *
 * <Metric
 *   title="Title"
 *   value="Value"
 *   variant="negativeDown"
 * >
 *   NegativeDown
 * </Metric>
 * ```
 */
export declare const Metric: React.FunctionComponent<MetricProps>;
