import * as React from "react";
import { CommonProps } from "../utility/commonProps";
interface ErrorMessageProps extends Omit<React.ComponentPropsWithoutRef<"div">, "title">, CommonProps {
    /**
     * The actions for the error message.
     */
    actions?: React.ReactNode;
    /**
     * The content for the error message.
     */
    children: React.ReactNode;
    /**
     * The media for the error message.
     */
    media?: React.ReactNode;
    /**
     * The title for the error message.
     */
    title: React.ReactNode;
}
/**
 * The Error Message is a familiar layout with descriptive content and suggested actions for error scenarios.
 *
 * - {@link https://livingdesign.walmart.com/components/error-message/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/error-message/react/ React documentation}
 *
 * ### Title
 *
 * The Error Message requires a title to be provided through the `title` prop.
 *
 * ### Text Label
 *
 * The Error Message requires a text label to be provided through the `children` prop.
 *
 * ```tsx
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 *
 * ### Action Content
 *
 * The Error Message accepts action content, such as a button, through the `actions` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import {
 *   Button,
 *   ErrorMessage
 * } from "@walmart-web/livingdesign-components";
 *
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 *   actions={
 *     <Button size="medium" variant="primary" onClick={...}>
 *       Refresh page
 *     </Button>
 *   }
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 * ### Media
 *
 * The Error Message accepts media to display, such as an illustration, through the `media` prop.
 *
 * * ```tsx
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 *   media={
 *     <img
 *       alt=""
 *       height="200"
 *       src="/SearchNotFoundCircle.svg"
 *       width="200"
 *     />
 *   }
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 */
export declare const ErrorMessage: React.FunctionComponent<ErrorMessageProps>;
export {};
