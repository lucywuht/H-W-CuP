/**
 * NOTE: These utilities are private implementations. For legacy reasons, we have
 * publically provided some utilities. This library's goal is to only export
 * code that backs a design system pattern.
 *
 * If additional utilities are needed publically, please create a ticket with
 * justification on how these utilities would facilitate the library's goal.
 */
export type { CommonProps } from "./commonProps";
