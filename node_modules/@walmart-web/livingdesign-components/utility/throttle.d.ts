/**
 * Throttle calls to a function.
 * @private
 *
 * @param {Function} fn The function to call
 * @param {number} timeout The time to throttle calls
 * @returns {Function} The throttled function
 */
export declare const throttle: <T extends unknown[], R = void>(fn: (...args: T) => R, timeout: number) => (...args: T) => void;
