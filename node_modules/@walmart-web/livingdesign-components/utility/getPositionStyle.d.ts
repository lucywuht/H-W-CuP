import * as React from "react";
export declare type CalculatePositionFn = (dimensions: {
    referrerHeight: number;
    referrerWidth: number;
    targetHeight: number;
    targetWidth: number;
}) => {
    left: number;
    top: number;
};
/**
 * Get position styles for a referrer element and a target element.
 * @private
 *
 * @param {Object} options
 * @param {Function} options.calculatePosition Get the left and top positions
 * @param {React.RefObject<HTMLElement>} options.referrerRef The element that refers to the target
 * @param {React.RefObject<HTMLElement>} options.targetRef The target element
 * @returns {Object} The alignment styles
 */
export declare const getPositionStyle: ({ calculatePosition, referrerRef, targetRef, }: {
    calculatePosition: CalculatePositionFn;
    referrerRef: React.RefObject<HTMLElement>;
    targetRef: React.RefObject<HTMLElement>;
}) => {
    transform: string;
};
