import { ComponentPropsWithoutRef, FunctionComponent, ReactElement, ReactNode } from 'react';
import { CommonProps } from '../utility/commonProps';
export interface Option {
    id: string;
    name: string;
    data?: any;
}
export interface noResult {
    title: string;
    description: string;
}
export interface AutoCompleteProps extends Omit<ComponentPropsWithoutRef<'div'>, 'className' | 'onChange' | 'style' | 'onClick'>, CommonProps {
    /**
     * The label for the auto complete.
     */
    label: ReactNode;
    /**
     * selected menu item
     */
    onOptionSelected(option: Option, index: number): void;
    /**
     * The callback fired when the text field hit enter.
     */
    onEnterKeyDown: () => void;
    /**
     * The callback fired when the type something is the text box.
     */
    onSearchTextChanged: (value: string) => void;
    /**
     * The min lengthfor the auto complete.
     */
    minInputLengthForSuggestion: number;
    /**
     * If the auto complete is disabled.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * The id for the auto complete.
     */
    id?: string;
    /**
     * Array of options to show.
     *
     */
    options?: Array<Option>;
    /**
     * To know when the api is fetching data to show loader
     */
    isLoading?: boolean;
    /**
     * The placeholder for the text field.
     */
    placeholder?: string;
    /**
     * The error for the text field.
     */
    error?: ReactNode;
    /**
     * The customOptionRender for the drop down.
     */
    customOptionRenderer?: (option: Option) => ReactElement;
    /**
     * No results Message.
     */
    noResultsMessage?: noResult;
    /**
     * Default search Value.
     */
    defaultSearchValue?: string;
}
/**
 * auto complete allow users to search and the result.
 *
 * {@link https://livingdesign.walmart.com/components/text-field/react/ React documentation}
 *
 * ### Content
 *
 * #### Label
 *
 * Auto Complete requires a label to be accessible. Use the `label` property to add a label:
 *
 * ```tsx
 * <AutoComplete
 *   label="Search"
 *   onChange={(event) => console.log(event)}
 * />
 * ```
 *  ### Interaction
 *
 * when user type anything  an callback handler call the `onSearchTextChanged` prop, and also pass result in options prop.
 *
 *
 * ```tsx
 * import * as React from "react";
 *
 * export default () => {
 *   const [value, setValue] = useState("CA");
 *   const defaultOptions = [
 *  {
 *    id: '1',
 *     name: "Search term example 1",
 *   },
 *  {
 *     id: '2',
 *     name: "Activity code",
 *   },
 *   {
 *     id: '3',
 *     name: "Search term example 3",
 *   },
 *   {
 *     id: '4',
 *     name: "error",
 *   },
 *   {
 *     id: '5',
 *     name: "test",
 *   }
 * ];
 *
 *   return (
 *     <AutoComplete
 *       label="Full name"
 *       onSearchTextChanged={(event) => setValue(event.target.value)}
 *       options={defaultOptions}
 *     />
 *   );
 * };
 * ```
 */
export declare const AutoComplete: FunctionComponent<AutoCompleteProps>;
