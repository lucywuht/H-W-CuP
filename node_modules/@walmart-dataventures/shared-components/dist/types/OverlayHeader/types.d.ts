import { IconButtonSize, MenuPosition } from '@walmart-web/livingdesign-components';
import React from 'react';
import { CommonProps } from '../utility/commonProps';
export declare enum NavigationState {
    default = "default",
    active = "active",
    complete = "complete",
    warning = "warning"
}
export type OnClickFn = ({ label, id, }: {
    label: string | number;
    id: string | number;
}) => void;
export type ActionListItem = {
    label: string | number;
    id: string | number;
    icon: React.ReactElement;
};
export type ConfigProps = {
    titleConfig: {
        /** The title text to display in the header */
        title: string;
        /** Optional description text to display below the title */
        description?: string;
        /** Optional list of actions for the title dropdown menu */
        actionList?: ActionListItem[];
    };
    /** Optional list of actions for the overflow menu */
    moreActionList?: ActionListItem[];
};
export type HeaderProps = CommonProps & {
    /**
     * The 'config' property is a required field that includes the header title, an title action list, a more action list, and stepper/icon navigation.
     * `ConfigProps = {
     * titleConfig: {
     *  title: string;
     *  actionList?: ActionList[];
     * };
     * moreActionList?: ActionListItem[];
     *}`
     */
    config: ConfigProps;
    /**
     *
     * OnTitleClose is an title close event handler function; by defining it, we can enable the title close feature.
     */
    onTitleClose?: (event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => void;
    /**
     *
     * onTitleMenuClick is an Title Menu event handler function.
     * `OnClickFn = ({
     * label,
     * id,
     * }: {
     *  label: string | number;
     * id: string | number;
     * }) => void;`
     */
    onTitleMenuClick?: OnClickFn;
    /**
     *
     * onOverflowMenuClick is an Title More action event handler function.
     * `OnClickFn = ({
     * label,
     * id,
     * }: {
     *  label: string | number;
     * id: string | number;
     * }) => void;`
     */
    onOverflowMenuClick?: OnClickFn;
    /** TopRightHeaderPrimary - It's an optional prop that accepts a React component and is placed in the top-right section, specifically in the left part of the divider.
     */
    TopRightHeaderPrimary?: React.ReactNode;
    /** TopRightHeaderPrimary - It's an optional prop that accepts a React component and is placed in the top-right section, specifically in the Right part  of the divider.
     */
    TopRightHeaderSecondary?: React.ReactNode;
    /** children prop - which accept React component */
    children?: React.ReactNode;
};
export type PopoverMenuListProps = {
    actionList: ActionListItem[];
    onMenuClickHandler: OnClickFn;
    Icon: React.ReactNode;
    menuPosition?: MenuPosition;
    ariaLabel?: string;
    title?: string;
    iconButtonSize?: IconButtonSize;
};
