import * as React from 'react';
export interface CommonProps {
    /**
     * If provided, will be used to check against accessibility violations.
     *
     * @note In almost all cases `aria-label` should not be used. Proper labelling should be already accounted for within the components.
     */
    'aria-label'?: string;
    /**
     * If provided, the additional class name to provide to the root element.
     *
     * @note This property is prefixed with `UNSAFE` as its use often results in
     * unintended side effects.
     */
    UNSAFE_className?: string;
    /**
     * If provided, the `style` to provide to the root element.
     *
     * @note This property is prefixed with `UNSAFE` as its use often results in
     * unintended side effects.
     */
    UNSAFE_style?: React.CSSProperties;
}
type AppliedCommonProps<T> = T & {
    /**
     * If provided, the additional class name to provide to the root element.
     */
    className?: string;
    /**
     * If provided, the `style` to provide to the root element.
     */
    style?: React.CSSProperties;
};
/**
 * Tranform common props to properties for use with a HTMLElement.
 */
export declare const applyCommonProps: <T extends Object = {}>(props: CommonProps & T, Component: React.FunctionComponent<any>, isAriaLabelBanned?: boolean) => AppliedCommonProps<T>;
export {};
