/**
 * Debounce calls to a function.
 * @private
 *
 * @param {Function} fn The function to call
 * @param {number} timeout The time to debounce calls
 * @returns {Function} The debounced function
 */
export declare const debounce: <T extends unknown[], R = {
    (): void;
    cancel: () => void;
}>(fn: (...args: T) => R, timeout: number) => {
    (...args: T): void;
    cancel: () => void;
};
