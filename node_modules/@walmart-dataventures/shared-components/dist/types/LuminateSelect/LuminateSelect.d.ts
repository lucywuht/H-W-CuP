import React from 'react';
import { ChipSize, MenuPosition } from '..';
export type SelectOptionTypeWithKeyValue = {
    key: string;
    label: string;
    icon?: React.ReactNode;
};
export interface SelectProps {
    /**
     * List of items for Select field
     */
    options: string[] | SelectOptionTypeWithKeyValue[];
    /**
     * Callback executed when the select field is blurred.
     * Since this is a custom component, the onBlur callback is called when the menu is closed
     */
    onBlur?: () => void;
    /**
     * Callback to send the selected option
     */
    onChange: (option: string) => void;
    /**
     * Classname for the root div.
     */
    className?: string;
    /**
     * The label for the Select field.
     */
    label?: string;
    /**
     * The placeholder for the Select field.
     */
    placeholder?: string;
    /**
     * The leading content for the list item.
     */
    leadingIcon?: React.ReactNode;
    /**
     * Flag to disable the Select field.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * The helper text for the Select field.
     */
    helperText?: React.ReactNode;
    /**
     * The error for the Select field.
     */
    error?: React.ReactNode;
    /**
     * The size of the chip.
     *
     * @default large
     */
    size?: ChipSize;
    /**
     * The value for the component.
     *
     */
    value?: string;
    /**
     * Flag to horizontally align label and select dropdown.
     *
     * @default false
     */
    horizontalLabel?: boolean;
    /**
     * If the button is displayed at full width.
     *
     * @default false
     */
    isFullWidth?: boolean;
    /**
     * ref for the select menu
     */
    menuTriggerRef?: React.RefObject<HTMLButtonElement>;
    /**
     * Maximum height for the menu
     */
    maxMenuHeight?: string;
    /**
     *
     * @default 'bottomLeft'
     */
    menuPosition?: MenuPosition;
    /**
     * Optional ID for the scrollable div
     *
     * @default menuListScrollableDiv
     */
    scrollableDivId?: string;
    /**
     * Position of the check icon (left or right).
     *
     * @default right
     */
    checkPosition?: 'left' | 'right';
    /**
     * The id for the select component.
     */
    id?: string;
    /**
     * Controls the z-index stacking context for the select component and its dropdown.
     * Use higher values when the dropdown needs to appear above other UI elements.
     * - '100': Lowest level, for basic components with no overlay requirements
     * - '200': Low level, for components that should appear above basic content
     * - '300': Medium level (default), standard for most dropdowns
     * - '400': High level, for dropdowns that should appear above most UI elements
     * - '500': Very high level, for critical UI elements
     * - '600': Highest level, for modal-like behavior
     * @default '300'
     */
    zIndex?: '100' | '200' | '300' | '400' | '500' | '600';
    /**
     * Controls whether the menu is open (controlled mode).
     * When provided, the component becomes controlled and the consumer
     * is responsible for managing the open state.
     */
    isOpen?: boolean;
    /**
     * Callback fired when the menu open state changes.
     * Use alongside `isOpen` for controlled mode.
     */
    onOpenChange?: (isOpen: boolean) => void;
}
/**
 * Select is a component that allows users to select an option from a list.
 * It is a custom component built with a combination of the `Menu`, `ListItem` and `MenuItem` Living Design components.
 *
 *
 * ### Content
 *
 * Select is a controlled component i.e., the state is controlled by the `value` prop and `onChange` callback passed from the parent component.
 *
 * Select accepts the following props:
 *  - `options`: List of items for the Select field. This can either be an array of options OR an array of objects with a `key` and `label`
 *  - `onBlur`: The callback executed when the select field is blurred. Since this is a custom component, the onBlur callback is called when the menu is closed (Optional)
 *  - `onChange`: Callback to send the selected option
 *  - `label`: The label for the Select field. (Optional)
 *  - `placeholder`: The placeholder for the Select field. (Optional)
 *  - `leadingIcon`: The leading content for the list item. (Optional)
 *  - `disabled`: Flag to disable the Select field. (Optional)
 *  - `helperText`: The helper text for the Select field.  (Optional)
 *  - `error`: The error for the Select field. (Optional)
 *  - `size`: The size of the list item. (Optional)
 *  - `value`: The value for the component. Passing value will also preselect the option. (Optional)
 *  - `menuTriggerRef`: The ref for the select menu (Optional)
 *  - `maxMenuHeight`: The maximum height for the menu (Optional)
 *  - `checkPosition`: Position of the check icon (left or right). (Optional)
 *
 * ### Usage
 *
 * ```tsx
 *
 * import { Select } from '@walmart-dataventures/shared-components';
 *
 * const options = ['January', 'February', 'March'];
 *
 * <Select
 *   label="Select an option"
 *   options={options}
 *   onChange={() => console.log}
 * />
 *
 * ```
 *
 * OR
 *
 * ```tsx
 *
 * import { Select } from '@walmart-dataventures/shared-components';
 *
 * const options = [
 *  { key: 'jan', label: 'January' }
 *  { key: 'feb', label: 'February' }
 *  { key: 'mar', label: 'March' }
 * ];
 *
 * <Select
 *   label="Select an option"
 *   options={options}
 *   onChange={() => console.log}
 * />
 *
 * ```
 * */
export declare const Select: ({ className, label, placeholder, leadingIcon, options, onChange, disabled, helperText, error, size, value, horizontalLabel, isFullWidth, menuTriggerRef, onBlur, maxMenuHeight, menuPosition, scrollableDivId, checkPosition, id: initialId, zIndex, isOpen: controlledIsOpen, onOpenChange, ...rest }: SelectProps) => import("react/jsx-runtime").JSX.Element;
export declare const LuminateSelect: React.FC<SelectProps>;
