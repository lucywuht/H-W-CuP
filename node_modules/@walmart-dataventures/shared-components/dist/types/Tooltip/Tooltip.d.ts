import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
import { PlacementType } from './positioning';
declare const tooltipManager: {
    generateId: () => string;
    show: (id: string, hideFunction: () => void, updatePositionFunction: () => void) => void;
    hide: (id: string) => void;
    hideAll: () => void;
    updateCurrentPosition: () => void;
};
export interface ITooltipProps extends CommonProps {
    /** The content to be displayed in the tooltip */
    content: string;
    /** The placement of the tooltip relative to its trigger element */
    placement?: PlacementType;
    /** The maximum width of the tooltip */
    maxWidth?: string;
    /** The children element that triggers the tooltip */
    children?: React.ReactElement;
    /** Ref to an arbitrary DOM node to trigger the tooltip */
    triggerRef?: React.RefObject<HTMLElement>;
    /** Whether the tooltip is disabled */
    disabled?: boolean;
    /** Z-index for tooltip positioning */
    zIndex?: number;
}
declare const Tooltip: React.NamedExoticComponent<ITooltipProps>;
/**
 * Tooltip component for displaying contextual information
 */
export { Tooltip };
/**
 * @internal
 * Tooltip manager for internal state coordination.
 *
 * ⚠️ WARNING: This is an internal API that may change without notice.
 * Only exposed for advanced use cases.
 *
 * NOTE: Automatic cleanup happens in Jest tests - no setup needed!
 */
export { tooltipManager };
export type { PlacementType } from './positioning';
