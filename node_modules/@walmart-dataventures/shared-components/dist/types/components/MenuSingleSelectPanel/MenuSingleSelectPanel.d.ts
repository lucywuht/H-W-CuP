import React from 'react';
import { IDvMenuSingleSelectPanelPropsWithInfiniteLoading } from './MenuSingleSelectPanel.types';
/**
 * A standalone single-select menu panel that can be attached to any trigger element.
 *
 * Unlike `MenuSingleSelect`, this component does not include a built-in trigger.
 * The consumer controls visibility, positioning (via `anchorRef`), and closing.
 *
 * @param props - Component props
 * @returns The rendered menu panel
 *
 * @example
 * ```tsx
 * <MenuSingleSelectPanel
 *   anchorRef={buttonRef}
 *   options={options}
 *   selectedOptionId={selectedId}
 *   onSelect={(id) => setSelectedId(id)}
 *   onClose={() => setIsOpen(false)}
 * />
 * ```
 */
export declare const MenuSingleSelectPanel: React.ForwardRefExoticComponent<IDvMenuSingleSelectPanelPropsWithInfiniteLoading & React.RefAttributes<HTMLDivElement>>;
