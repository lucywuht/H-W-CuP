import React from 'react';
import { IGroupedTableProps } from './GroupedTable.types';
/**
 * A flexible table component that supports row grouping with rowspan functionality.
 * Perfect for displaying survey results, questionnaire data, and any tabular data
 * that needs logical grouping with visual consolidation.
 * This is just a sample story, it's not a component to be imported and used by feature applications.
 *
 * @example
 * ```tsx
 * const columns = [
 *   { key: 'category', header: 'Category', width: '200px' },
 *   { key: 'rating', header: 'Rating', width: '100px', align: 'center' },
 * ];
 *
 * const data = [
 *   { id: 1, category: 'Quality', rating: '95%' },
 *   { id: 2, category: 'Quality', rating: '87%' },
 *   { id: 3, category: 'Design', rating: '92%' },
 * ];
 *
 * <GroupedTable
 *   columns={columns}
 *   data={data}
 *   groupBy="category"
 *   spanColumns={['category']}
 * />
 * ```
 */
export declare const GroupedTable: React.ForwardRefExoticComponent<IGroupedTableProps & React.RefAttributes<HTMLTableElement>>;
export default GroupedTable;
