import * as LivingDesignIcons from '@livingdesign/icons';
import { SpotIconSize } from '@walmart-web/livingdesign-components';
import * as React from 'react';
import { CommonProps } from '../../utility/commonProps';
export type SpotIconSizeExtended = SpotIconSize | 'medium';
export type SpotIconColor = 'white' | 'purple' | 'gray';
interface ISpotIconProps extends CommonProps {
    /** SpotIcon children for icon */
    childrenProp?: keyof typeof LivingDesignIcons;
    /**
     * Background color of the spotIcon
     * @default 'purple'
     * @deprecated 'blue' color will be removed in v2.0.0, use 'purple' instead
     */
    color?: SpotIconColor;
    /**
     * Size of spotIcon
     * - `small`: 16px (default)
     * - `medium`: 24px
     * - `large`: 32px
     * @default 'small'
     */
    size?: SpotIconSizeExtended;
    /**
     * Whether icon is decorative (hidden from screen readers) or informative
     * - `true`: Icon is decorative, hidden from assistive technology
     * - `false`: Icon is informative, announced to screen readers
     * @default false
     */
    decorative?: boolean;
}
/**
 * Spot Icons are decorative elements that add visual interest to messaging or other screen elements such as list items.
 *
 * - {@link https://livingdesign.walmart.com/components/spot-icon/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/spot-icon/react/ React documentation}
 *
 * ### Content
 *
 *
 * The Spot icon accepts ChildrenProp color and size props.
 *
 * ```tsx
 * <SpotIcon
 *  childrenProp: 'Home',
 *   size: 'small',
 *   color: 'purple'
 *   decorative: false
 * />
 *```
 *
 * ### Features
 *
 *
 * ### ChildrenProps
 *
 *
 * The spot icon children are icons from the living design icons.
 *
 * #### Size
 *
 *
 * The Spot icon comes in three sizes:
 *
 * - `small` (default) 16px
 * - `medium` 24px
 * - `large` 32px
 *
 * ### color
 *
 *
 * The Spot icon has 3 colors avalable as backgrounds
 *  - 'purple' (default)
 *  - 'white'
 *  - 'gray'
 *
 * */
export declare const SpotIcon: React.FC<ISpotIconProps>;
export default SpotIcon;
export type { ISpotIconProps };
