import { ChangeEvent, KeyboardEvent } from 'react';
import { TextFieldProps } from '../..';
import { IOption, SearchBoxVariant } from './SearchBoxWithChips';
export type TextFieldInputType = 'email' | 'number' | 'password' | 'search' | 'tel' | 'text' | 'time' | 'url';
export interface ISearchBoxProps extends Omit<TextFieldProps, 'onChange'> {
    /**
     * The callback fired when the close icon is clicked.
     */
    onCloseIconClick: () => void;
    /**
     * The callback fired when the search box requests to change.
     */
    onKeyDown: (event: KeyboardEvent<HTMLElement>) => void;
    /**
     * The callback fired when input value changes (optional when variant is provided).
     */
    onChange?: (event: ChangeEvent<HTMLInputElement>) => void;
    /**
     * The variant of the search box.
     * When provided, enables chips or dropdown functionality
     * - 'chips': User types and hits enter to add chips
     * - 'dropdown': User selects from dropdown options
     */
    variant?: SearchBoxVariant;
    /**
     * Array of current chip values (for chips variant).
     */
    chips?: string[];
    /**
     * Array of options for dropdown variant.
     */
    options?: IOption[];
    /**
     * The selected option for dropdown variant.
     */
    selectedOption?: IOption | null;
    /**
     * The callback fired when a chip is added (chips variant).
     */
    onChipAdd?: (value: string) => void;
    /**
     * The callback fired when a chip is removed.
     */
    onChipRemove?: (chipValue: string) => void;
    /**
     * The callback fired when an option is selected (dropdown variant).
     */
    onOptionSelect?: (option: IOption | null) => void;
    /**
     * The callback fired when the search text changes.
     */
    onSearchTextChange?: (value: string) => void;
    /**
     * Text to show when no results found in dropdown.
     */
    noResultsText?: string;
    /**
     * Description for no results found.
     */
    noResultsDescription?: string;
    /**
     * The callback fired when clear all is clicked.
     */
    onClearAll?: () => void;
    /**
     * The callback fired when custom action is clicked.
     */
    onCustomAction?: () => void;
    /**
     * Text for the custom action button.
     */
    customActionText?: string;
    /**
     * Minimum input length to show dropdown suggestions.
     */
    minInputLength?: number;
    /**
     * Maximum character limit for the text field.
     */
    characterLimit?: number;
    /**
     * Applies a fully rounded (pill-shaped) border to the search box.
     * @default false
     * @remarks Only applies to the default variant. Not supported for the `chips` or `dropdown` variants by design.
     */
    rounded?: boolean;
}
/**
 * Search box allow users to search text.
 *
 * {@link https://livingdesign.walmart.com/components/text-field/ Guidelines}
 * {@link https://livingdesign.walmart.com/components/text-field/react/ React documentation}
 *
 * ### Content
 *
 * #### Label
 *
 * Search Box requires a label to be accessible. Use the `label` property to add a label:
 *
 * ```tsx
 * <SearchBox
 *   label="Search"
 *   onChange={(event) => console.log(event)}
 * />
 * ```
 *
 * #### Leading and trailing
 *
 * Clarify the Seach Box's purpose by adding an icon with the `leadingIcon` prop:
 *
 * ```tsx
 * import React from "react";
 * import { Search } from '@livingdesign/icons';
 *
 * export default () => {
 *   return (
 *     <SearchBox
 *       label="search"
 *       leadingIcon={<Search size="small" />}
 *       onChange={(event) => console.log(event)}
 *     />
 *   );
 * };
 * ```
 *
 * Enhance Search Box's functionality by passing additional content to the `trailing` slot:
 *
 * ```tsx
 * import React from "react";
 * import { Close } from '@livingdesign/icons';
 *
 * export default () => {
 *   return (
 *     <SearchBox
 *       label="Search"
 *       onChange={(event) => console.log(event)}
 *       trailing={<Close size="small" />}
 *     />
 *   );
 * };
 * ```
 *
 * ### Interaction
 *
 * Configure the Search Box's value using the `value` property. Respond to changes by passing an callback handler to the `onChange` prop:
 *
 * ```tsx
 * import * as React from "react";
 *
 * export default () => {
 *   const [value, setValue] = useState("CA");
 *
 *   return (
 *     <SearchBox
 *       label="Full name"
 *       onChange={(event) => setValue(event.target.value)}
 *       value={value}
 *     />
 *   );
 * };
 * ```
 */
export declare const SearchBox: import("react").ForwardRefExoticComponent<ISearchBoxProps & import("react").RefAttributes<HTMLInputElement>>;
