/**
 * MenuPortal
 *
 * SSR-safe portal that renders children into document.body.
 * Portaling to document.body ensures `position: fixed` on the dropdown is always
 * relative to the viewport, regardless of CSS transforms on ancestors.
 *
 * Follows the same pattern as TooltipPortal and DVPopoverPortal — defers DOM access
 * to useLayoutEffect so server renders produce no output and hydration is clean.
 */
import React from 'react';
interface IMenuPortalProps {
    children: React.ReactNode;
}
export declare const MenuPortal: React.FC<IMenuPortalProps>;
export {};
