import * as React from 'react';
interface Props<T> {
    children: (state: T, setState: React.Dispatch<React.SetStateAction<T>>) => React.ReactElement;
    initialState: T;
}
declare const WithState: <T extends {}>(props: Props<T>) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
export default WithState;
