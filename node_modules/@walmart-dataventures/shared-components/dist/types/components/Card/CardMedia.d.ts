import React from 'react';
import { ICardMediaProps } from './types';
/**
 * A `CardMedia` component for displaying images or videos at the top of a card.
 * Supports responsive media that fits within the card's dimensions.
 *
 * @component
 * @param {ICardMediaProps} props - The properties for the `CardMedia` component.
 * @returns {JSX.Element} A media container for card content.
 *
 * @property {string} src - The source URL for the image or video.
 * @property {'image' | 'video'} type - The type of media to display.
 * @property {string} alt - Alt text for images (required for accessibility).
 * @property {string} className - Additional CSS class names.
 * @property {'small' | 'large'} size - Size variant matching the parent card.
 * @property {ICardMediaVideoProps} videoProps - Additional video element props with optional captions support.
 * @property {React.ImgHTMLAttributes<HTMLImageElement>} imageProps - Additional image element props.
 */
export declare const DvCardMedia: React.FC<ICardMediaProps>;
