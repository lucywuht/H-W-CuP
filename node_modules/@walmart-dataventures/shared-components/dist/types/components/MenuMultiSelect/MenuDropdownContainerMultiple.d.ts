import React from 'react';
import { IMenuHeader, IMenuOption, IMenuTabOption, InfiniteLoadingProps } from './MenuMultiSelect.types';
export interface IMenuDropdownContainerMultipleProps {
    /** Whether the dropdown menu is open */
    isOpen: boolean;
    /** Ref to the menu container element */
    menuRef: React.RefObject<HTMLDivElement>;
    /** Ref to the trigger button element — used to read a live viewport rect when
     *  search/tab changes trigger a re-layout, preventing stale-position jumps after
     *  the user has scrolled the page since the menu was opened. */
    triggerRef: React.RefObject<HTMLButtonElement>;
    /** Maximum height for the dropdown menu */
    maxHeight?: string | number;
    /** Test identifier for testing purposes */
    testId?: string;
    /** Menu header configuration with title and action buttons */
    menuHeader?: IMenuHeader;
    /** Whether to show tab navigation */
    showTabs?: boolean;
    /** Array of tab configuration objects */
    tabs?: IMenuTabOption[];
    /** Currently active tab ID */
    activeTabId?: string;
    /** Handler for tab change events */
    safeOnTabChange: (tabId: string) => void;
    /** Whether to show search functionality */
    showSearch?: boolean;
    /** Filtered array of selectable options */
    filteredOptions: IMenuOption[];
    /** Ref to the search input element */
    searchRef?: React.RefObject<HTMLInputElement>;
    /** Placeholder text for search input */
    searchPlaceholder?: string;
    /** Current search query value */
    searchQuery: string;
    /** Handler for search input changes */
    handleSearchChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /** Handler for clearing search input */
    handleSearchClear: () => void;
    /** Handler for search input keyboard events */
    handleSearchKeyDown: (event: React.KeyboardEvent<HTMLElement>) => void;
    /** Map of item handlers keyed by option ID */
    itemHandlers: Record<string, {
        onToggle: () => void;
    }>;
    /** Selected option IDs */
    selectedOptionIds: string[];
    /** Whether to show section headers in the menu */
    showSectionHeaders?: boolean;
    /** Parent position for dropdown positioning */
    parentPosition: DOMRect | null;
    /** Apply handler - receives current selected IDs */
    onApply: (selectedIds: string[]) => void;
    /** Cancel handler */
    onCancel: () => void;
    /** Text for the Cancel button */
    cancelText: string;
    /** Text for the Apply button */
    applyText: string;
    /** Handle Select All functionality */
    onSelectAll: () => void;
    /** Handle Clear All functionality */
    onClearAll: () => void;
    /** All options for select/clear all calculations */
    allOptions: IMenuOption[];
    /** Whether the dropdown should be scrollable when content exceeds maxHeight */
    isScrollable?: boolean;
    /** Whether to show loading state */
    isLoading?: boolean;
    /** Text for the Select All button */
    selectAllText: string;
    /** Text for the Clear All button */
    clearAllText: string;
    /** Text for the Selected label */
    selectedLabel: string;
}
export type IMenuDropdownContainerMultiplePropsWithInfiniteLoading = IMenuDropdownContainerMultipleProps & InfiniteLoadingProps;
declare const MenuDropdownContainerMultiple: React.FC<IMenuDropdownContainerMultiplePropsWithInfiniteLoading>;
export default MenuDropdownContainerMultiple;
