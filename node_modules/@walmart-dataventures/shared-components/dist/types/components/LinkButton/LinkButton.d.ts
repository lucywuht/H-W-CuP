import * as React from 'react';
import { CommonProps } from '../../utility/commonProps';
export type LinkButtonSize = 'small' | 'medium' | 'large';
export type LinkButtonType = 'button' | 'reset' | 'submit';
export type LinkButtonColor = 'default' | 'subtle' | 'white';
interface ILinkButtonBaseProps extends CommonProps {
    /**
     * The content for the link button.
     */
    children: React.ReactNode;
    /**
     * The color for the link button.
     *
     * @default "default"
     */
    color?: LinkButtonColor;
    /**
     * If the link button is displayed at full width.
     *
     * @default false
     */
    isFullWidth?: boolean;
    /**
     * The leading icon for the link button.
     */
    leading?: React.ReactNode;
    /**
     * The size for the link button.
     *
     * @default "small"
     */
    size?: LinkButtonSize;
    /**
     * The trailing icon for the link button.
     */
    trailing?: React.ReactNode;
}
export type LinkButtonAnchorProps = ILinkButtonBaseProps & Omit<React.JSX.IntrinsicElements['a'], 'className' | 'style'> & {
    /**
     * The href for the link button (Anchor only).
     */
    href: string;
    /**
     * If the link button is disabled (applies to both anchor and button).
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * Indicates the current page in a navigation context.
     * @see https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-current
     */
    'aria-current'?: 'page' | 'step' | 'location' | 'date' | 'time' | 'true' | 'false';
    /**
     * The target for the link button (Anchor only).
     */
    target?: string;
    /**
     * The rel attribute for the link button (Anchor only).
     */
    rel?: string;
    /**
     * The callback fired when the link button is clicked (Anchor only).
     */
    onClick?: (event: React.MouseEvent<HTMLAnchorElement>) => void;
};
export type LinkButtonButtonProps = ILinkButtonBaseProps & Omit<React.JSX.IntrinsicElements['button'], 'className' | 'style'> & {
    /**
     * If the link button is disabled (Button only).
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * The type for the link button (Button only).
     *
     * @default "button"
     */
    type?: LinkButtonType;
    /**
     * The callback fired when the link button is clicked (Button only).
     */
    onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;
};
export type LinkButtonProps = (LinkButtonAnchorProps & {
    type?: never;
}) | (LinkButtonButtonProps & {
    href?: never;
});
/**
 * Link Buttons allow users to take actions and make choices, with a single press or a tap. Link Buttons are used as secondary navigation elements that provide a bridge between traditional buttons and links.
 *
 * - {@link https://livingdesign.walmart.com/components/link-button/ Guidelines}
 * - {@link https://digitaltoolkit.livingdesign.walmart.com/develop/react/components/link-button/ React documentation}
 *
 * ### Content
 *
 * #### Leading and trailing icons
 *
 * The Link Button accepts optional leading and trailing icons through the `leading` and `trailing` props:
 *
 * ```tsx
 * import * as React from "react";
 * import { LinkButton } from "@walmart-web/livingdesign-components";
 * import * as Icon from "@livingdesign/icons";
 *
 * export default () => {
 *   return (
 *     <LinkButton
 *       leading={<Icon.Home />}
 *       trailing={<Icon.ChevronRight />}
 *       href="https://www.walmart.com/cart"
 *     >
 *       Navigate to Home
 *     </LinkButton>
 *   );
 * }
 *```
 *
 * ### Features
 *
 * #### Color
 *
 * The Link Button comes in three colors:
 *
 * - `default` (default): Standard dark color for most use cases
 * - `subtle`: Lighter color for secondary actions
 * - `white`: For use on dark backgrounds
 *
 * Set the Link Button's color with the `color` prop:
 *
 * ```tsx
 * <LinkButton color="default">Default</LinkButton>
 * <LinkButton color="subtle">Subtle</LinkButton>
 * <LinkButton color="white">White</LinkButton>
 *```
 *
 * #### Size
 *
 * The Link Button comes with three sizes:
 *
 * - `small` (default): 32px height
 * - `medium`: 40px height
 * - `large`: 48px height
 *
 * Set the Link Button's size with the `size` prop:
 *
 * ```tsx
 * <LinkButton size="small">Small</LinkButton>
 * <LinkButton size="medium">Medium</LinkButton>
 * <LinkButton size="large">Large</LinkButton>
 *```
 *
 * ### Interaction
 *
 * #### Linking
 *
 * The Link Button can be configured to be an `HTMLAnchorElement` or an `HTMLButtonElement` depending on the props passed to it.
 *
 * Provide an `href` prop to make the Link Button an `HTMLAnchorElement`:
 *
 * ```tsx
 * <LinkButton href="https://www.walmart.com/">
 *   Walmart Home
 * </LinkButton>
 * ```
 *
 * Otherwise, the Link Button will be rendered as an `HTMLButtonElement` and accept the `type` and `disabled` attributes as props:
 *
 * ```tsx
 * <LinkButton type="submit" disabled>Submit</LinkButton>
 * ```
 *
 * ### Layout
 *
 * #### Width
 *
 * The Link Button can be configured to fill the width of its parent using the `isFullWidth` prop:
 *
 * ```tsx
 * <LinkButton isFullWidth>Full width</LinkButton>
 * ```
 */
export declare const LinkButton: React.MemoExoticComponent<React.ForwardRefExoticComponent<(Omit<ILinkButtonBaseProps & Omit<React.DetailedHTMLProps<React.AnchorHTMLAttributes<HTMLAnchorElement>, HTMLAnchorElement>, "className" | "style"> & {
    /**
     * The href for the link button (Anchor only).
     */
    href: string;
    /**
     * If the link button is disabled (applies to both anchor and button).
     *
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * Indicates the current page in a navigation context.
     * @see https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-current
     */
    'aria-current'?: "page" | "false" | "true" | "time" | "step" | "location" | "date" | undefined;
    /**
     * The target for the link button (Anchor only).
     */
    target?: string | undefined;
    /**
     * The rel attribute for the link button (Anchor only).
     */
    rel?: string | undefined;
    /**
     * The callback fired when the link button is clicked (Anchor only).
     */
    onClick?: ((event: React.MouseEvent<HTMLAnchorElement>) => void) | undefined;
} & {
    type?: undefined;
}, "ref"> | Omit<ILinkButtonBaseProps & Omit<React.DetailedHTMLProps<React.ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>, "className" | "style"> & {
    /**
     * If the link button is disabled (Button only).
     *
     * @default false
     */
    disabled?: boolean | undefined;
    /**
     * The type for the link button (Button only).
     *
     * @default "button"
     */
    type?: LinkButtonType | undefined;
    /**
     * The callback fired when the link button is clicked (Button only).
     */
    onClick?: ((event: React.MouseEvent<HTMLButtonElement>) => void) | undefined;
} & {
    href?: undefined;
}, "ref">) & React.RefAttributes<HTMLButtonElement | HTMLAnchorElement>>>;
export {};
