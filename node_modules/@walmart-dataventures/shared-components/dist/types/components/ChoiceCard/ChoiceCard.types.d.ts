import { AriaAttributes, HTMLAttributes, ReactNode } from 'react';
import type { CommonProps } from '../../utility/commonProps';
/**
 * Base properties for ChoiceCard component.
 * Extends CommonProps and HTMLDivElement attributes with custom choice-specific props.
 */
interface IChoiceCardBaseProps extends CommonProps, Omit<HTMLAttributes<HTMLDivElement>, 'onChange'>, Pick<AriaAttributes, 'aria-label' | 'aria-labelledby' | 'aria-describedby'> {
    /** The label text for the choice - used as the accessible name for the input control */
    label: string;
    /** Whether the choice is currently selected/checked */
    checked?: boolean;
    /** Whether the choice is disabled and non-interactive */
    disabled?: boolean;
    /** The value attribute for the input element - submitted with form data */
    value?: string;
    /**
     * Header content slot (optional) - appears to the right of the input/label.
     * Typically used for badges, icons, or supplementary information.
     */
    headerContent?: ReactNode;
    /** Whether to enable/show the header content area - defaults to true */
    enableHeaderContent?: boolean;
    /**
     * Body content slot (required) - appears below the header row.
     * Main content area for descriptions, details, or additional UI elements.
     */
    bodyContent: ReactNode;
    /**
     * Change event handler - called when the input selection state changes.
     * @param event - The change event from the underlying input element
     */
    onChange?: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /**
     * Focus event handler - called when the input receives focus.
     * @param event - The focus event from the underlying input element
     */
    onFocus?: (event: React.FocusEvent<HTMLInputElement>) => void;
    /**
     * Blur event handler - called when the input loses focus.
     * @param event - The blur event from the underlying input element
     */
    onBlur?: (event: React.FocusEvent<HTMLInputElement>) => void;
    /** Additional CSS classes to apply to the card container */
    className?: string;
    /**
     * Optional width for the card - can be a CSS value string or number (pixels).
     * @example "300px", "20rem", 400
     */
    width?: string | number;
    /**
     * Hide the radio/checkbox control icon while keeping the label visible.
     * Useful for custom visual designs where the card itself indicates selection.
     */
    hideControl?: boolean;
    /**
     * Hide the radio/checkbox control label text while keeping the input functional.
     * The input remains accessible but the label text is visually hidden.
     */
    hideControlLabel?: boolean;
    /** Test identifier for automated testing - defaults to 'choice-card' */
    'data-testid'?: string;
    /**
     * External control for hover state - when provided, overrides internal hover state management.
     * Useful for programmatic state control or testing.
     */
    isHovered?: boolean;
    /**
     * External control for focus state - when provided, displays focus styling.
     * Focus behavior is typically managed by the underlying input components.
     * Useful for testing or special focus state demonstrations.
     */
    isFocused?: boolean;
}
/**
 * Props for ChoiceCard when used as a radio button.
 * Radio buttons are used for single-select scenarios where only one option can be chosen.
 */
export type ChoiceCardRadioProps = IChoiceCardBaseProps & {
    /** The type of input control to use - defaults to 'radio' */
    inputType?: 'radio';
    /**
     * The name attribute for the input (required for radio buttons).
     * Groups radio buttons together for single-select behavior.
     */
    name: string;
};
/**
 * Props for ChoiceCard when used as a checkbox.
 * Checkboxes are used for multi-select scenarios where multiple options can be chosen.
 */
export type ChoiceCardCheckboxProps = IChoiceCardBaseProps & {
    /** The type of input control to use - must be 'checkbox' for this variant */
    inputType: 'checkbox';
    /** The name attribute for the input (optional for checkboxes) */
    name?: string;
};
/**
 * Union type representing all possible ChoiceCard component props.
 * Automatically infers the correct prop requirements based on inputType.
 */
export type ChoiceCardProps = ChoiceCardRadioProps | ChoiceCardCheckboxProps;
/**
 * Supported input types for the ChoiceCard component.
 */
export type ChoiceCardInputType = 'radio' | 'checkbox';
export {};
