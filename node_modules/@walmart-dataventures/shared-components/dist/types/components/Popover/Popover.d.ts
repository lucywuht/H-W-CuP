import { PopoverProps as LDPopoverProps } from '@walmart-web/livingdesign-components';
import { FC } from 'react';
/**
 * Popover component
 *
 * ###Content
 *
 * Popovers are overlays that display contextual content on demand.
 *
 * - {@link https://livingdesign.walmart.com/components/popover/ Guidelines}
 * - {@link https://digitaltoolkit.livingdesign.walmart.com/develop/react/components/popover/ React documentation}
 *
 * ```tsx
 * const ref = React.useRef(null);
 * const [isOpen, setIsOpen] = React.useState(false);
 * <Popover
 *  onClose={() => setIsOpen(false)}
 *  triggerRef={ref}
 *  hasNubbin
 *  content={getContent()}
 *  position='left'
 *  isOpen={isOpen}
 * >
 *  <Link ref={ref} onClick={()=> setIsOpen(!isOpen)}><span>Popover at left.</span></Link>
 * </Popover>
 *```
 * */
export type PopoverProps = LDPopoverProps;
export declare const Popover: FC<PopoverProps>;
