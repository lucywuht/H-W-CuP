/**
 * Validates a file's type against the accepted file types
 * @param file The file to validate
 * @param acceptedTypes Accepted file types object from react-dropzone
 * @returns boolean indicating if the file type is valid
 */
export declare const isValidFileType: (file: File, acceptedTypes: Record<string, string[]>) => boolean;
/**
 * Formats a file size in bytes into a human-readable string
 * @param bytes File size in bytes
 * @param decimals Number of decimal places to show
 * @returns Formatted string (e.g., "1.5 KB")
 */
export declare const formatFileSize: (bytes: number, decimals?: number) => string;
/**
 * Checks if the total size of files exceeds the maximum total size
 * @param files Array of files to check
 * @param maxTotalSize Maximum total size in bytes
 * @returns boolean indicating if the total size is valid
 */
export declare const isValidTotalSize: (files: File[], maxTotalSize: number) => boolean;
