/// <reference types="react" />
import { CellContext } from '@tanstack/react-table';
import { ValueChangeProps } from '../../../../ValueChange';
export type TrendDirection = 'Up' | 'Down';
export interface IValueChangeCellProps<TData, TValue> {
    /**
     * Cell context from Tanstack Table
     */
    cell: CellContext<TData, TValue>;
    /**
     * ValueChange component props
     */
    valueChangeProps: ValueChangeProps;
}
/**
 * A cell renderer that displays content as a ValueChange component
 *
 * @example
 * // Basic usage - displays a value change with trend indicator
 * <ValueChangeCell
 *   cell={cell}
 *   valueChangeProps={{
 *     value: "12.5%",
 *     trendDirection: "Up"
 *   }}
 * />
 *
 * @example
 * // For negative changes
 * <ValueChangeCell
 *   cell={cell}
 *   valueChangeProps={{
 *     value: "8.4%",
 *     trendDirection: "Down"
 *   }}
 * />
 */
export declare const ValueChangeCell: <TData, TValue>({ cell, valueChangeProps, }: IValueChangeCellProps<TData, TValue>) => JSX.Element;
