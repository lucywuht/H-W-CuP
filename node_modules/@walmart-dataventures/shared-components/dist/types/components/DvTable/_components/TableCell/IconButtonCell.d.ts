/// <reference types="react" />
import { Cell } from '@tanstack/react-table';
import { IconButtonButtonProps } from '../../../../IconButton/IconButton';
export interface IIconButtonCellProps<TData, TValue> {
    /**
     * The cell object from React Table
     */
    cell: Cell<TData, TValue>;
    /**
     * Props to pass to the Icon component
     */
    iconButtonProps?: IconButtonButtonProps;
}
/**
 * A cell renderer that displays content within an Icon component
 *
 * @example
 * // Basic usage - displays cell value as an icon
 * <IconButtonCell cell={icon} />
 */
export declare const IconButtonCell: <TData, TValue>({ cell, iconButtonProps, }: IIconButtonCellProps<TData, TValue>) => JSX.Element | null;
