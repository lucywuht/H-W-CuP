/// <reference types="react" />
import { CellContext } from '@tanstack/react-table';
import { TableRowType } from '../../types/table.types';
export interface ICheckboxCellProps<TData extends TableRowType, TValue = unknown> {
    /**
     * Cell context from Tanstack Table
     */
    cell: CellContext<TData, TValue>;
    /**
     * Callback when row selection changes
     * Provides the row data and selection state
     */
    onRowSelectionChange: (row: TData, isSelected: boolean) => void;
    /**
     * Optional prop to disable checkbox
     * @default false
     */
    disabled?: boolean;
    /**
     * Optional className to add to the checkbox container
     */
    className?: string;
    /**
     * Aria label for the checkbox
     * @default "Select row"
     */
    ariaLabel?: string;
    /**
     * label for the checkbox
     * @default " "
     */
    label?: string;
}
/**
 * A cell renderer that displays a checkbox for row selection
 *
 * @example
 * // Basic usage - column definition
 * {
 *   id: 'select',
 *   header: ({ table }) => (
 *     <CheckboxHeaderCell table={table} />
 *   ),
 *   cell: ({ row }) => (
 *     <CheckboxCell cell={{ row }} onRowSelectionChange={...} />
 *   ),
 *   size: 40,
 * }
 *
 * @remarks
 * If you use row selection in a controlled way, you must provide the `onRowSelectionChange` callback. Omitting it will cause the component to switch between controlled and uncontrolled modes, which is not supported and will log a warning in development mode.
 */
export declare const CheckboxCell: <TData extends {
    readonly id: string | number;
}, TValue = unknown>({ cell, onRowSelectionChange, disabled, className, ariaLabel, label, }: ICheckboxCellProps<TData, TValue>) => JSX.Element;
