/// <reference types="react" />
import { IDvTableBodyProps } from '../../types/table.types';
export declare const TableBody: <T extends {
    readonly id: string | number;
}>({ rows, className, tableRef, emptyState, noResults, isFiltered, loadingComponent, ...props }: IDvTableBodyProps<T>) => JSX.Element;
