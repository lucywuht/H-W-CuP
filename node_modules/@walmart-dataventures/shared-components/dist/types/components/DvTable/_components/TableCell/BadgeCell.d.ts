/// <reference types="react" />
import { CellContext } from '@tanstack/react-table';
import { BadgeProps } from '../../../..';
export interface IBadgeCellProps<TData, TValue> {
    /**
     * Cell context from Tanstack Table
     */
    cell: CellContext<TData, TValue>;
    /**
     * Cell role for accessibility
     */
    role?: string;
    /**
     * Badge component props
     *
     * Note: The cell value will be used as the Badge's children by default.
     * You can override this by providing a 'children' prop in badgeProps.
     */
    badgeProps?: Partial<BadgeProps>;
}
/**
 * A cell renderer that displays content within a Badge component
 *
 * @example
 * // Basic usage - displays cell value in a Badge
 * <BadgeCell cell={cell} />
 *
 * @example
 * // With custom Badge props
 * <BadgeCell
 *   cell={cell}
 *   badgeProps={{
 *     color: 'blue',
 *   }}
 * />
 *
 * @example
 * // Override cell value with custom children
 * <BadgeCell
 *   cell={cell}
 *   badgeProps={{
 *     color: 'blue',
 *     children: <span>Custom content</span>
 *   }}
 * />
 */
type BadgeCellComponent = {
    <TData, TValue>(props: IBadgeCellProps<TData, TValue>): JSX.Element;
    displayName?: string;
};
export declare const BadgeCell: BadgeCellComponent;
export {};
