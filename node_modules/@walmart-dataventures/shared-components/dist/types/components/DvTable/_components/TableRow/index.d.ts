/// <reference types="react" />
import { Row, TableRowType } from '../../types/table.types';
interface ITableRowProps<T extends TableRowType> {
    row: Row<T>;
    className?: string;
    isRowClickable?: boolean;
    onRowClick?: (row: T) => void;
}
export declare const TableRow: <T extends {
    readonly id: string | number;
}>({ row, className, isRowClickable, onRowClick, }: ITableRowProps<T>) => JSX.Element;
export default TableRow;
