/// <reference types="react" />
import { CellContext } from '@tanstack/react-table';
import { TagProps } from '../../../../Tag';
export interface ITagCellProps<TData, TValue> {
    /**
     * Cell context from Tanstack Table
     */
    cell: CellContext<TData, TValue>;
    /**
     * Tag component props
     *
     * Note: The cell value will be used as the Tag's children by default.
     * You can override this by providing a 'children' prop in tagProps.
     */
    tagProps?: Partial<TagProps>;
    /**
     * Maximum number of tags to show before showing "+X more"
     * If not provided, it will auto-calculate based on available width
     */
    maxVisibleTags?: number;
}
/**
 * A cell renderer that displays content within Tag components with overflow handling
 *
 * @example
 * // Basic usage - displays cell value in a Tag
 * <TagCell cell={cell} />
 *
 * @example
 * // With custom Tag props
 * <TagCell
 *   cell={cell}
 *   tagProps={{
 *     color: 'green',
 *   }}
 * />
 *
 * @example
 * // With array of tags
 * <TagCell
 *   cell={cell}
 *   tagProps={{ color: 'blue' }}
 *   maxVisibleTags={3}
 * />
 */
export declare const TagCell: <TData, TValue>({ cell, tagProps, maxVisibleTags, }: ITagCellProps<TData, TValue>) => JSX.Element;
