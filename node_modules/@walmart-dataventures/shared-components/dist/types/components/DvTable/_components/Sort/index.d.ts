import { Header } from '@tanstack/react-table';
interface ISortProps<T extends object> {
    header: Header<T, unknown>;
}
declare const Sort: <T extends object>({ header }: ISortProps<T>) => import("react/jsx-runtime").JSX.Element;
export default Sort;
