import { CellContext } from '@tanstack/react-table';
import { ReactNode } from 'react';
import { TableRowType } from '../../types/table.types';
export interface IDescriptionCellProps<TData extends TableRowType, TValue = unknown> {
    /**
     * Cell context from Tanstack Table
     */
    cell: CellContext<TData, TValue>;
    /**
     * Description text to show below the main content
     */
    description: ReactNode;
}
/**
 * A cell renderer that displays a primary value with a description underneath
 * Description is only shown in non-compact mode
 *
 * @example
 * // Basic usage
 * <DescriptionCell
 *   cell={info}
 *   description="This is a description"
 * />
 */
export declare const DescriptionCell: <TData extends {
    readonly id: string | number;
}, TValue = unknown>({ cell, description, }: IDescriptionCellProps<TData, TValue>) => JSX.Element;
export default DescriptionCell;
