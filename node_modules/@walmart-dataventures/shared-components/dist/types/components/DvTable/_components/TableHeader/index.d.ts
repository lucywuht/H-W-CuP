import { HeaderGroup } from '@tanstack/react-table';
import * as React from 'react';
import { TableRowType } from '../../types/table.types';
interface ITableHeaderProps<T extends TableRowType> {
    headerGroups: HeaderGroup<T>[];
    className?: string;
    compact?: boolean;
    stickyHeader?: boolean;
}
export declare const TableHeader: <T extends {
    readonly id: string | number;
}>({ headerGroups, className, compact, stickyHeader, }: ITableHeaderProps<T>) => React.ReactElement;
export {};
