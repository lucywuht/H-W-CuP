import { CommonProps } from '../../utility/commonProps';
export type LegendShapeType = 'square' | 'circle' | 'line' | 'lineArea' | 'lineCircle' | 'lineSquare';
export type LegendShapeStyle = 'solid' | 'outline';
export type LegendOrientation = 'vertical' | 'horizontal';
export interface ILegendItemProps extends CommonProps {
    /**
     * The shape of the legend item
     * @default 'square'
     */
    shape?: LegendShapeType;
    /**
     * The style of the shape (solid or outline)
     * @default 'solid'
     */
    shapeStyle?: LegendShapeStyle;
    /**
     * The label text to display next to the shape
     */
    label: string;
    /**
     * The color of the shape. Can be any valid CSS color.
     */
    color: string;
    /**
     * Whether to truncate the label text if it overflows
     * @default false
     */
    truncateLabel?: boolean;
    /**
     * Maximum width of the label text in pixels before truncation
     * @default 120
     */
    maxLabelWidth?: number;
    /**
     * ARIA role for the legend item
     * @default 'listitem'
     */
    role?: string;
}
export interface ILegendProps extends CommonProps {
    /**
     * Array of legend items to display
     */
    items: ILegendItemProps[];
    /**
     * The orientation of the legend (vertical or horizontal)
     * @default 'vertical'
     */
    orientation?: LegendOrientation;
}
export type ShapeIconProps = {
    shape: LegendShapeType;
    shapeStyle: LegendShapeStyle;
    color: string;
};
