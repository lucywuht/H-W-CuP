import React from 'react';
import { CommonProps } from '../../utility/commonProps';
type MenuPosition = 'bottomLeft' | 'bottomRight' | 'topLeft' | 'topRight';
export interface IDvMenuActionProps extends CommonProps {
    /** Trigger element that opens the menu */
    trigger: React.ReactElement;
    /** Ref to the trigger element for positioning */
    triggerRef: React.RefObject<HTMLElement>;
    /** Whether the menu is open */
    isOpen: boolean;
    /** Callback when menu should close */
    onClose: () => void;
    /** Callback when menu should open */
    onOpen?: () => void;
    /** Menu position relative to trigger */
    position?: MenuPosition;
    /** Menu content */
    children: React.ReactNode;
    /** Additional CSS class name */
    className?: string;
    /** Custom z-index for the menu portal - defaults to 300 */
    zIndex?: number;
}
export declare const MenuAction: React.ForwardRefExoticComponent<IDvMenuActionProps & React.RefAttributes<HTMLDivElement>>;
export default MenuAction;
