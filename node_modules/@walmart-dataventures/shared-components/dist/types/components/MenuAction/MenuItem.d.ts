import { MenuItemProps } from '@walmart-web/livingdesign-components';
import React from 'react';
import { CommonProps } from '../../utility/commonProps';
export interface IDvMenuItemProps extends CommonProps, MenuItemProps {
    /** Visual variant of the menu item */
    variant?: 'default' | 'destructive';
    /** Disables the menu item, making it non-interactive with reduced opacity */
    disabled?: boolean;
}
export declare const DvMenuItem: React.FC<IDvMenuItemProps>;
export default DvMenuItem;
