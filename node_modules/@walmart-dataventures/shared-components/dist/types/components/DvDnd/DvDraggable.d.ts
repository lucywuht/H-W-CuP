import { ReactNode } from 'react';
export interface IDvDraggableProps {
    /** Unique identifier for the draggable item */
    id: string;
    /** Whether this item is disabled for dragging */
    disabled?: boolean;
    /** Whether this item is currently being dragged */
    isDragging?: boolean;
    /** Children to render inside the draggable wrapper */
    children: ReactNode;
    /** Test identifier for testing purposes */
    'data-testid'?: string;
}
/**
 * DvDraggable - Wrapper component for making any content draggable
 *
 * This component wraps content to make it draggable within a DvDnd container.
 * It's a declarative approach that allows any React component to be draggable.
 *
 * @example
 * ```tsx
 * <DvDnd onDragEnd={handleDragEnd}>
 *   <DvDraggable id="item-1">
 *     <MyComponent />
 *   </DvDraggable>
 *   <DvDraggable id="item-2" disabled>
 *     <AnotherComponent />
 *   </DvDraggable>
 * </DvDnd>
 * ```
 */
export declare const DvDraggable: import("react").NamedExoticComponent<IDvDraggableProps>;
