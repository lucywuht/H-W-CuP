import { AvatarType } from '../../Avatar.types';
/**
 * Utility function to generate ARIA labels for avatars
 */
export declare const getAvatarAriaLabel: (type: AvatarType, userName?: string, generatedAltText?: string) => string;
