import { CommonProps } from '../../utility/commonProps';
import { IComparison } from '../Comparison/types';
export interface IMetricTileProps extends CommonProps {
    /** Whether to show dotted underline for the metric title. Defaults to false. */
    hasUnderline?: boolean;
    name: string;
    value: string | number;
    warningMessage?: string;
    comparison?: IComparison[];
    metricDescription: string;
    isLoading?: boolean;
}
