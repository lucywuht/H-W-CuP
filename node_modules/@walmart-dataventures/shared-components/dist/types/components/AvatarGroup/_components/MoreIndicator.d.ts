import React from 'react';
/**
 * More Indicator Component Props
 * Displays count of hidden avatars and handles overflow interactions
 */
export interface MoreIndicatorProps {
    /** Number of hidden avatars to display */
    count: number;
    /** Size variant matching the parent AvatarGroup */
    size: 'xsmall' | 'small' | 'medium' | 'large';
    /** Tooltip text shown on hover/focus */
    tooltipText?: string;
    /** Names of visible users to include in tooltip */
    visibleUserNames?: string[];
    /** Click handler for more indicator interaction */
    onMoreClick?: () => void;
    /** Whether the more indicator is disabled */
    disabled?: boolean;
    /** Additional CSS classes */
    className?: string;
    /** Test identifier */
    'data-testid'?: string;
}
/**
 * More Indicator Component
 *
 * Displays the "+N" overflow indicator for AvatarGroup when the number of users
 * exceeds the maxVisible limit. Supports interactive features and accessibility.
 *
 * @example
 * ```tsx
 * <MoreIndicator
 *   count={7}
 *   size="medium"
 *   tooltipText="Click to view all collaborators"
 *   visibleUserNames={["John Doe", "Jane Smith", "Bob Johnson"]}
 *   onMoreClick={() => openModal()}
 * />
 * ```
 */
export declare const MoreIndicator: React.FC<MoreIndicatorProps>;
