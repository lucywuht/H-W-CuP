import { AriaAttributes, ReactNode } from 'react';
import type { CommonProps } from '../../utility/commonProps';
import type { DVPopoverPosition } from './positioning';
export interface IDvPopoverProps extends CommonProps, Pick<AriaAttributes, 'aria-label' | 'aria-labelledby' | 'aria-describedby'> {
    /** The content to be displayed in the popover */
    content: ReactNode;
    /** The position of the popover relative to its trigger element */
    position?: DVPopoverPosition;
    /** Whether the popover is open */
    isOpen: boolean;
    /** Callback function when the popover should be closed */
    onClose: () => void;
    /** Ref to the trigger element */
    triggerRef: React.RefObject<HTMLElement>;
    /** Whether to show the nubbin/arrow */
    hasNubbin?: boolean;
    /** Whether the popover is disabled */
    disabled?: boolean;
    /** Custom class name for the popover */
    className?: string;
    /** Z-index for popover positioning */
    zIndex?: number;
    /** Maximum width of the popover */
    maxWidth?: string;
    /** The children element (trigger) */
    children?: ReactNode;
    /** Additional CSS class name (unsafe) */
    UNSAFE_className?: string;
    /** Test identifier */
    'data-testid'?: string;
}
export type DVPopoverProps = IDvPopoverProps;
