/**
 * DVPopover positioning logic
 * Calculates optimal position for popover relative to trigger element
 */
/**
 * Position options for the DVPopover relative to its trigger element
 * @public
 */
export type DVPopoverPosition = 'topLeft' /** Position popover at top-left of trigger */ | 'topCenter' /** Position popover centered above trigger */ | 'topRight' /** Position popover at top-right of trigger */ | 'bottomLeft' /** Position popover at bottom-left of trigger */ | 'bottomCenter' /** Position popover centered below trigger */ | 'bottomRight' /** Position popover at bottom-right of trigger */ | 'left' /** Position popover to the left of trigger */ | 'right'; /** Position popover to the right of trigger */
interface IPopoverDimensions {
    width: number;
    height: number;
}
interface ITriggerRect {
    top: number;
    left: number;
    width: number;
    height: number;
    bottom: number;
    right: number;
}
interface IPositionResult {
    x: number;
    y: number;
    actualPosition: DVPopoverPosition;
    arrowOffset?: number;
}
/**
 * Calculate popover position relative to trigger element
 */
export declare const calculatePopoverPosition: (triggerRect: ITriggerRect, popoverDimensions: IPopoverDimensions, preferredPosition: DVPopoverPosition, viewport?: {
    width: number;
    height: number;
}) => IPositionResult;
/**
 * Create memoized position calculator to avoid recalculating positions unnecessarily
 */
export declare const createMemoizedPopoverPositionCalculator: () => (triggerRect: DOMRect, popoverDimensions: IPopoverDimensions, position: DVPopoverPosition) => IPositionResult;
export {};
