import * as React from 'react';
import type { IDvPopoverProps } from './DVPopover.types';
declare const popoverManager: {
    generateId: () => string;
    show: (id: string, hideFunction: () => void, updatePositionFunction: () => void) => void;
    hide: (id: string) => void;
    hideAll: () => void;
    updateCurrentPosition: () => void;
};
declare const DVPopover: React.NamedExoticComponent<IDvPopoverProps>;
/**
 * DVPopover component for displaying contextual information with portal rendering
 */
export { DVPopover };
/**
 * @internal
 * DVPopover manager for internal state coordination.
 *
 * ⚠️ WARNING: This is an internal API that may change without notice.
 * Only exposed for advanced use cases.
 *
 * NOTE: Automatic cleanup happens in Jest tests - no setup needed!
 */
export { popoverManager };
export type { DVPopoverPosition } from './positioning';
