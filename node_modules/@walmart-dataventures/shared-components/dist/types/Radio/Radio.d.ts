import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
interface RadioBaseProps extends Omit<React.ComponentPropsWithoutRef<'label'>, 'clasName' | 'onChange' | 'style'>, CommonProps {
    /**
     * If the radio is checked.
     *
     * @default false
     */
    checked?: boolean;
    /**
     * If the radio is disabled.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * The helper text for radio.
     */
    helperText?: React.ReactNode;
    /**
     * The id for the radio.
     */
    id?: string;
    /**
     * The name for the radio.
     */
    name: string;
    /**
     * The callback fired when the radio requests to change.
     */
    onChange: (event: React.ChangeEvent<HTMLInputElement>) => void;
    /**
     * The props spread to the radio's input element.
     *
     * @default {}
     */
    radioProps?: React.ComponentPropsWithoutRef<'input'>;
    /**
     * The value for the radio.
     */
    value?: number | string;
}
export interface RadioA11yProps extends RadioBaseProps {
    /**
     * The accessible label reference IDs for the radio (Required if omitting `label` and `aria-label`).
     */
    a11yLabelledBy: string;
    label?: never;
    ariaLabel?: never;
}
export interface RadioLabelProps extends RadioBaseProps {
    /**
     * The label for the radio (Required if omitting `a11yLabelledBy` and `aria-label`).
     */
    label: React.ReactNode;
    a11yLabelledBy?: never;
    ariaLabel?: never;
}
export interface RadioAriaLabelProps extends RadioBaseProps {
    /**
     * The aria-label for the radio (Required if omitting `a11yLabelledBy` and `label`).
     */
    ariaLabel: string;
    label?: never;
    a11yLabelledBy?: never;
}
export type RadioProps = RadioA11yProps | RadioLabelProps | RadioAriaLabelProps;
/**
 * Radio Buttons represent a group of mutually exclusive choices.
 *
 * {@link https://livingdesign.walmart.com/components/radio/ Guidelines}
 * {@link https://livingdesign.walmart.com/components/radio/react/ React documentation}
 *
 * ### Content
 *
 * #### Label
 *
 * The Radio must include a label describing the option being selected. Provide a label through the `label` prop.
 *
 * Alternatively, a separate element can be used to label the Radio using `a11yLabelledBy` prop or aria-label can be provided using `ariaLabel` prop.
 *
 * In this case, an `a11yLabelledBy` that references the labelling component's `id` must be provided.
 *
 * (Note: `label`, `a11yLabelledBy` and `ariaLabel` props are mutually exclusive.)
 *
 * ```tsx
 * <Radio
 *   label="My radio"
 *   name="myRadioGroup"
 *   onChange={(event) => console.log(event)}
 * />
 * ```
 *
 * *OR*
 *
 * ```tsx
 * <Radio
 *   a11yLabelledBy="custom-label-id"
 *   name="myRadioGroup"
 *   onChange={(event) => console.log(event)}
 * >
 * <span id="custom-label-id">My radio</span>
 * ```
 *
 * *OR*
 *
 * ```tsx
 * <Radio
 *   ariaLabel="aria-label"
 *   name="myRadioGroup"
 *   onChange={(event) => console.log(event)}
 * >
 * ```
 *
 * #### Name
 *
 * The Radio requires a `name` property for keyboard navigation to work correctly with groups of Radio elements. Ensure that `name` is set to the same value for the group.
 *
 * ```tsx
 * <Radio
 *   label="One"
 *   name="numberRadio"
 *   onChange={(event) => console.log(event)}
 * />
 * <Radio
 *   label="Two"
 *   name="numberRadio"
 *   onChange={(event) => console.log(event)}
 * />
 * <Radio
 *   label="Three"
 *   name="numberRadio"
 *   onChange={(event) => console.log(event)}
 * />
 * ```
 *
 * ### Interaction
 *
 * The Radio can be configured as selected by using the `checked` property. Listen to changes in selection by passing an event handler to the `onChange` property.
 *
 * ```tsx
 * export default () => {
 *   const [value, setValue] = React.useState("0");
 *
 *   return (
 *     <Radio
 *       checked={value === "0"}
 *       label="No"
 *       onChange={(event) => setValue(event.target.value)}
 *       name="name"
 *       value="0"
 *     />
 *     <Radio
 *       checked={value === "1"}
 *       label="Yes"
 *       onChange={(event) => setValue(event.target.value)}
 *       name="name"
 *       value="1"
 *     />
 *   );
 * };
 * ```
 */
export declare const Radio: React.ForwardRefExoticComponent<RadioProps & React.RefAttributes<HTMLInputElement>>;
export {};
