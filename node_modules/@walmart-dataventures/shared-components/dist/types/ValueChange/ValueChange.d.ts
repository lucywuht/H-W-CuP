import React from 'react';
export type TrendDirection = 'Up' | 'Down';
export interface ValueChangeProps {
    /** value to be displayed */
    value: string;
    /** direction of the trend */
    trendDirection: TrendDirection;
    /** Whether to show background
     * @default false
     *  */
    showBackground?: boolean;
    /** Custom class name for the component
     * @default ""
     * */
    className?: string;
    /** Icon icon for upward trend. Defaults to ArrowUp
     * @default <ArrowUp/>
     * */
    trendUpIcon?: React.ReactElement;
    /** Icon icon for downward trend. Defaults to ArrowDown
     * @default <ArrowDown/>
     * */
    trendDownIcon?: React.ReactElement;
}
/**
 * A component used to show visualization of value increase or decrease
 * @param {ValueChangeProps} props - properties for ValueChange component
 * @returns {React.ReactElement} - ValueChange component
 */
/**
 * Value Change displays the changes in a metric's value.
 */
export declare const ValueChange: {
    ({ value, trendDirection, showBackground, className, trendUpIcon, trendDownIcon, }: ValueChangeProps): React.ReactElement;
    displayName: string;
};
