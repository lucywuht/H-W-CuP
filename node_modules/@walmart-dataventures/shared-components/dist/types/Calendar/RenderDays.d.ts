import { Dayjs } from 'dayjs';
import { IAppState } from './Interface/app.interface';
interface RenderDaysProps {
    _month: number;
    calSelection: IAppState;
    onDaySelected: (a: Dayjs) => void;
    min: Dayjs;
    max: Dayjs;
    showWkSelection: boolean;
    minWk: Dayjs;
    maxWk: Dayjs;
    boxSz: number;
    fontSz: number;
}
declare function RenderDays({ _month, calSelection, onDaySelected, min, max, showWkSelection, minWk, maxWk, boxSz, fontSz, }: RenderDaysProps): import("react/jsx-runtime").JSX.Element[];
export default RenderDays;
