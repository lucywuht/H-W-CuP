import { Dayjs } from 'dayjs';
interface ILayoutProps {
    month: number[];
    min: Dayjs;
    max: Dayjs;
    minWk: Dayjs;
    maxWk: Dayjs;
}
declare const Layout: ({ month, min, max, minWk, maxWk }: Readonly<ILayoutProps>) => import("react/jsx-runtime").JSX.Element;
export default Layout;
