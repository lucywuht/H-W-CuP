import React from 'react';
import { CaptionProps } from '../index';
export interface FormHelperTextProps extends CaptionProps<'span'> {
    /**
     * The content for the helper text.
     */
    children: React.ReactNode;
    /**
     * If the helper text is disabled.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * If the helper text has an error.
     *
     * @default false
     */
    hasError?: boolean;
}
/**
 * @private
 */
export declare const FormHelperText: React.FunctionComponent<FormHelperTextProps>;
