import React from 'react';
interface InfiniteScrollProps {
    /**
     * Callback function that is called when the end of the scrollable area is reached.
     * This function should load more data.
     */
    onLoadMore: () => void;
    /**
     * Boolean indicating whether there is more content to load.
     */
    hasMoreContent: boolean;
    /**
     * Boolean indicating whether the content is currently loading.
     * This can be used to show a loading indicator and prevent multiple load calls.
     */
    isLoading: boolean;
    /**
     *  Id of the element to apply infinite scrolling to
     */
    scrollableTargetId: string;
    /**
     * Children components to render inside the scrollable area.
     */
    children: React.ReactNode;
}
/**
 * The InfiniteScroll component allows infinite scrolling behavior for loading more content as the user scrolls down the scrollable area.
 *
 * ### Props
 *
 * - `onLoadMore`: Callback function called when the end of the scrollable area is reached. Should load more data.
 * - `isLoading`: Boolean indicating whether content is currently being loaded.
 * - `hasMoreContent`: Boolean indicating whether there is more content to load.
 * - `scrollableTargetId`: Id of the element to apply infinite scrolling to.
 * - `children`: Children components to render inside the scrollable area.
 *
 * ```tsx
 * <InfiniteScroll
 *   onLoadMore={() => {...}}
 *   isLoading={false}
 *   hasMoreContent={true}
 *   scrollableTargetId="scrollable"
 * >
 *   { Children components }
 * </InfiniteScroll>
 * ```
 *
 * ### Example
 *
 * ```tsx
 * import * as React from 'react';
 * import { InfiniteScroll } from 'components';
 *
 * const MyComponent = () => {
 *   const loadMoreData = () => {...};
 *
 *   return (
 *     <InfiniteScroll
 *       onLoadMore={loadMoreData}
 *       isLoading={false}
 *       hasMoreContent={true}
 *       scrollableTargetId="scrollable"
 *     >
 *       <MenuList
 *          menuTrigger={<Button ref={ref}>Default</Button>}
 *          isMulti={false}
 *          menuTriggerRef={ref}
 *          menuPosition="bottomLeft"
 *          preSelectedOptions={['feb']}
 *          updateCheckedList={() => {}}
 *          {...args}
 *      />
 *     </InfiniteScroll>
 *   );
 * };
 * ```
 */
export declare const InfiniteScroll: React.FC<InfiniteScrollProps>;
export {};
