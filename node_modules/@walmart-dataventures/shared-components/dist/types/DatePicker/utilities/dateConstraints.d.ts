import type { DisabledDateFilter } from '../types';
/**
 * Checks if a date falls before the minimum allowed date.
 */
export declare function isBeforeMin(date: Date, minDate?: Date): boolean;
/**
 * Checks if a date falls after the maximum allowed date.
 */
export declare function isAfterMax(date: Date, maxDate?: Date): boolean;
/**
 * Checks if a date is outside the allowed min/max range.
 */
export declare function isOutOfRange(date: Date, minDate?: Date, maxDate?: Date): boolean;
/**
 * Checks if a date is disabled by either the range constraints or the filter.
 */
export declare function isDateDisabled(date: Date, minDate?: Date, maxDate?: Date, disabledDateFilter?: DisabledDateFilter): boolean;
