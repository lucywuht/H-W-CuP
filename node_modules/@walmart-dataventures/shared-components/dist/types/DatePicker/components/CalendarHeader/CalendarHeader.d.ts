import React from 'react';
export interface ICalendarHeaderProps {
    /** Formatted month/year string, e.g. "January 2025" */
    monthYearLabel: string;
    /** Callback to go to the previous month */
    onPreviousMonth: () => void;
    /** Callback to go to the next month */
    onNextMonth: () => void;
    /** Whether the previous month button is disabled */
    isPreviousMonthDisabled?: boolean;
    /** Whether the next month button is disabled */
    isNextMonthDisabled?: boolean;
    /** Hide the previous-month button (preserves layout space).
     *  Used in range calendars where the right calendar hides ←. */
    hidePreviousButton?: boolean;
    /** Hide the next-month button (preserves layout space).
     *  Used in range calendars where the left calendar hides →. */
    hideNextButton?: boolean;
    /** A11y label for previous month button */
    previousMonthLabel?: string;
    /** A11y label for next month button */
    nextMonthLabel?: string;
    /** Whether the month/year label should be an aria-live region.
     *  Set to false when a parent provides its own aria-live announcement.
     *  @default true */
    announceMonthChange?: boolean;
    /** Additional CSS class name */
    className?: string;
}
/**
 * CalendarHeader — Displays the month/year label with
 * left/right chevron buttons for navigating between months.
 *
 * ```
 * [ < ]   January 2025   [ > ]
 * ```
 */
export declare const CalendarHeader: React.FunctionComponent<ICalendarHeaderProps>;
