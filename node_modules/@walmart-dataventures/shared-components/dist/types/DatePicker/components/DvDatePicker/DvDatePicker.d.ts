import React from 'react';
import type { IDvDatePickerProps } from '../../types';
/**
 * DvDatePicker — A single-date picker with a text input, calendar icon,
 * and a calendar popover for Standard mode.
 *
 * ```tsx
 * <DvDatePicker
 *   label="Choose date"
 *   onSelect={(date) => console.log(date)}
 * />
 * ```
 */
export declare const DvDatePicker: React.ForwardRefExoticComponent<IDvDatePickerProps & React.RefAttributes<HTMLInputElement>>;
