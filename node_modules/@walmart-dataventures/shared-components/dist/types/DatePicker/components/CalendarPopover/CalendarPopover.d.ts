import React from 'react';
export interface ICalendarPopoverProps {
    /** The calendar content to render inside the popover */
    children: React.ReactNode;
    /** Optional ID for the calendar panel (for aria-controls) */
    id?: string;
    /** Whether the popover is open */
    isOpen: boolean;
    /** Callback to close the popover */
    onClose: () => void;
    /** Ref to the trigger element (input or icon button) */
    triggerRef: React.RefObject<HTMLElement>;
    /** Custom width override. Default: 352 (single calendar) */
    width?: number;
    /** Additional CSS class name */
    className?: string;
    /** Accessibility label for the calendar popover */
    'aria-label'?: string;
    /** Whether to show drop shadow on calendar. Default: true */
    overlay?: boolean;
}
/**
 * CalendarPopover — A thin wrapper around DVPopover that provides
 * calendar-specific defaults (position, width, shadow, no nubbin).
 *
 * Used by DvDatePicker and DvDateRangePicker to host the calendar panel.
 */
export declare const CalendarPopover: React.FunctionComponent<ICalendarPopoverProps>;
