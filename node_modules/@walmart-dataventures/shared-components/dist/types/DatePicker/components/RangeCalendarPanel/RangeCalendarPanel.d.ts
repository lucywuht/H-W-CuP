import React from 'react';
import type { IRangeCalendarMonth } from '../../hooks/useRangeCalendar';
export interface IRangeCalendarPanelProps {
    /** Left month data from useRangeCalendar */
    leftMonth: IRangeCalendarMonth;
    /** Right month data from useRangeCalendar */
    rightMonth: IRangeCalendarMonth;
    /** Day-of-week header labels */
    dayOfWeekLabels: string[];
    /** Callback when navigating to previous month */
    onPreviousMonth: () => void;
    /** Callback when navigating to next month */
    onNextMonth: () => void;
    /** Whether prev month nav is disabled */
    isPreviousMonthDisabled?: boolean;
    /** Whether next month nav is disabled */
    isNextMonthDisabled?: boolean;
    /** Callback when a day is clicked */
    onSelectDay: (date: Date) => void;
    /** Callback when mouse enters a day cell */
    onHoverDay?: (date: Date) => void;
    /** Callback when mouse leaves a day cell */
    onHoverDayEnd?: () => void;
    /** Predicate: is a day in the hover range preview? */
    isInHoverRange?: (date: Date) => boolean;
    /** Footer content (Apply/Cancel buttons, Clear All link) */
    footer?: React.ReactNode;
    /** Accessibility label overrides for navigation buttons */
    a11yLabels?: {
        calendarPreviousMonth?: string;
        calendarNextMonth?: string;
    };
    /** Additional CSS class name */
    className?: string;
}
/**
 * RangeCalendarPanel — Two side-by-side calendar grids with
 * a shared navigation header and optional footer.
 *
 * ```
 * [ < ]   January 2025    |    February 2025   [ > ]
 *  Sun Mon Tue ... Sat      Sun Mon Tue ... Sat
 *   ...day cells...          ...day cells...
 *
 *  Clear all                     Cancel | Apply
 * ```
 */
export declare const RangeCalendarPanel: React.FunctionComponent<IRangeCalendarPanelProps>;
