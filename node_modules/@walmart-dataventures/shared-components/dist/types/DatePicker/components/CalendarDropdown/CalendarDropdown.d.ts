/**
 * CalendarDropdown — Lightweight dropdown container for DatePicker calendars.
 *
 * Replaces DVPopover for DatePicker components with a simpler implementation:
 * - No portal (renders in place, avoids click-outside complexity)
 * - position: absolute (aligns with parent container)
 * - Simple click-outside detection
 * - No animation (instant show/hide)
 *
 * **Focus management contract:**
 * This component sets `role="dialog"` and `aria-modal="true"` but does NOT
 * manage focus internally. The parent component is responsible for:
 * 1. Moving focus into the dropdown when it opens (e.g. to the first
 *    focusable element or the selected day cell)
 * 2. Returning focus to the trigger element when the dropdown closes
 *    (typically handled in the `onClose` callback)
 * 3. Implementing a focus trap if needed (to prevent Tab from escaping
 *    into background content while the dropdown is open)
 *
 * This is intentional — each consumer (DvDatePicker, DvDateRangePicker)
 * has different focus targets and navigation patterns.
 *
 */
import React from 'react';
export interface ICalendarDropdownProps {
    /** Whether the dropdown is open */
    isOpen: boolean;
    /** Callback when dropdown should close (click outside or escape) */
    onClose: () => void;
    /** Content to render inside the dropdown */
    children: React.ReactNode;
    /** Optional width override */
    width?: number | string;
    /** Optional z-index override (default: 300) */
    zIndex?: number;
    /** Optional test ID */
    'data-testid'?: string;
    /** Optional additional class name */
    className?: string;
    /** Accessible label for the dialog */
    'aria-label'?: string;
    /** Whether to show drop shadow on calendar. Default: true */
    overlay?: boolean;
}
/**
 * A simple, lightweight dropdown for calendar content.
 * Positions absolutely below its parent container, left-aligned.
 */
export declare const CalendarDropdown: React.FC<ICalendarDropdownProps>;
