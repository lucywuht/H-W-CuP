import React from 'react';
import type { IDayCellState } from '../../types';
export interface ICalendarDayCellProps {
    /** The day cell state from useCalendar */
    dayState: IDayCellState;
    /** Callback when a day is clicked */
    onSelect: (date: Date) => void;
    /** Callback when mouse enters the cell (for hover preview) */
    onHover?: (date: Date) => void;
    /** Callback when mouse leaves the cell */
    onHoverEnd?: () => void;
    /** Whether this cell is in a hovered range preview */
    isInHoverRange?: boolean;
    /** Whether this cell is the first day of the hovered week range */
    isWeekRangeStart?: boolean;
    /** Whether this cell is the last day of the hovered week range */
    isWeekRangeEnd?: boolean;
    /** Whether week-level hover mode is active.
     *  When true, mid-cells use a lighter background to distinguish from standard hover. */
    isWeekHoverMode?: boolean;
    /** Additional CSS class name */
    className?: string;
}
/**
 * CalendarDayCell — Renders a single day in the calendar grid.
 *
 * Handles:
 * - Selected state (purple fill)
 * - Today indicator (purple border)
 * - Range states (rangeStart, rangeMiddle, rangeEnd)
 * - Hover preview
 * - Disabled & outside-month dimming
 * - Keyboard accessibility
 *
 * Wrapped in React.memo to avoid unnecessary re-renders when parent
 * re-renders (e.g. during hover — only 2-3 of 42 cells actually change).
 */
export declare const CalendarDayCell: React.FunctionComponent<ICalendarDayCellProps>;
