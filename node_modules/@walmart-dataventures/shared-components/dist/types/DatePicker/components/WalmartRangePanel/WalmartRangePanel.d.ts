import React from 'react';
import type { IRangeCalendarMonth } from '../../hooks/useRangeCalendar';
import type { IFiscalWeekInfo } from '../../types';
export interface IWalmartRangePanelProps {
    /** Left month data from useRangeCalendar */
    leftMonth: IRangeCalendarMonth;
    /** Right month data from useRangeCalendar */
    rightMonth: IRangeCalendarMonth;
    /** Day-of-week header labels (Sat, Sun, Mon, ...) */
    dayOfWeekLabels: string[];
    /** Navigate to previous month */
    onPreviousMonth: () => void;
    /** Navigate to next month */
    onNextMonth: () => void;
    /** Whether prev month nav is disabled */
    isPreviousMonthDisabled?: boolean;
    /** Whether next month nav is disabled */
    isNextMonthDisabled?: boolean;
    /** Handle day click (snaps to week in Walmart mode) */
    onSelectDay: (date: Date) => void;
    /** Handle day hover */
    onHoverDay?: (date: Date) => void;
    /** Clear hover state */
    onHoverDayEnd?: () => void;
    /** Selected start week info */
    startWeek?: IFiscalWeekInfo;
    /** Selected end week info */
    endWeek?: IFiscalWeekInfo;
    /** Hovered week info */
    hoveredWeek?: IFiscalWeekInfo;
    /** Is date in selected range? */
    isDateInSelectedRange?: (date: Date) => boolean;
    /** Is date in hovered week? */
    isDateInHoveredWeek?: (date: Date) => boolean;
    /** Is date in hover preview range? */
    isDateInHoverPreviewRange?: (date: Date) => boolean;
    /** Footer content (Apply/Cancel/Clear) */
    footer?: React.ReactNode;
    /** Additional CSS class name */
    className?: string;
}
/**
 * WalmartRangePanel — Dual WalmartCalendarGrid for week-level selection.
 * Week triggers are in the parent DvDateRangePicker component (outer selects).
 *
 * Layout:
 * ```
 * [ < ]  January 2025   |   February 2025  [ > ]
 * WK Sat Sun Mon Tue Wed Thu Fri  |  WK Sat Sun Mon Tue Wed Thu Fri
 *  1  ..  ..  ..  ..  ..  ..  ..  |   1  ..  ..  ..  ..  ..  ..  ..
 *  ...
 * Clear all                          Cancel | Apply
 * ```
 */
export declare const WalmartRangePanel: React.FunctionComponent<IWalmartRangePanelProps>;
