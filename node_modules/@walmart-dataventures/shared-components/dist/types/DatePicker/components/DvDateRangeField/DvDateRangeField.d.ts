import React from 'react';
import type { ActiveField, IDvDateRangeFieldProps } from '../../types';
export interface IDvDateRangeFieldInternalProps extends IDvDateRangeFieldProps {
    /** Which field is active (used by parent DvDateRangePicker) */
    activeField?: ActiveField;
    /** Callback when a field gains focus */
    onFieldFocus?: (field: ActiveField) => void;
}
/**
 * DvDateRangeField — Two DateFields side by side for start/end dates.
 * No calendar icon — this is the text-only range input.
 *
 * ```tsx
 * <DvDateRangeField
 *   onChange={({ startValue, endValue }) => console.log(startValue, endValue)}
 * />
 * ```
 */
export declare const DvDateRangeField: React.ForwardRefExoticComponent<IDvDateRangeFieldInternalProps & React.RefAttributes<HTMLDivElement>>;
