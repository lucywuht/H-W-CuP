import React from 'react';
import type { ICalendarWeek } from '../../hooks/useCalendar';
import type { IFiscalWeekInfo } from '../../types';
export interface IWalmartCalendarGridProps {
    /** Day-of-week header labels, e.g. ['Sat', 'Sun', 'Mon', ...] */
    dayOfWeekLabels: string[];
    /** Calendar weeks grid from useRangeCalendar */
    weeks: ICalendarWeek[];
    /** Callback when a day is clicked (selection snaps to full week) */
    onSelectDay: (date: Date) => void;
    /** Callback when mouse enters a day cell */
    onHoverDay?: (date: Date) => void;
    /** Callback when mouse leaves a day cell */
    onHoverDayEnd?: () => void;
    /** Predicate: is a day in the current selected range? */
    isDateInSelectedRange?: (date: Date) => boolean;
    /** Predicate: is a day in the hovered week? */
    isDateInHoveredWeek?: (date: Date) => boolean;
    /** Predicate: is a day in the hover preview range? */
    isDateInHoverPreviewRange?: (date: Date) => boolean;
    /** Currently selected start week (for WMT WK column highlighting) */
    startWeek?: IFiscalWeekInfo;
    /** Currently selected end week */
    endWeek?: IFiscalWeekInfo;
    /** Hovered week (from DvSelect or calendar row hover) */
    hoveredWeek?: IFiscalWeekInfo;
    /** Whether to show the WMT WK column */
    showWeekNumbers?: boolean;
    /** Position of the week number column: 'left' (default) or 'right' */
    weekNumberPosition?: 'left' | 'right';
    /** Accessible label for the table (defaults to "Walmart fiscal calendar") */
    ariaLabel?: string;
    /** Additional CSS class name */
    className?: string;
}
export declare const WalmartCalendarGrid: React.NamedExoticComponent<IWalmartCalendarGridProps>;
