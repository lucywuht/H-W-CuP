import React from 'react';
import type { IDvDateRangePickerProps } from '../../types';
/**
 * DvDateRangePicker — Full date range picker with dual text inputs,
 * calendar icon, dual-month calendar popover, and Apply/Cancel footer.
 *
 * Supports two modes:
 * - **Standard** (default): Day-level selection with text inputs, Sun-Sat weeks.
 * - **Walmart**: Week-level selection with DvSelect dropdowns, Sat-Fri fiscal weeks.
 *
 * ```tsx
 * <DvDateRangePicker
 *   calendarMode="standard"
 *   onApply={(range) => console.log(range)}
 * />
 *
 * <DvDateRangePicker
 *   calendarMode="walmart"
 *   onWeekSelect={(weekRange) => console.log(weekRange)}
 *   onApply={(range) => console.log(range)}
 * />
 * ```
 */
export declare const DvDateRangePicker: React.ForwardRefExoticComponent<IDvDateRangePickerProps & React.RefAttributes<HTMLDivElement>>;
