import type { DisabledDateFilter, IDayCellState, WeekStartDay } from '../types';
export interface IUseCalendarOptions {
    /** The initially displayed month. Default: today's month */
    defaultMonth?: Date;
    /** Predicate to disable specific dates */
    disabledDateFilter?: DisabledDateFilter;
    /** Maximum selectable date */
    maxDate?: Date;
    /** Minimum selectable date */
    minDate?: Date;
    /** Currently selected date (single picker) */
    selectedDate?: Date;
    /** Start day of week. Default: 0 (Sunday) */
    weekStartDay?: WeekStartDay;
}
export interface ICalendarWeek {
    /** Unique key for this week row */
    key: string;
    /** Day cells in this week */
    days: IDayCellState[];
}
export interface IUseCalendarReturn {
    /** Navigate to the next month */
    goToNextMonth: () => void;
    /** Navigate to the previous month */
    goToPreviousMonth: () => void;
    /** Navigate to a specific month */
    goToMonth: (date: Date) => void;
    /** The currently displayed month (Date at day 1) */
    currentMonth: Date;
    /** Formatted "Month Year" label, e.g. "January 2025" */
    monthYearLabel: string;
    /** Day-of-week header labels, e.g. ['Sun', 'Mon', ...] */
    dayOfWeekLabels: string[];
    /** Grid of weeks, each containing 7 DayCellStates */
    weeks: ICalendarWeek[];
    /** Whether the previous month button should be disabled */
    isPreviousMonthDisabled: boolean;
    /** Whether the next month button should be disabled */
    isNextMonthDisabled: boolean;
}
/**
 * useCalendar — Generates the month grid, handles navigation,
 * and computes each day cell's state.
 *
 * This hook powers the CalendarGrid component used inside
 * DvDatePicker and DvDateRangePicker.
 */
export declare function useCalendar(options?: IUseCalendarOptions): IUseCalendarReturn;
