import type { DisabledDateFilter, IDateRange, WeekStartDay } from '../types';
import type { ICalendarWeek } from './useCalendar';
export interface IUseRangeCalendarOptions {
    /** The initially displayed left month. Default: today's month */
    defaultMonth?: Date;
    /** Predicate to disable specific dates */
    disabledDateFilter?: DisabledDateFilter;
    /** Maximum selectable date */
    maxDate?: Date;
    /** Minimum selectable date */
    minDate?: Date;
    /** Currently selected date range */
    range?: IDateRange;
    /** Start day of week. Default: 0 (Sunday) */
    weekStartDay?: WeekStartDay;
}
export interface IRangeCalendarMonth {
    /** The month Date (first day) */
    month: Date;
    /** Formatted month/year label */
    monthYearLabel: string;
    /** Grid of weeks with day cells */
    weeks: ICalendarWeek[];
}
export interface IUseRangeCalendarReturn {
    /** Left calendar month data */
    leftMonth: IRangeCalendarMonth;
    /** Right calendar month data (leftMonth + 1) */
    rightMonth: IRangeCalendarMonth;
    /** The raw Date for the left calendar month (first of month) */
    leftMonthDate: Date;
    /** The raw Date for the right calendar month (first of month) */
    rightMonthDate: Date;
    /** Navigate to the previous month (shifts both calendars left) */
    goToPreviousMonth: () => void;
    /** Navigate to the next month (shifts both calendars right) */
    goToNextMonth: () => void;
    /** Navigate to a specific month (sets left calendar) */
    goToMonth: (date: Date) => void;
    /** Day-of-week header labels */
    dayOfWeekLabels: string[];
    /** Whether previous month nav is disabled */
    isPreviousMonthDisabled: boolean;
    /** Whether next month nav is disabled */
    isNextMonthDisabled: boolean;
}
/**
 * useRangeCalendar — Manages two side-by-side calendar months for
 * the date range picker. Left month and right month are always
 * consecutive (right = left + 1).
 */
export declare function useRangeCalendar(options?: IUseRangeCalendarOptions): IUseRangeCalendarReturn;
