import type { IDateRangePickerA11yLabels } from '../types';
/**
 * Return type for useDatePickerTranslations hook.
 * Provides all localized strings for DatePicker components.
 */
export interface IUseDatePickerTranslationsReturn {
    /** Active locale code (e.g., 'en-US', 'es-MX', 'fr-CA') */
    locale: string;
    /** ARIA labels for accessibility */
    defaultA11yLabels: Required<IDateRangePickerA11yLabels>;
    /** Visible field and heading label strings */
    labels: {
        chooseDate: string;
        dateField: string;
        startDate: string;
        endDate: string;
        startWeek: string;
        endWeek: string;
    };
    /** Button text strings */
    buttons: {
        apply: string;
        cancel: string;
        clearAll: string;
        clearAllShort: string;
    };
    /** Input placeholder strings */
    placeholders: {
        selectWeek: string;
        selectStartWeek: string;
        selectEndWeek: string;
    };
    /** Locale-specific date format display hints */
    formatDisplay: {
        default: string;
    };
    /** Validation error message factories */
    errors: {
        invalidFormat: (format?: string) => string;
        outOfRange: string;
        dateUnavailable: string;
    };
    /**
     * Raw i18next translation function for one-off translations.
     * Use this for dynamic translations not covered by the structured properties above.
     *
     * @example
     * ```ts
     * const { t } = useDatePickerTranslations();
     * const customLabel = t('DatePicker:custom.label', 'Default text');
     * ```
     */
    t: (...args: any[]) => any;
}
/**
 * Hook to provide localized accessibility labels for DatePicker components.
 * Returns translations from the DatePicker namespace, falling back to English defaults.
 *
 * Returned shape:
 * - `defaultA11yLabels`: aria-label strings for all DatePicker/DateRangePicker variants
 * - `labels`:            visible field/heading label strings
 * - `buttons`:           visible button text strings
 * - `placeholders`:      input placeholder strings
 * - `formatDisplay`:     locale date format hint (e.g. "mm/dd/yyyy")
 * - `errors`:            validation error message factories
 * - `locale`:            active i18n language tag (resolvedLanguage takes priority)
 * - `t`:                 raw i18next `t` function for one-off translations
 */
export declare function useDatePickerTranslations(): IUseDatePickerTranslationsReturn;
