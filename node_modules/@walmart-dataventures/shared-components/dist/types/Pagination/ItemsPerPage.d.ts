import { MenuPosition } from '../index';
type ItemsPerPageProps = {
    hideItemsPerPageDropdown: boolean;
    itemsPerPage: string;
    itemsPerPageMenuPosition: MenuPosition;
    onDropdownChange: (value: string) => void;
    size?: 'small' | 'medium' | 'large';
    id?: string;
};
declare const ItemsPerPage: ({ hideItemsPerPageDropdown, itemsPerPage, itemsPerPageMenuPosition, onDropdownChange, size, id: initialId, }: ItemsPerPageProps) => import("react/jsx-runtime").JSX.Element | null;
export default ItemsPerPage;
