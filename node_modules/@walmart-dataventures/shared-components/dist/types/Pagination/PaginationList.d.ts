import { PageType } from './Pagination';
/**
 * Props for the `PaginationList` component.
 *
 * @property {boolean} [hideItemsPerPageCount] - Determines whether to hide the items-per-page count display.
 * @property {boolean} [hideItemsPerPageDropdown] - Determines whether to hide the items-per-page dropdown selector.
 * @property {(number | string)[]} paginationView - An array representing the pagination view, containing page numbers or strings (e.g., ellipses).
 * @property {number} parsedCurrentPage - The current page number, parsed as a number.
 * @property {(page: PageType) => void} setCurrentPage - Callback function to set the current page.
 * @property {'small' | 'medium' | 'large'} [size] - The size of the pagination component. Defaults to 'medium' if not specified.
 * @property {number[]} totalPages - An array representing the total pages available.
 * @property {string} [id] - An optional unique identifier for the pagination component.
 */
type PaginationListProps = {
    hideItemsPerPageCount?: boolean;
    hideItemsPerPageDropdown?: boolean;
    paginationView: (number | string)[];
    parsedCurrentPage: number;
    setCurrentPage: (page: PageType) => void;
    size?: 'small' | 'medium' | 'large';
    totalPages: number[];
    id?: string;
};
declare const PaginationList: ({ hideItemsPerPageCount, hideItemsPerPageDropdown, paginationView, parsedCurrentPage, setCurrentPage, size, totalPages, id: initialId, }: PaginationListProps) => import("react/jsx-runtime").JSX.Element | null;
export default PaginationList;
