import { AriaAttributes, CSSProperties, HTMLAttributes } from 'react';
export interface IDvSnackbarProps extends Omit<HTMLAttributes<HTMLDivElement>, 'role'>, Pick<AriaAttributes, 'aria-label' | 'aria-labelledby' | 'aria-describedby'> {
    /** Snackbar message content */
    message: string;
    /** Optional action button text */
    actionText?: string;
    /** Action button click handler */
    onAction?: () => void;
    /** Show/hide close button */
    showCloseButton?: boolean;
    /** UNSAFE: Additional CSS classes (use with caution) */
    UNSAFE_className?: string;
    /** UNSAFE: Inline styles (use with caution) - from applyCommonProps */
    UNSAFE_style?: CSSProperties;
    /** Test identifier */
    'data-testid'?: string;
    /** Accessible label for the snackbar */
    ariaLabel?: string;
    /** UNSAFE: Additional CSS classes for the action text on the snackbar */
    UNSAFE_actionButtonClassName?: string;
    /** UNSAFE: Inline styles for the close button (use with caution) */
    UNSAFE_actionButtonStyles?: CSSProperties;
    /** UNSAFE: Additional CSS classes for the close button on the snackbar */
    UNSAFE_closeButtonClassName?: string;
    /** UNSAFE: Inline styles for the close button (use with caution) */
    UNSAFE_closeButtonStyles?: CSSProperties;
}
export interface IDvSnackbarProviderProps {
    children: React.ReactNode;
    /** Render the snackbar container as a portal at document.body to escape stacking contexts */
    usePortal?: boolean;
}
export interface IDvSnackbarContextValue {
    /** Show a snackbar */
    addSnack: (options: Omit<IDvSnackbarProps, 'open' | 'onClose'>) => void;
    /** Hide current snackbar */
    hideSnackbar: () => void;
    /** Show a snackbar */
    showSnackbar: (options: Omit<IDvSnackbarProps, 'open' | 'onClose'>) => void;
}
