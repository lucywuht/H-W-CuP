import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
export type IconButtonSize = 'xsmall' | 'small' | 'medium' | 'large';
export type IconVariant = 'primary' | 'secondary';
export type IconButtonShape = 'round' | 'full';
interface IconButtonBaseProps extends Omit<CommonProps, 'className' | 'style'> {
    /**
     * The accessible label for the icon button.
     */
    a11yLabel: string;
    /**
     * The content for the icon button.
     */
    children: React.ReactNode;
    /**
     * The size for the icon button.
     *
     * @default "small"
     */
    size?: IconButtonSize;
    /**
     * The variant for the tag.
     *
     * @default "secondary"
     */
    variant?: IconVariant;
    /**
     * The shape of the icon button.
     *
     * @default "round"
     */
    shape?: IconButtonShape;
}
export type IconButtonAnchorProps = IconButtonBaseProps & Omit<React.JSX.IntrinsicElements['a'], 'className' | 'style'> & {
    /**
     * The href for the icon button (Anchor only).
     */
    href: string;
};
export type IconButtonButtonProps = IconButtonBaseProps & Omit<React.JSX.IntrinsicElements['button'], 'className' | 'style'> & {
    /**
     * If the icon button is disabled (Button only).
     *
     * @default false
     */
    disabled?: boolean;
};
export type IconButtonProps = IconButtonAnchorProps | IconButtonButtonProps;
export type IconButtonPolymorphicType = {
    (props: IconButtonAnchorProps): React.JSX.Element;
    (props: IconButtonButtonProps): React.JSX.Element;
    displayName?: string;
};
/**
 * Icon Buttons trigger actions and rely solely on icons to convey their purpose.
 *
 * - {@link https://livingdesign.walmart.com/components/icon-button/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/icon-button/react/ React documentation}
 *
 * ### Content
 *
 * The Icon Button accepts an icon to display through the `children` prop.
 *
 * ```tsx
 * import * as React from "react";
 * import { IconButton } from "@walmart-web/livingdesign-components";
 * import Icon from "@livingdesign/icons";
 *
 * <IconButton a11yLabel="icon">
 *   <Icon.Wallet />
 * </IconButton>
 * ```
 *
 * ### Features
 *
 * #### Size
 *
 * The Icon Button comes in four sizes:
 *
 * - `xsmall` (24px)
 * - `small` (32px, default)
 * - `medium` (40px)
 * - `large` (48px)
 *
 * Set the Icon Button's size using the `size` prop.
 *
 * ### Accessibility
 *
 * The Icon Button requires an accessible label. Set the accessible label through the `a11yLabel` prop.
 *
 * ### Example
 * ```tsx
 * import * as React from "react";
 * import { IconButton } from "@walmart-web/livingdesign-components";
 * import Icon from "@livingdesign/icons";
 *
 * <IconButton a11yLabel="icon" size="large">
 *   <Icon.Wallet />
 * </IconButton>
 * ```
 */
export declare const IconButton: IconButtonPolymorphicType;
export {};
