import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
/**
 * Color variants for the Badge component.
 *
 * @remarks
 * - Use semantic colors when conveying status (success, error, etc.)
 * - Use neutral or decorative colors for non-status information
 */
export type BadgeColor = 'blue' | 'gray' | 'green' | 'brand' | 'red' | 'spark' | 'black' | 'white' | 'purple' | 'info' | 'brandbold' | 'orange' | 'cyan' | 'pink' | 'yellow' | 'neutral';
export interface IBadgeProps extends Omit<React.ComponentPropsWithoutRef<'span'>, 'className' | 'style'>, CommonProps {
    /**
     * The text label for the badge.
     */
    children?: React.ReactNode;
    /**
     * The variant for the badge.
     *
     * @default "blue"
     */
    color?: BadgeColor;
}
/**
 * Badges are visual indicators of status or count.
 *
 * - {@link https://digitaltoolkit.livingdesign.walmart.com/components/badge/ Guidelines}
 * - {@link https://digitaltoolkit.livingdesign.walmart.com/develop/react/components/badge/ React documentation}
 *
 * @example
 * ```tsx
 * // Basic usage
 * <Badge>10</Badge>
 *
 * // With color variant
 * <Badge color="green">Success</Badge>
 *
 * // Empty badge (no text label)
 * <Badge color="red" />
 * ```
 *
 * @remarks
 * Badges automatically adjust their styling based on whether they contain text:
 * - With text: Pill-shaped with padding around the text
 * - Without text: Circular dot indicator
 */
export declare const Badge: React.FunctionComponent<IBadgeProps>;
