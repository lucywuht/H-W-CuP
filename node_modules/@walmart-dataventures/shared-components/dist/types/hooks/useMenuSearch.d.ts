/// <reference types="react" />
/**
 * Custom hook for client-side menu search functionality
 * Provides search query state and filtered options based on label matching
 *
 * @template T - Type of menu option, must have a label property
 * @param options - Array of menu options to filter
 * @returns Object containing searchQuery state, setter, and filtered options
 *
 * @example
 * ```tsx
 * const { searchQuery, setSearchQuery, filteredOptions } = useMenuSearch(menuOptions);
 *
 * <MenuSingleSelect
 *   options={filteredOptions}
 *   onSearch={setSearchQuery}
 *   // ...other props
 * />
 * ```
 */
export declare function useMenuSearch<T extends {
    label: string;
}>(options: T[]): {
    searchQuery: string;
    setSearchQuery: import("react").Dispatch<import("react").SetStateAction<string>>;
    filteredOptions: T[];
};
