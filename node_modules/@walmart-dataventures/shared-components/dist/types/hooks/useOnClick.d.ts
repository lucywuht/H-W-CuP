/**
 * Trigger click event when clicking a given ref.
 * @private
 *
 * {@link https://github.com/gragland/usehooks/blob/master/src/pages/useOnClickOutside.md}
 *
 * @example
 * useOnClick(ref1);
 * useOnClick([ref1, ref2]);
 *
 * @param {Function} onClick Click event handler
 * @param {Array} [dependencies=[]] Use effect dependencies
 * @param {boolean} [useCapture=false] Whether to dispatch before any events beneath it in the DOM tree
 * @returns {undefined}
 */
export declare const useOnClick: (onClick: (event: PointerEvent | MouseEvent | TouchEvent) => void, dependencies?: ReadonlyArray<unknown>, useCapture?: boolean) => void;
