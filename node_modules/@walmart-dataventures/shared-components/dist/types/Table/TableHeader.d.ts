import React from 'react';
import { DataTableHeaderProps } from '..';
import { ColumnDef } from '.';
export interface TableHeaderProps extends Omit<DataTableHeaderProps, 'children'> {
    columnDef: ColumnDef;
    sortProps?: {
        onSort: () => void;
        sort: string;
    };
}
export declare const TableHeader: React.FC<TableHeaderProps>;
