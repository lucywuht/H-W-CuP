import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
export type ChipSize = 'large' | 'small';
export interface ChipProps extends Omit<React.ComponentPropsWithRef<'button'>, 'className' | 'style'>, CommonProps {
    /**
     * The content for the chip.
     */
    children: React.ReactNode;
    /**
     * If the chip is disabled.
     *
     * @default false
     */
    disabled?: boolean;
    /**
     * If the chip is in an error state.
     * Error chips can still be selected and maintain all interaction states.
     *
     * @default false
     */
    error?: boolean;
    /**
     * The leading content for the chip.
     */
    leading?: React.ReactNode;
    /**
     * The callback fired when the chip is clicked.
     */
    onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;
    /**
     * If the chip is selected.
     *
     * @default false
     */
    selected?: boolean;
    /**
     * The size for the chip.
     *
     * @default "small"
     */
    size?: ChipSize;
    /**
     * The trailing content for the chip.
     */
    trailing?: React.ReactNode;
}
/**
 * Chips allow users to make selections, filter content, or trigger actions.
 *
 * - {@link https://livingdesign.walmart.com/components/chip/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/chip/react/ React documentation}
 *
 * ### Content
 *
 * #### Leading and trailing
 *
 * The Chip accepts leading and trailing icons through the optional `leading` and `trailing` props.
 *
 * ### Features
 *
 * #### Size
 *
 * The Chip comes in two sizes:
 *
 * - `small` (default)
 * - `large`
 *
 * Set the Chip's size using the `size` prop:
 *
 * ```tsx
 * <Chip size="small">Dorito</Chip>
 * <Chip size="large">Dorito</Chip>
 * ```
 *
 * #### States
 *
 * The Chip's states can be managed through the following optional props: `disabled`, `selected`, and `error`.
 * Error chips maintain full interactivity and can be combined with selected and other states.
 *
 * ```tsx
 * import * as React from "react";
 * import { Chip } from "@walmart-web/livingdesign-components";
 * import Icon from "@livingdesign/icons";
 *
 * export default () => {
 *   return (
 *     <>
 *       <Chip
 *         disabled
 *         leading={<Icon.Spark />}
 *         size="small"
 *       >
 *         Dorito
 *       </Chip>
 *       <Chip
 *         selected
 *         size="large"
 *         trailing={<Icon.Camera />}
 *       >
 *         Lays
 *       </Chip>
 *       <Chip
 *         error
 *         selected
 *         leading={<Icon.Alert />}
 *       >
 *         Error Selected
 *       </Chip>
 *     </>
 *   );
 * }
 * ```
 */
export declare const Chip: React.ForwardRefExoticComponent<Omit<ChipProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
