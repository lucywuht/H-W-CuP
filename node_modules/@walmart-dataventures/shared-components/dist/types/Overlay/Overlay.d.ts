import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
export type PanelPosition = 'left' | 'right';
export interface OverlayProps extends Omit<React.ComponentPropsWithoutRef<'div'>, 'className' | 'onChange' | 'style'>, CommonProps {
    /**
     * whether to open the panel as modal with background scrim.
     *
     * @default false
     */
    disableScrim?: boolean;
    /**
     * The position for the panel.
     *
     * @default "left"
     */
    position?: PanelPosition;
    /**
     * side panel top offset
     *
     * @default 0
     */
    topOffset?: number;
}
/**
 * @private
 */
export declare const Overlay: React.FunctionComponent<OverlayProps>;
