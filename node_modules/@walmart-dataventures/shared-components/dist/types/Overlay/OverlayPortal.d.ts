import * as React from 'react';
import { CommonProps } from '../utility/commonProps';
import { PanelPosition } from './Overlay';
export interface OverlayPortalProps extends Omit<React.ComponentPropsWithoutRef<'div'>, 'className' | 'onChange' | 'style'>, CommonProps {
    /**
     * whether to open the panel as modal with background scrim
     *
     * @default false
     */
    disableScrim?: boolean;
    /**
     * The position for the panel.
     *
     * @default "left"
     */
    position?: PanelPosition;
    className?: any;
    /**
     * side panel top offset
     *
     * @default 0
     */
    topOffset?: number;
}
/**
 * @private
 */
export declare const OverlayPortal: React.FunctionComponent<OverlayPortalProps>;
