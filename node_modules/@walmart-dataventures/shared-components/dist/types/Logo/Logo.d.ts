import React from 'react';
interface ExtendedImgHTMLAttributes extends React.ImgHTMLAttributes<HTMLImageElement> {
    fetchpriority?: 'auto' | 'high' | 'low';
}
export interface LogoProps extends ExtendedImgHTMLAttributes {
    variant?: 'default' | 'inverted';
    height?: number;
    width?: number;
    className?: string;
}
/**
 *
 * The logo component represents the Scintilla app and comes in two variants:
 * Purple (default) and White (inverted), ensuring flexibility for different backgrounds and themes.
 *
 * ### Usage
 *
 * #### Colors
 * 1. Default Logo (Purple)
 * - Use on light backgrounds only.
 * - Ensure sufficient color contrast by following accessibility standards (WCAG AA).
 *
 *
 * 2. Inverted Logo (White)
 * - Use on dark backgrounds only.
 * - Ensure the logo remains clearly visible by maintaining contrast.
 *
 * ** Do not use non-approved colors or low-contrast combinations that reduce legibility. **
 * ** Always follow our design system color styles for consistency. **
 *
 * ### Retiring old logos
 *
 * - Scintilla is to be referred to on its own, ** NOT as Walmart Scintilla or as Walmart Data Ventures Scintilla. **
 * - The Scintilla logomark does not contain a graphical element. ** Please discontinue any use of the Walmart Luminate spotlight visual and/or Walmart Spark. **
 */
export declare const Logo: React.FC<LogoProps>;
export {};
