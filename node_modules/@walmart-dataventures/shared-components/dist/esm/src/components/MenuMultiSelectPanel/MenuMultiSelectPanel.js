import { objectWithoutProperties as _objectWithoutProperties, slicedToArray as _slicedToArray, toConsumableArray as _toConsumableArray, typeof as _typeof, objectSpread2 as _objectSpread2, defineProperty as _defineProperty } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import classNames from 'classnames';
import debounce from 'lodash/debounce';
import { forwardRef, useState, useRef, useId, useMemo, useCallback, useEffect, useLayoutEffect } from 'react';
import ReactDOM__default from 'react-dom';
import { Button } from '../../Button/Button.js';
import '../../Button/ButtonGroup.js';
import { useMenuPositioning } from '../../hooks/useMenuPositioning.js';
import { InfiniteScroll } from '../../InfiniteScroll/InfiniteScroll.js';
import { CONSTANTS } from '../../tokens/constants.js';
import { SearchBox } from '../SearchBox/SearchBox.js';
import '../SearchBox/SearchBoxWithChips.js';
import { isValidSegmentedControlOptionsLength, SegmentedControl } from '../SegmentedControl/SegmentedControl.js';
import { useMenuPanelTranslations } from '../shared/hooks/useMenuPanelTranslations.js';
import { Spinner } from '../Spinner/Spinner.js';
import { useTabStateManagement } from './hooks/useTabStateManagement.js';
import { MenuItemWithCheckbox } from './MenuItemWithCheckbox.js';
import styles from './MenuMultiSelectPanel.module.scss.js';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';

var _excluded = ["options", "groups", "selectedOptionIds", "initialSelectedOptions", "onApply", "onCancel", "onOptionToggle", "onClearAll", "onSelectAll", "showTabs", "tabs", "activeTabId", "onTabChange", "showSearch", "searchPlaceholder", "onSearch", "searchDebounceMs", "showSectionHeaders", "maxHeight", "menuHeader", "className", "UNSAFE_className", "UNSAFE_style", "aria-label", "aria-labelledby", "aria-describedby", "data-testid", "maxMenuItemWidth", "zIndex", "cancelText", "applyText", "isScrollable", "isLoading", "allowSelectionsFromSingleTab", "enableInfiniteLoading", "onLoadMore", "hasMoreData", "isLoadingMore", "offset", "anchorRef", "placement", "onClose"];
var EMPTY_ARRAY = [];

/** Resolve the DOM element for portal rendering */
function getPortalTarget() {
  try {
    var target = document.getElementById('root') || document.querySelector('[data-react-root]') || document.querySelector('[data-testid="app-root"]') || document.body;
    return target || document.body;
  } catch (_unused) {
    return document.body;
  }
}
var MenuMultiSelectPanel = /*#__PURE__*/forwardRef(function (_ref, _ref3) {
  var _ref2, _find$value, _find, _;
  var _ref$options = _ref.options,
    options = _ref$options === void 0 ? EMPTY_ARRAY : _ref$options,
    _ref$groups = _ref.groups,
    groups = _ref$groups === void 0 ? EMPTY_ARRAY : _ref$groups,
    _ref$selectedOptionId = _ref.selectedOptionIds,
    selectedOptionIds = _ref$selectedOptionId === void 0 ? EMPTY_ARRAY : _ref$selectedOptionId,
    _ref$initialSelectedO = _ref.initialSelectedOptions,
    initialSelectedOptions = _ref$initialSelectedO === void 0 ? EMPTY_ARRAY : _ref$initialSelectedO,
    onApply = _ref.onApply,
    onCancel = _ref.onCancel,
    onOptionToggle = _ref.onOptionToggle,
    onClearAll = _ref.onClearAll,
    onSelectAll = _ref.onSelectAll,
    _ref$showTabs = _ref.showTabs,
    showTabs = _ref$showTabs === void 0 ? false : _ref$showTabs,
    _ref$tabs = _ref.tabs,
    tabs = _ref$tabs === void 0 ? EMPTY_ARRAY : _ref$tabs,
    activeTabId = _ref.activeTabId,
    onTabChange = _ref.onTabChange,
    _ref$showSearch = _ref.showSearch,
    showSearch = _ref$showSearch === void 0 ? false : _ref$showSearch,
    searchPlaceholderProp = _ref.searchPlaceholder,
    onSearch = _ref.onSearch,
    _ref$searchDebounceMs = _ref.searchDebounceMs,
    searchDebounceMs = _ref$searchDebounceMs === void 0 ? 300 : _ref$searchDebounceMs,
    _ref$showSectionHeade = _ref.showSectionHeaders,
    showSectionHeaders = _ref$showSectionHeade === void 0 ? true : _ref$showSectionHeade,
    _ref$maxHeight = _ref.maxHeight,
    maxHeight = _ref$maxHeight === void 0 ? 360 : _ref$maxHeight,
    menuHeader = _ref.menuHeader,
    className = _ref.className,
    UNSAFE_className = _ref.UNSAFE_className,
    UNSAFE_style = _ref.UNSAFE_style,
    ariaLabel = _ref['aria-label'],
    ariaLabelledby = _ref['aria-labelledby'],
    ariaDescribedby = _ref['aria-describedby'],
    testId = _ref['data-testid'],
    maxMenuItemWidth = _ref.maxMenuItemWidth,
    _ref$zIndex = _ref.zIndex,
    zIndex = _ref$zIndex === void 0 ? CONSTANTS.Z_INDEX : _ref$zIndex,
    cancelTextProp = _ref.cancelText,
    applyTextProp = _ref.applyText,
    _ref$isScrollable = _ref.isScrollable,
    isScrollable = _ref$isScrollable === void 0 ? true : _ref$isScrollable,
    _ref$isLoading = _ref.isLoading,
    isLoading = _ref$isLoading === void 0 ? false : _ref$isLoading,
    _ref$allowSelectionsF = _ref.allowSelectionsFromSingleTab,
    allowSelectionsFromSingleTab = _ref$allowSelectionsF === void 0 ? false : _ref$allowSelectionsF,
    _ref$enableInfiniteLo = _ref.enableInfiniteLoading,
    enableInfiniteLoading = _ref$enableInfiniteLo === void 0 ? false : _ref$enableInfiniteLo,
    onLoadMore = _ref.onLoadMore,
    _ref$hasMoreData = _ref.hasMoreData,
    hasMoreData = _ref$hasMoreData === void 0 ? false : _ref$hasMoreData,
    _ref$isLoadingMore = _ref.isLoadingMore,
    isLoadingMore = _ref$isLoadingMore === void 0 ? false : _ref$isLoadingMore,
    offset = _ref.offset,
    anchorRef = _ref.anchorRef,
    _ref$placement = _ref.placement,
    placement = _ref$placement === void 0 ? 'bottom-start' : _ref$placement,
    onClose = _ref.onClose,
    rest = _objectWithoutProperties(_ref, _excluded);
  var _useMenuPanelTranslat = useMenuPanelTranslations(),
    labels = _useMenuPanelTranslat.labels,
    t = _useMenuPanelTranslat.t;
  var searchPlaceholder = searchPlaceholderProp !== null && searchPlaceholderProp !== void 0 ? searchPlaceholderProp : labels.searchPlaceholder;
  var cancelText = cancelTextProp !== null && cancelTextProp !== void 0 ? cancelTextProp : labels.cancel;
  var applyText = applyTextProp !== null && applyTextProp !== void 0 ? applyTextProp : labels.apply;
  var filterMenuOptionsByCategoryAriaLabel = t('MenuPanel:menuPanel.labels.filterMenuOptionsByCategory', 'Filter menu options by category');
  var searchAriaLabel = t('MenuPanel:menuPanel.labels.searchAriaLabel', 'Search {{target}}', {
    target: ariaLabel || t('MenuPanel:menuPanel.labels.menuOptions', 'menu options')
  });

  // ── State ──
  var _useState = useState(''),
    _useState2 = _slicedToArray(_useState, 2),
    searchQuery = _useState2[0],
    setSearchQuery = _useState2[1];
  var _useState3 = useState(selectedOptionIds),
    _useState4 = _slicedToArray(_useState3, 2),
    internalSelectedIds = _useState4[0],
    setInternalSelectedIds = _useState4[1];
  var _useState5 = useState(selectedOptionIds),
    _useState6 = _slicedToArray(_useState5, 2),
    sessionStartState = _useState6[0],
    setSessionStartState = _useState6[1];
  var sessionInitialized = useRef(false);

  // Ref to read current selection state inside stable callbacks (avoids
  // putting internalSelectedIds in dependency arrays which would break
  // memoisation of handlers like handleOptionToggle / handleClearAll).
  var internalSelectedIdsRef = useRef(internalSelectedIds);
  internalSelectedIdsRef.current = internalSelectedIds;

  // Cache option objects to preserve cross-tab selections
  var _useState7 = useState(function () {
      var initialCache = new Map();
      initialSelectedOptions.forEach(function (opt) {
        if (!opt.isHeader) {
          initialCache.set(opt.id, opt);
        }
      });
      return initialCache;
    }),
    _useState8 = _slicedToArray(_useState7, 2),
    selectedOptionsCache = _useState8[0],
    setSelectedOptionsCache = _useState8[1];

  // ── Refs ──
  var menuRef = useRef(null);
  var searchRef = useRef(null);
  var optionsContainerRef = useRef(null);
  var debouncedSearchRef = useRef(null);

  // ── IDs ──
  var uniqueId = useId();
  var menuId = useMemo(function () {
    return "menu-panel-multi-".concat(testId || uniqueId).replace(/[^a-zA-Z0-9\-_]/g, '-');
  }, [testId, uniqueId]);
  var scrollAreaId = useMemo(function () {
    return (testId ? "".concat(testId, "-scroll-area") : "menu-multi-panel-scroll-area-".concat(uniqueId)).replace(/[^a-zA-Z0-9\-_]/g, '-');
  }, [testId, uniqueId]);

  // ── Positioning ──
  var _useMenuPositioning = useMenuPositioning({
      anchorRef: anchorRef,
      menuRef: menuRef,
      placement: placement,
      offset: offset,
      zIndex: zIndex,
      isOpen: true // Panel is always "open" while mounted
    }),
    positionStyle = _useMenuPositioning.style;

  // The panel always uses internalSelectedIds for immediate feedback
  var currentSelectedIds = internalSelectedIds;

  // O(1) lookups for selection checks (avoids O(n²) with .includes in render loops)
  var selectedIdSet = useMemo(function () {
    return new Set(currentSelectedIds);
  }, [currentSelectedIds]);

  // ── Tab state management ──
  var _useTabStateManagemen = useTabStateManagement({
      allowSelectionsFromSingleTab: allowSelectionsFromSingleTab,
      showTabs: showTabs,
      tabs: tabs,
      currentSelectedIds: currentSelectedIds,
      activeTabId: activeTabId,
      selectedOptionIds: selectedOptionIds
    }),
    computedTabs = _useTabStateManagemen.computedTabs,
    setTabWithSelections = _useTabStateManagemen.setTabWithSelections;
  var safeOnTabChange = useCallback(function (tabValue) {
    onTabChange === null || onTabChange === void 0 || onTabChange(tabValue);
  }, [onTabChange]);

  // ── Development warnings ──
  useEffect(function () {
    if (process.env.NODE_ENV !== 'production') {
      if (allowSelectionsFromSingleTab && !showTabs) {
        console.warn('MenuMultiSelectPanel: `allowSelectionsFromSingleTab` requires `showTabs={true}` to be visible. ' + 'The single-tab constraint is enforced internally but tabs are not rendered.');
      }
    }
  }, [allowSelectionsFromSingleTab, showTabs]);

  // ── Flatten options from groups and direct options ──
  var allOptions = useMemo(function () {
    var flatOptions = _toConsumableArray(options);
    if (groups && groups.length > 0) {
      groups.forEach(function (group) {
        if (showSectionHeaders) {
          flatOptions.push({
            id: "header_".concat(group.id),
            label: group.label,
            isHeader: true
          });
        }
        flatOptions.push.apply(flatOptions, _toConsumableArray(group.options));
      });
    }
    return flatOptions;
  }, [options, groups, showSectionHeaders]);

  // Update cache with current options
  useEffect(function () {
    setSelectedOptionsCache(function (prevCache) {
      var newCache = new Map(prevCache);
      allOptions.forEach(function (opt) {
        if (!opt.isHeader) {
          newCache.set(opt.id, opt);
        }
      });
      return newCache;
    });
  }, [allOptions]);

  // ── Filtered options ──
  var filteredOptions = useMemo(function () {
    if (onSearch) return allOptions;
    if (!searchQuery) return allOptions;
    return allOptions.filter(function (option) {
      return option.isHeader || option.label.toLowerCase().includes(searchQuery.toLowerCase());
    });
  }, [allOptions, searchQuery, onSearch]);

  // Validate and process options
  var processedOptions = useMemo(function () {
    var validOptions = filteredOptions.filter(function (option) {
      if (!option || _typeof(option) !== 'object') return false;
      if (typeof option.id !== 'string' || !option.id.trim()) return false;
      if (typeof option.label !== 'string') return false;
      return true;
    });
    if (showSectionHeaders) {
      return validOptions;
    }
    return validOptions.filter(function (option) {
      return !option.isHeader;
    });
  }, [filteredOptions, showSectionHeaders]);

  // Selectable options for select-all/clear-all/count
  var selectableOptions = useMemo(function () {
    return allOptions.filter(function (option) {
      return !option.disabled && !option.isHeader;
    });
  }, [allOptions]);
  var hasSelectableOptions = useMemo(function () {
    return processedOptions.some(function (option) {
      return !option.isHeader && !option.disabled;
    });
  }, [processedOptions]);
  var selectedTabCount = useMemo(function () {
    return selectableOptions.filter(function (option) {
      return selectedIdSet.has(option.id);
    }).length;
  }, [selectableOptions, selectedIdSet]);
  var isAllSelected = selectableOptions.length > 0 && selectableOptions.every(function (option) {
    return selectedIdSet.has(option.id);
  });
  var segmentedControlTabs = useMemo(function () {
    var mappedTabs = (computedTabs || []).map(function (tab) {
      return {
        value: tab.value,
        label: tab.label,
        disabled: tab.disabled
      };
    });
    if (!isValidSegmentedControlOptionsLength(mappedTabs)) {
      return undefined;
    }
    return mappedTabs;
  }, [computedTabs]);

  // ── Session state tracking ──
  useEffect(function () {
    if (!sessionInitialized.current) {
      setSessionStartState(_toConsumableArray(selectedOptionIds));
      sessionInitialized.current = true;
    }
  }, [selectedOptionIds]);

  // ── Debounced search ──
  useEffect(function () {
    var _debouncedSearchRef$c;
    if ((_debouncedSearchRef$c = debouncedSearchRef.current) !== null && _debouncedSearchRef$c !== void 0 && _debouncedSearchRef$c.cancel) {
      debouncedSearchRef.current.cancel();
    }
    if (onSearch && searchDebounceMs > 0) {
      debouncedSearchRef.current = debounce(function (query) {
        onSearch(query);
      }, searchDebounceMs);
    } else if (onSearch && searchDebounceMs === 0) {
      debouncedSearchRef.current = Object.assign(function (query) {
        return onSearch(query);
      }, {
        cancel: function cancel() {}
      });
    } else {
      debouncedSearchRef.current = null;
    }
    return function () {
      var _debouncedSearchRef$c2;
      if ((_debouncedSearchRef$c2 = debouncedSearchRef.current) !== null && _debouncedSearchRef$c2 !== void 0 && _debouncedSearchRef$c2.cancel) {
        debouncedSearchRef.current.cancel();
      }
    };
  }, [onSearch, searchDebounceMs]);

  // ── Handlers ──

  var handleClose = useCallback(function () {
    var _debouncedSearchRef$c3;
    setSearchQuery('');
    (_debouncedSearchRef$c3 = debouncedSearchRef.current) === null || _debouncedSearchRef$c3 === void 0 || _debouncedSearchRef$c3.cancel();
    if (onSearch) {
      onSearch('');
    }
    onClose();
  }, [onClose, onSearch]);
  var handleOptionToggle = useCallback(function (optionId) {
    if (allOptions.find(function (opt) {
      return opt.id === optionId && (opt.disabled || opt.isHeader);
    })) {
      return;
    }

    // Read from ref so this callback stays stable across selection toggles
    var currentIds = internalSelectedIdsRef.current;
    var isCurrentlySelected = currentIds.includes(optionId);

    // Pure state update — no side effects inside the updater
    setInternalSelectedIds(function (prev) {
      return prev.includes(optionId) ? prev.filter(function (id) {
        return id !== optionId;
      }) : [].concat(_toConsumableArray(prev), [optionId]);
    });

    // Side effects outside the updater (safe for React 18 StrictMode)
    onOptionToggle === null || onOptionToggle === void 0 || onOptionToggle(optionId, !isCurrentlySelected);
    if (allowSelectionsFromSingleTab && showTabs) {
      if (isCurrentlySelected && currentIds.length === 1) {
        setTabWithSelections(null);
      } else if (!isCurrentlySelected && activeTabId) {
        setTabWithSelections(activeTabId);
      }
    }
  }, [allOptions, allowSelectionsFromSingleTab, showTabs, activeTabId, onOptionToggle, setTabWithSelections]);
  var itemHandlers = useMemo(function () {
    var handlers = {};
    filteredOptions.forEach(function (option) {
      if (!option.isHeader) {
        handlers[option.id] = {
          onToggle: function onToggle() {
            return handleOptionToggle(option.id);
          }
        };
      }
    });
    return handlers;
  }, [filteredOptions, handleOptionToggle]);
  var handleSearchChange = useCallback(function (event) {
    var newQuery = event.target.value;
    setSearchQuery(newQuery);
    if (debouncedSearchRef.current) {
      debouncedSearchRef.current(newQuery);
    }
  }, []);
  var handleSearchClear = useCallback(function () {
    var _debouncedSearchRef$c4;
    setSearchQuery('');
    (_debouncedSearchRef$c4 = debouncedSearchRef.current) === null || _debouncedSearchRef$c4 === void 0 || _debouncedSearchRef$c4.cancel();
    if (onSearch) {
      onSearch('');
    }
  }, [onSearch]);
  var handleApply = useCallback(function () {
    var selectedOpts = currentSelectedIds.map(function (id) {
      return selectedOptionsCache.get(id);
    }).filter(function (opt) {
      return opt !== undefined;
    });
    onApply(currentSelectedIds, selectedOpts);
    handleClose();
  }, [currentSelectedIds, selectedOptionsCache, onApply, handleClose]);
  var handleCancel = useCallback(function () {
    setInternalSelectedIds(selectedOptionIds || EMPTY_ARRAY);
    if (allowSelectionsFromSingleTab) {
      setTabWithSelections(null);
    }
    if (onCancel) {
      onCancel();
    }
    handleClose();
  }, [selectedOptionIds, onCancel, handleClose, allowSelectionsFromSingleTab, setTabWithSelections]);
  var handleSelectAll = useCallback(function () {
    var selectableInView = allOptions.filter(function (o) {
      return !o.isHeader && !o.disabled;
    }).map(function (o) {
      return o.id;
    });
    setInternalSelectedIds(function (prev) {
      return showTabs ? Array.from(new Set([].concat(_toConsumableArray(prev), _toConsumableArray(selectableInView)))) : selectableInView;
    });
    if (allowSelectionsFromSingleTab && showTabs && activeTabId && selectableInView.length > 0) {
      setTabWithSelections(activeTabId);
    }
    onSelectAll === null || onSelectAll === void 0 || onSelectAll(showTabs ? Array.from(new Set([].concat(_toConsumableArray(internalSelectedIdsRef.current), _toConsumableArray(selectableInView)))) : selectableInView);
  }, [allOptions, showTabs, allowSelectionsFromSingleTab, activeTabId, setTabWithSelections, onSelectAll]);
  var handleClearAll = useCallback(function () {
    var idsInView = new Set(allOptions.filter(function (o) {
      return !o.isHeader && !o.disabled;
    }).map(function (o) {
      return o.id;
    }));

    // Compute next state from ref so the side effect stays outside the updater
    // (avoids React 18 StrictMode double-invocation of setTimeout)
    var currentIds = internalSelectedIdsRef.current;
    var next = showTabs ? currentIds.filter(function (id) {
      return !idsInView.has(id);
    }) : [];
    setInternalSelectedIds(next);
    if (allowSelectionsFromSingleTab && showTabs && next.length === 0) {
      setTabWithSelections(null);
    }
    var clearedIds = showTabs ? currentIds.filter(function (id) {
      return idsInView.has(id);
    }) : _toConsumableArray(currentIds);
    onClearAll === null || onClearAll === void 0 || onClearAll(clearedIds);
  }, [allOptions, showTabs, allowSelectionsFromSingleTab, setTabWithSelections, onClearAll]);

  // ── Apply button enable logic ──
  var shouldEnableApply = useMemo(function () {
    var currentSorted = _toConsumableArray(currentSelectedIds).sort();
    var sessionSorted = _toConsumableArray(sessionStartState).sort();
    if (currentSorted.length !== sessionSorted.length) {
      return true;
    }
    return !currentSorted.every(function (id, index) {
      return id === sessionSorted[index];
    });
  }, [currentSelectedIds, sessionStartState]);

  // ── Menu header action handlers ──
  var handleMenuHeaderPrimaryAction = useCallback(function () {
    var _menuHeader$primaryAc;
    menuHeader === null || menuHeader === void 0 || (_menuHeader$primaryAc = menuHeader.primaryAction) === null || _menuHeader$primaryAc === void 0 || _menuHeader$primaryAc.onClick();
  }, [menuHeader]);
  var handleMenuHeaderSecondaryAction = useCallback(function () {
    var _menuHeader$secondary;
    menuHeader === null || menuHeader === void 0 || (_menuHeader$secondary = menuHeader.secondaryAction) === null || _menuHeader$secondary === void 0 || _menuHeader$secondary.onClick();
  }, [menuHeader]);

  // ── Keyboard navigation ──
  var handleMenuKeyDown = useCallback(function (event) {
    var currentFocusedElement = document.activeElement;
    var isTypingInInput = (currentFocusedElement === null || currentFocusedElement === void 0 ? void 0 : currentFocusedElement.tagName) === 'INPUT' || (currentFocusedElement === null || currentFocusedElement === void 0 ? void 0 : currentFocusedElement.tagName) === 'TEXTAREA';
    if (isTypingInInput) {
      if (event.key === 'Escape') {
        event.preventDefault();
        event.stopPropagation();
        handleCancel();
      }
      return;
    }
    var selectableOpts = filteredOptions.filter(function (option) {
      return !option.disabled && !option.isHeader;
    });
    if (selectableOpts.length === 0) {
      if (event.key === 'Escape') {
        event.preventDefault();
        event.stopPropagation();
        handleCancel();
      }
      return;
    }
    var currentIndex = selectableOpts.findIndex(function (option) {
      var element = document.getElementById("menu-item-".concat(option.id));
      return element === currentFocusedElement;
    });
    var nextIndex = currentIndex;
    switch (event.key) {
      case 'ArrowDown':
        event.preventDefault();
        nextIndex = currentIndex < selectableOpts.length - 1 ? currentIndex + 1 : 0;
        break;
      case 'ArrowUp':
        event.preventDefault();
        nextIndex = currentIndex > 0 ? currentIndex - 1 : selectableOpts.length - 1;
        break;
      case 'Home':
        event.preventDefault();
        nextIndex = 0;
        break;
      case 'End':
        event.preventDefault();
        nextIndex = selectableOpts.length - 1;
        break;
      case 'Enter':
      case ' ':
        event.preventDefault();
        if (currentIndex >= 0) {
          var targetOption = selectableOpts[currentIndex];
          handleOptionToggle(targetOption.id);
        }
        return;
      case 'Escape':
        event.preventDefault();
        event.stopPropagation();
        handleCancel();
        return;
      default:
        return;
    }
    var nextOption = selectableOpts[nextIndex];
    if (nextOption) {
      var nextElement = document.getElementById("menu-item-".concat(nextOption.id));
      nextElement === null || nextElement === void 0 || nextElement.focus();
    }
  }, [filteredOptions, handleOptionToggle, handleCancel]);

  // Attach keyboard handler to menu container
  useEffect(function () {
    if (!menuRef.current) return undefined;
    var menuElement = menuRef.current;
    menuElement.addEventListener('keydown', handleMenuKeyDown);
    return function () {
      menuElement.removeEventListener('keydown', handleMenuKeyDown);
    };
  }, [handleMenuKeyDown]);

  // ── Click-outside detection ──
  useEffect(function () {
    var handleClickOutside = function handleClickOutside(event) {
      var target = event.target;
      var isInsideMenu = menuRef.current && menuRef.current.contains(target);
      var isInsideAnchor = (anchorRef === null || anchorRef === void 0 ? void 0 : anchorRef.current) && anchorRef.current.contains(target);
      if (!isInsideMenu && !isInsideAnchor) {
        handleCancel();
      }
    };
    document.addEventListener('mousedown', handleClickOutside);
    return function () {
      document.removeEventListener('mousedown', handleClickOutside);
    };
  }, [handleCancel, anchorRef]);

  // ── Focus management on mount ──
  useLayoutEffect(function () {
    if (showSearch && searchRef.current) {
      searchRef.current.focus();
    } else {
      var selectableOpts = filteredOptions.filter(function (option) {
        return !option.disabled && !option.isHeader;
      });
      if (selectableOpts.length > 0) {
        var firstOption = selectableOpts[0];
        var firstElement = document.getElementById("menu-item-".concat(firstOption.id));
        firstElement === null || firstElement === void 0 || firstElement.focus();
      }
    }
    // Only run on mount
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  // ── Apply maxHeight to options container when scrollable ──
  useLayoutEffect(function () {
    var optionsContainer = optionsContainerRef.current;
    if (!optionsContainer) return;
    if (isScrollable) {
      optionsContainer.style.maxHeight = "".concat(maxHeight, "px");
    } else {
      optionsContainer.style.maxHeight = 'none';
    }
  }, [maxHeight, isScrollable]);

  // ── Event propagation control (modal-safe) ──
  var handlePointerDown = useCallback(function (e) {
    e.stopPropagation();
  }, []);
  var handleClick = useCallback(function (e) {
    e.stopPropagation();
  }, []);
  var handleKeyDownPropagation = useCallback(function (e) {
    e.stopPropagation();
  }, []);

  // Stable reference for onLoadMore fallback
  var handleLoadMore = useCallback(function () {
    if (onLoadMore) {
      onLoadMore();
    }
  }, [onLoadMore]);

  // ── Render helpers ──

  var renderOptionsList = function renderOptionsList() {
    return processedOptions.map(function (option) {
      if (option.isHeader) {
        return /*#__PURE__*/jsx("div", {
          className: styles.sectionHeader,
          role: "separator",
          children: option.label
        }, option.id);
      }
      var handlers = itemHandlers === null || itemHandlers === void 0 ? void 0 : itemHandlers[option.id];
      var isSelected = selectedIdSet.has(option.id);
      return /*#__PURE__*/jsx(MenuItemWithCheckbox, {
        option: option,
        isSelected: isSelected,
        onToggle: handlers === null || handlers === void 0 ? void 0 : handlers.onToggle
      }, option.id);
    });
  };
  var renderMenuContent = function renderMenuContent() {
    if (isLoading) {
      return /*#__PURE__*/jsx("div", {
        className: styles.loadingContainer,
        children: /*#__PURE__*/jsx(Spinner, {
          "aria-label": labels.loading
        })
      });
    }
    if (!hasSelectableOptions && !isLoadingMore) {
      return /*#__PURE__*/jsx("div", {
        className: styles.noResults,
        children: labels.noResultsFound
      });
    }
    return enableInfiniteLoading ? /*#__PURE__*/jsx(InfiniteScroll, {
      onLoadMore: handleLoadMore,
      hasMoreContent: hasMoreData && !isLoadingMore,
      isLoading: isLoadingMore,
      scrollableTargetId: scrollAreaId,
      children: /*#__PURE__*/jsxs(Fragment, {
        children: [renderOptionsList(), isLoadingMore && /*#__PURE__*/jsxs("div", {
          className: styles.loadingContainer,
          role: "status",
          "aria-live": "polite",
          children: [/*#__PURE__*/jsx("span", {
            className: styles.srOnly,
            children: labels.loadingMore
          }), /*#__PURE__*/jsx(Spinner, {
            "aria-hidden": "true",
            size: "small"
          })]
        })]
      })
    }) : renderOptionsList();
  };

  // ── Build panel element ──

  var panelElement = /*#__PURE__*/jsx("div", _objectSpread2(_objectSpread2({
    ref: function ref(node) {
      // Merge refs: internal menuRef + forwarded ref
      menuRef.current = node;
      if (typeof _ref3 === 'function') {
        _ref3(node);
      } else if (_ref3) {
        _ref3.current = node;
      }
    },
    className: classNames(styles.panel, _defineProperty({}, styles.panelInline, !anchorRef), className, UNSAFE_className),
    id: menuId,
    role: "menu",
    tabIndex: -1,
    "aria-label": ariaLabel,
    "aria-labelledby": ariaLabelledby,
    "aria-describedby": ariaDescribedby,
    "data-testid": testId ? "".concat(testId, "-menu") : undefined,
    "data-menu-dropdown": "true",
    style: _objectSpread2(_objectSpread2(_objectSpread2({}, positionStyle), maxMenuItemWidth ? {
      maxWidth: maxMenuItemWidth
    } : {}), UNSAFE_style),
    onPointerDown: handlePointerDown,
    onClick: handleClick,
    onKeyDown: handleKeyDownPropagation
  }, rest), {}, {
    children: /*#__PURE__*/jsxs("div", {
      className: classNames(styles.menuContainer, _defineProperty({}, styles.menuContainerScrollable, isScrollable)),
      children: [menuHeader && (menuHeader.title || menuHeader.primaryAction || menuHeader.secondaryAction) && /*#__PURE__*/jsx("div", {
        className: styles.menuHeader,
        children: /*#__PURE__*/jsxs("div", {
          className: styles.menuHeaderContent,
          children: [menuHeader.title && /*#__PURE__*/jsx("h3", {
            className: styles.menuHeaderTitle,
            children: menuHeader.title
          }), /*#__PURE__*/jsxs("div", {
            className: styles.menuHeaderActions,
            children: [menuHeader.primaryAction && /*#__PURE__*/jsx(Button, {
              type: "button",
              variant: "tertiary",
              UNSAFE_className: menuHeader.primaryAction.UNSAFE_className || styles.menuHeaderAction,
              onClick: handleMenuHeaderPrimaryAction,
              disabled: menuHeader.primaryAction.disabled,
              children: menuHeader.primaryAction.label
            }), menuHeader.secondaryAction && /*#__PURE__*/jsx(Button, {
              type: "button",
              variant: "tertiary",
              UNSAFE_className: menuHeader.secondaryAction.UNSAFE_className || styles.menuHeaderAction,
              onClick: handleMenuHeaderSecondaryAction,
              disabled: menuHeader.secondaryAction.disabled,
              children: menuHeader.secondaryAction.label
            })]
          })]
        })
      }), showTabs && segmentedControlTabs && /*#__PURE__*/jsx("div", {
        className: styles.tabContainer,
        children: /*#__PURE__*/jsx(SegmentedControl, {
          options: segmentedControlTabs,
          value: activeTabId && (computedTabs || []).some(function (tab) {
            return tab.value === activeTabId && !tab.disabled;
          }) ? activeTabId : (_ref2 = (_find$value = (_find = (computedTabs || []).find(function (tab) {
            return !tab.disabled;
          })) === null || _find === void 0 ? void 0 : _find.value) !== null && _find$value !== void 0 ? _find$value : (_ = (computedTabs || [])[0]) === null || _ === void 0 ? void 0 : _.value) !== null && _ref2 !== void 0 ? _ref2 : '',
          onChange: safeOnTabChange,
          width: "100%",
          "aria-label": filterMenuOptionsByCategoryAriaLabel
        })
      }), showSearch && /*#__PURE__*/jsx("div", {
        className: styles.searchContainer,
        children: /*#__PURE__*/jsx(SearchBox, {
          ref: searchRef,
          label: null,
          "aria-label": searchAriaLabel,
          value: searchQuery,
          onChange: handleSearchChange,
          onCloseIconClick: handleSearchClear,
          onKeyDown: function onKeyDown(e) {
            if (e.key === 'Escape') {
              e.preventDefault();
              e.stopPropagation();
              handleCancel();
            }
          },
          placeholder: searchPlaceholder,
          UNSAFE_className: styles.searchBox,
          size: "small"
        })
      }), /*#__PURE__*/jsxs("div", {
        className: styles.header,
        children: [/*#__PURE__*/jsxs("h3", {
          children: [/*#__PURE__*/jsx("span", {
            children: labels.selected
          }), /*#__PURE__*/jsx("span", {
            className: styles.headerCount,
            children: selectedTabCount
          })]
        }), /*#__PURE__*/jsxs("div", {
          className: styles.headerActions,
          children: [/*#__PURE__*/jsx(Button, {
            type: "button",
            UNSAFE_className: styles.clearAllButton,
            onClick: handleSelectAll,
            disabled: isAllSelected,
            children: labels.selectAll
          }), /*#__PURE__*/jsx(Button, {
            type: "button",
            UNSAFE_className: styles.clearAllButton,
            onClick: handleClearAll,
            disabled: selectedTabCount === 0,
            children: labels.clearAll
          })]
        })]
      }), /*#__PURE__*/jsx("div", {
        id: scrollAreaId,
        ref: optionsContainerRef,
        role: "presentation",
        className: isScrollable ? styles.optionsContainerScrollable : styles.optionsContainer,
        children: renderMenuContent()
      }), /*#__PURE__*/jsxs("div", {
        className: styles.footer,
        children: [/*#__PURE__*/jsx(Button, {
          UNSAFE_className: styles.cancelButton,
          onClick: handleCancel,
          type: "button",
          children: cancelText
        }), /*#__PURE__*/jsx(Button, {
          UNSAFE_className: styles.applyButton,
          onClick: handleApply,
          type: "button",
          variant: "primary",
          disabled: !shouldEnableApply,
          children: applyText
        })]
      })]
    })
  }));

  // ── Portal vs Inline rendering ──
  if (anchorRef) {
    return /*#__PURE__*/ReactDOM__default.createPortal(panelElement, getPortalTarget());
  }
  return panelElement;
});
MenuMultiSelectPanel.displayName = 'MenuMultiSelectPanel';

export { MenuMultiSelectPanel };
