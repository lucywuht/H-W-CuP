import { slicedToArray as _slicedToArray, objectSpread2 as _objectSpread2, defineProperty as _defineProperty } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import classNames from 'classnames';
import * as React from 'react';
import { useRef, useState, useCallback, useMemo, useEffect, useLayoutEffect } from 'react';
import { applyCommonProps } from '../../utility/commonProps.js';
import { throttle } from '../../utility/throttle.js';
import styles from './DVPopover.module.scss.js';
import { DvPopoverPortal } from './DVPopoverPortal.js';
import { createMemoizedPopoverPositionCalculator } from './positioning.js';
import { jsxs, Fragment, jsx } from 'react/jsx-runtime';

var POSITION_UPDATE_THROTTLE = 100;

// Simple module-level popover management to ensure only one popover is open at a time
var idCounter = 0;
var activePopover = null;

// Auto-reset for Jest tests
var lastJestTestName;
var autoResetForTests = function autoResetForTests() {
  var _expect, _expect$getState;
  // Only run in Jest environment
  if (typeof jest === 'undefined') return;
  var currentTestName = (_expect = global.expect) === null || _expect === void 0 || (_expect$getState = _expect.getState) === null || _expect$getState === void 0 || (_expect$getState = _expect$getState.call(_expect)) === null || _expect$getState === void 0 ? void 0 : _expect$getState.currentTestName;

  // Reset if we're in a new test
  if (currentTestName && currentTestName !== lastJestTestName) {
    if (activePopover) {
      activePopover.hide();
      activePopover = null;
    }
    idCounter = 0;
    lastJestTestName = currentTestName;
  }
};
var generatePopoverId = function generatePopoverId() {
  idCounter += 1;
  return "dv-popover-".concat(idCounter);
};
var showPopover = function showPopover(id, hideFunction, updatePositionFunction) {
  // Auto-reset for Jest tests
  autoResetForTests();

  // Hide current popover if it's different
  if (activePopover && activePopover.id !== id) {
    activePopover.hide();
  }
  activePopover = {
    id: id,
    hide: hideFunction,
    updatePosition: updatePositionFunction
  };
};
var hidePopover = function hidePopover(id) {
  var _activePopover;
  if (((_activePopover = activePopover) === null || _activePopover === void 0 ? void 0 : _activePopover.id) === id) {
    activePopover = null;
  }
};
var hideAllPopovers = function hideAllPopovers() {
  if (activePopover) {
    activePopover.hide();
    activePopover = null;
  }
};
var updateCurrentPopoverPosition = function updateCurrentPopoverPosition() {
  if (activePopover) {
    activePopover.updatePosition();
  }
};
var popoverManager = {
  generateId: generatePopoverId,
  show: showPopover,
  hide: hidePopover,
  hideAll: hideAllPopovers,
  updateCurrentPosition: updateCurrentPopoverPosition
};

// Custom hook for popover positioning logic
var usePopoverPositioning = function usePopoverPositioning(triggerRef, popoverRef, position) {
  // Create memoized position calculator
  var positionCalculator = useMemo(function () {
    return createMemoizedPopoverPositionCalculator();
  }, []);
  var updatePosition = useCallback(function () {
    if (!triggerRef.current || !popoverRef.current) return null;
    var triggerRect = triggerRef.current.getBoundingClientRect();
    var popoverRect = popoverRef.current.getBoundingClientRect();
    return positionCalculator(triggerRect, {
      width: popoverRect.width,
      height: popoverRect.height
    }, position);
  }, [position, positionCalculator, triggerRef, popoverRef]);
  return {
    updatePosition: updatePosition
  };
};

/**
 * A customizable popover component with portal rendering
 * @param props - The properties for the DVPopover component
 * @returns The DVPopover component
 */
var DVPopoverComponent = function DVPopoverComponent(props) {
  var _classNames;
  var _applyCommonProps = applyCommonProps(props, DVPopoverComponent),
    content = _applyCommonProps.content,
    _applyCommonProps$pos = _applyCommonProps.position,
    position = _applyCommonProps$pos === void 0 ? 'bottomCenter' : _applyCommonProps$pos,
    isOpen = _applyCommonProps.isOpen,
    onClose = _applyCommonProps.onClose,
    triggerRef = _applyCommonProps.triggerRef,
    _applyCommonProps$has = _applyCommonProps.hasNubbin,
    hasNubbin = _applyCommonProps$has === void 0 ? true : _applyCommonProps$has,
    _applyCommonProps$dis = _applyCommonProps.disabled,
    disabled = _applyCommonProps$dis === void 0 ? false : _applyCommonProps$dis,
    _applyCommonProps$cla = _applyCommonProps.className,
    className = _applyCommonProps$cla === void 0 ? '' : _applyCommonProps$cla,
    _applyCommonProps$zIn = _applyCommonProps.zIndex,
    zIndex = _applyCommonProps$zIn === void 0 ? 1000 : _applyCommonProps$zIn,
    _applyCommonProps$max = _applyCommonProps.maxWidth,
    maxWidth = _applyCommonProps$max === void 0 ? '280px' : _applyCommonProps$max,
    children = _applyCommonProps.children,
    _applyCommonProps$ari = _applyCommonProps['aria-label'],
    ariaLabel = _applyCommonProps$ari === void 0 ? 'Popover content' : _applyCommonProps$ari,
    ariaLabelledBy = _applyCommonProps['aria-labelledby'],
    ariaDescribedBy = _applyCommonProps['aria-describedby'],
    _applyCommonProps$dat = _applyCommonProps['data-testid'],
    testId = _applyCommonProps$dat === void 0 ? 'dvpopover' : _applyCommonProps$dat;
  var popoverId = useRef(popoverManager.generateId());
  var _useState = useState({
      phase: 'hidden',
      transform: {
        x: 0,
        y: 0
      },
      currentPosition: position
    }),
    _useState2 = _slicedToArray(_useState, 2),
    popoverState = _useState2[0],
    setPopoverState = _useState2[1];
  var phase = popoverState.phase,
    transform = popoverState.transform,
    currentPosition = popoverState.currentPosition,
    arrowOffset = popoverState.arrowOffset;
  var isVisible = phase === 'visible';
  var isMeasuring = phase === 'measuring';
  var popoverRef = useRef(null);

  // Refs for cleanup of async operations
  var rafRef = useRef();

  // Use custom positioning hook
  var _usePopoverPositionin = usePopoverPositioning(triggerRef, popoverRef, position),
    getPosition = _usePopoverPositionin.updatePosition;
  var updatePosition = useCallback(function () {
    var positionResult = getPosition();
    if (!positionResult) return;
    setPopoverState(function (prev) {
      return _objectSpread2(_objectSpread2({}, prev), {}, {
        transform: {
          x: positionResult.x,
          y: positionResult.y
        },
        currentPosition: positionResult.actualPosition,
        arrowOffset: positionResult.arrowOffset
      });
    });
  }, [getPosition]);
  var hidePopoverLocal = useCallback(function () {
    setPopoverState(function (prev) {
      return _objectSpread2(_objectSpread2({}, prev), {}, {
        phase: 'hidden'
      });
    });
    onClose();
  }, [onClose]);
  var showPopoverWithManager = useCallback(function () {
    if (disabled) return false;
    popoverManager.show(popoverId.current, hidePopoverLocal, updatePosition);

    // Start with measurement phase
    setPopoverState(function (prev) {
      return _objectSpread2(_objectSpread2({}, prev), {}, {
        phase: 'measuring',
        currentPosition: position,
        transform: {
          x: 0,
          y: 0
        }
      });
    });
    return true;
  }, [disabled, position, hidePopoverLocal, updatePosition]);
  var hidePopoverWithManager = useCallback(function () {
    popoverManager.hide(popoverId.current);
    hidePopoverLocal();
  }, [hidePopoverLocal]);

  // Create stable throttled function for scroll/resize events
  var throttledUpdatePosition = useMemo(function () {
    return throttle(function () {
      return popoverManager.updateCurrentPosition();
    }, POSITION_UPDATE_THROTTLE);
  }, []);

  // Handle isOpen prop changes
  useEffect(function () {
    if (isOpen && phase === 'hidden') {
      showPopoverWithManager();
    } else if (!isOpen && phase !== 'hidden') {
      hidePopoverWithManager();
    }
  }, [isOpen, phase, showPopoverWithManager, hidePopoverWithManager]);
  var stableHidePopover = useCallback(hidePopoverWithManager, [hidePopoverWithManager]);

  // Window event listeners
  useEffect(function () {
    var handleResize = throttledUpdatePosition;
    var handleScroll = throttledUpdatePosition;
    var handleEscape = function handleEscape(event) {
      if (event.key === 'Escape') {
        popoverManager.hideAll();
      }
    };
    var handleClickOutside = function handleClickOutside(event) {
      if (isVisible && triggerRef.current && popoverRef.current && !triggerRef.current.contains(event.target) && !popoverRef.current.contains(event.target)) {
        stableHidePopover();
      }
    };
    if (isVisible) {
      window.addEventListener('resize', handleResize);
      window.addEventListener('scroll', handleScroll, true);
      window.addEventListener('keydown', handleEscape);
      document.addEventListener('mousedown', handleClickOutside);
    }
    return function () {
      window.removeEventListener('resize', handleResize);
      window.removeEventListener('scroll', handleScroll, true);
      window.removeEventListener('keydown', handleEscape);
      document.removeEventListener('mousedown', handleClickOutside);
    };
  }, [isVisible, throttledUpdatePosition, stableHidePopover, triggerRef]);

  // Layout completion using double RAF pattern
  var ensureLayoutComplete = useCallback(function (callback) {
    // Clean up any previous RAF
    if (rafRef.current) {
      cancelAnimationFrame(rafRef.current);
      rafRef.current = undefined;
    }

    // Double RAF ensures layout calculations are complete
    rafRef.current = requestAnimationFrame(function () {
      if (rafRef.current === undefined) return;
      rafRef.current = requestAnimationFrame(function () {
        try {
          callback();
        } catch (error) {
          // eslint-disable-next-line no-console
          console.error('DVPopover positioning callback failed:', error);
        } finally {
          rafRef.current = undefined;
        }
      });
    });
  }, []);

  // Measurement phase using enhanced layout completion detection
  useLayoutEffect(function () {
    if (!isMeasuring) return;
    ensureLayoutComplete(function () {
      var finalPosition = getPosition();
      if (!finalPosition) return;
      setPopoverState(function (prev) {
        return _objectSpread2(_objectSpread2({}, prev), {}, {
          phase: 'visible',
          currentPosition: finalPosition.actualPosition,
          transform: {
            x: finalPosition.x,
            y: finalPosition.y
          },
          arrowOffset: finalPosition.arrowOffset
        });
      });
    });
  }, [isMeasuring, getPosition, ensureLayoutComplete]);

  // Cleanup effect
  useEffect(function () {
    return function () {
      popoverManager.hide(popoverId.current);
      if (rafRef.current) {
        cancelAnimationFrame(rafRef.current);
        rafRef.current = undefined;
      }
    };
  }, []);
  var getStyleForPosition = function getStyleForPosition(positionParam) {
    var positionStyles = {
      topLeft: styles.topLeft,
      topCenter: styles.topCenter,
      topRight: styles.topRight,
      bottomLeft: styles.bottomLeft,
      bottomCenter: styles.bottomCenter,
      bottomRight: styles.bottomRight,
      left: styles.left,
      right: styles.right
    };
    return positionStyles[positionParam];
  };
  var popoverClasses = classNames(styles.popover, getStyleForPosition(currentPosition), (_classNames = {}, _defineProperty(_classNames, styles.visible, isVisible), _defineProperty(_classNames, styles.measuring, isMeasuring), _defineProperty(_classNames, styles.hasNubbin, hasNubbin), _classNames), className);
  return /*#__PURE__*/jsxs(Fragment, {
    children: [children, (isVisible || isMeasuring) && /*#__PURE__*/jsx(DvPopoverPortal, {
      children: /*#__PURE__*/jsx("div", {
        ref: popoverRef,
        role: "tooltip",
        "aria-label": ariaLabel,
        "aria-labelledby": ariaLabelledBy,
        "aria-describedby": ariaDescribedBy || "".concat(popoverId.current, "-content"),
        id: popoverId.current,
        "data-testid": testId,
        className: popoverClasses,
        style: _objectSpread2({
          position: 'fixed',
          top: 0,
          left: 0,
          maxWidth: maxWidth,
          transform: "translate(".concat(transform.x, "px, ").concat(transform.y, "px)"),
          zIndex: zIndex
        }, arrowOffset && {
          '--arrow-offset': "".concat(arrowOffset)
        }),
        children: /*#__PURE__*/jsx("div", {
          className: styles.body,
          children: /*#__PURE__*/jsx("div", {
            id: "".concat(popoverId.current, "-content"),
            className: styles.content,
            role: "region",
            "aria-label": "Additional information",
            children: content
          })
        })
      })
    })]
  });
};
DVPopoverComponent.displayName = 'DVPopover';
var DVPopover = /*#__PURE__*/React.memo(DVPopoverComponent);

export { DVPopover, popoverManager };
