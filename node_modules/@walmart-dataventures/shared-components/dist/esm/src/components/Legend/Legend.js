import { objectWithoutProperties as _objectWithoutProperties, objectSpread2 as _objectSpread2 } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import classNames from 'classnames';
import { memo } from 'react';
import { applyCommonProps } from '../../utility/commonProps.js';
import styles from './Legend.module.scss.js';
import { LegendItem } from './LegendItem.js';
import { jsx } from 'react/jsx-runtime';

var _excluded = ["items", "orientation", "className"];
// Create a memoized version of LegendItem for better performance with large datasets
var MemoizedLegendItem = /*#__PURE__*/memo(LegendItem);

/**
 * Legend component that provides context for interpreting data in visualizations
 * @prop items Array of legend items to display.
 * @prop orientation (Optional) The orientation of the legend (vertical or horizontal). Default is vertical.
 * @prop className (Optional) Custom CSS class name for the legend container.
 * @prop 'aria-label' (Optional) aria-label for the component. Default is "DVLegend".
 */
var Legend = function Legend(props) {
  var _applyCommonProps = applyCommonProps(props, Legend),
    items = _applyCommonProps.items,
    _applyCommonProps$ori = _applyCommonProps.orientation,
    orientation = _applyCommonProps$ori === void 0 ? 'vertical' : _applyCommonProps$ori,
    className = _applyCommonProps.className,
    rest = _objectWithoutProperties(_applyCommonProps, _excluded);
  var legendClasses = classNames(styles.legend, orientation === 'vertical' ? styles.vertical : styles.horizontal, className);
  return /*#__PURE__*/jsx("div", _objectSpread2(_objectSpread2({
    className: legendClasses,
    role: "list",
    "aria-label": "DV Legend"
  }, rest), {}, {
    children: items.map(function (item, index) {
      return /*#__PURE__*/jsx(MemoizedLegendItem, _objectSpread2(_objectSpread2({
        role: "listitem"
      }, item), {}, {
        "aria-label": "".concat(item.label, " represented by ").concat(item.shape, " shape in ").concat(item.color)
      }), "legend-item-".concat(item.label || "item-".concat(index), "-").concat(index));
    })
  }));
};
Legend.displayName = 'Legend';

export { Legend };
