import { slicedToArray as _slicedToArray, toConsumableArray as _toConsumableArray, defineProperty as _defineProperty, typeof as _typeof } from '../../../_virtual/_rollupPluginBabelHelpers.js';
import classNames from 'classnames';
import { useState, useRef, useId, useCallback, useEffect, useMemo, useLayoutEffect } from 'react';
import { MenuPortal } from '../shared/MenuPortal.js';
import { MENU_MIN_AVAILABLE_SPACE, MENU_BELOW_GAP, MENU_MULTI_ABOVE_GAP, MENU_EDGE_BUFFER, MENU_MIN_SCROLL_AREA_HEIGHT, MENU_DEFAULT_MAX_HEIGHT } from '../shared/menuPositioningConstants.js';
import { Button } from '../../Button/Button.js';
import '../../Button/ButtonGroup.js';
import { InfiniteScroll } from '../../InfiniteScroll/InfiniteScroll.js';
import { SearchBox } from '../SearchBox/SearchBox.js';
import '../SearchBox/SearchBoxWithChips.js';
import { isValidSegmentedControlOptionsLength, SegmentedControl } from '../SegmentedControl/SegmentedControl.js';
import { Spinner } from '../Spinner/Spinner.js';
import { MenuItemWithCheckbox } from './MenuItemWithCheckbox.js';
import styles from './MenuMultiSelect.module.scss.js';
import { useMenuMultiSelectTranslations } from './hooks/useMenuMultiSelectTranslations.js';
import { jsx, jsxs, Fragment } from 'react/jsx-runtime';

var MenuDropdownContainerMultiple = function MenuDropdownContainerMultiple(_ref) {
  var _ref2, _tabs$find$value, _tabs$find, _tabs$;
  var isOpen = _ref.isOpen,
    menuRef = _ref.menuRef,
    triggerRef = _ref.triggerRef,
    _ref$maxHeight = _ref.maxHeight,
    maxHeight = _ref$maxHeight === void 0 ? 360 : _ref$maxHeight,
    testId = _ref.testId,
    menuHeader = _ref.menuHeader,
    _ref$showTabs = _ref.showTabs,
    showTabs = _ref$showTabs === void 0 ? false : _ref$showTabs,
    _ref$tabs = _ref.tabs,
    tabs = _ref$tabs === void 0 ? [] : _ref$tabs,
    activeTabId = _ref.activeTabId,
    safeOnTabChange = _ref.safeOnTabChange,
    _ref$showSearch = _ref.showSearch,
    showSearch = _ref$showSearch === void 0 ? false : _ref$showSearch,
    _ref$filteredOptions = _ref.filteredOptions,
    filteredOptions = _ref$filteredOptions === void 0 ? [] : _ref$filteredOptions,
    searchRef = _ref.searchRef,
    _ref$searchPlaceholde = _ref.searchPlaceholder,
    searchPlaceholder = _ref$searchPlaceholde === void 0 ? 'Search...' : _ref$searchPlaceholde,
    _ref$searchQuery = _ref.searchQuery,
    searchQuery = _ref$searchQuery === void 0 ? '' : _ref$searchQuery,
    handleSearchChange = _ref.handleSearchChange,
    handleSearchClear = _ref.handleSearchClear,
    handleSearchKeyDown = _ref.handleSearchKeyDown,
    _ref$itemHandlers = _ref.itemHandlers,
    itemHandlers = _ref$itemHandlers === void 0 ? {} : _ref$itemHandlers,
    _ref$selectedOptionId = _ref.selectedOptionIds,
    selectedOptionIds = _ref$selectedOptionId === void 0 ? [] : _ref$selectedOptionId,
    _ref$showSectionHeade = _ref.showSectionHeaders,
    showSectionHeaders = _ref$showSectionHeade === void 0 ? true : _ref$showSectionHeade,
    parentPosition = _ref.parentPosition,
    onApply = _ref.onApply,
    onCancel = _ref.onCancel,
    cancelText = _ref.cancelText,
    applyText = _ref.applyText,
    onSelectAll = _ref.onSelectAll,
    onClearAll = _ref.onClearAll,
    allOptions = _ref.allOptions,
    _ref$isScrollable = _ref.isScrollable,
    isScrollable = _ref$isScrollable === void 0 ? true : _ref$isScrollable,
    _ref$isLoading = _ref.isLoading,
    isLoading = _ref$isLoading === void 0 ? false : _ref$isLoading,
    _ref$enableInfiniteLo = _ref.enableInfiniteLoading,
    enableInfiniteLoading = _ref$enableInfiniteLo === void 0 ? false : _ref$enableInfiniteLo,
    onLoadMore = _ref.onLoadMore,
    _ref$hasMoreData = _ref.hasMoreData,
    hasMoreData = _ref$hasMoreData === void 0 ? false : _ref$hasMoreData,
    _ref$isLoadingMore = _ref.isLoadingMore,
    isLoadingMore = _ref$isLoadingMore === void 0 ? false : _ref$isLoadingMore,
    selectAllText = _ref.selectAllText,
    clearAllText = _ref.clearAllText,
    selectedLabel = _ref.selectedLabel;
  var _useMenuMultiSelectTr = useMenuMultiSelectTranslations(),
    messages = _useMenuMultiSelectTr.messages,
    placeholders = _useMenuMultiSelectTr.placeholders;
  // Track session state when menu opens
  var _useState = useState([]),
    _useState2 = _slicedToArray(_useState, 2),
    sessionStartState = _useState2[0],
    setSessionStartState = _useState2[1];
  var sessionInitialized = useRef(false);
  var optionsContainerRef = useRef(null);
  var _useState3 = useState(false),
    _useState4 = _slicedToArray(_useState3, 2),
    isPositioned = _useState4[0],
    setIsPositioned = _useState4[1];
  var positionDecided = useRef(null);
  var uniqueId = useId();
  // Must be a stable, unique DOM id — InfiniteScroll queries this element by id.
  // Sanitize to strip characters invalid in HTML id attributes (React useId returns
  // colons; testId may contain spaces or other special chars).
  var scrollAreaId = (testId ? "".concat(testId, "-scroll-area") : "menu-multi-scroll-area-".concat(uniqueId)).replace(/[^a-zA-Z0-9\-_]/g, '-');

  // Stable reference for onLoadMore fallback to avoid lint warning
  var handleLoadMore = useCallback(function () {
    if (onLoadMore) {
      onLoadMore();
    }
  }, [onLoadMore]);

  // Initialize session state only when dropdown initially opens
  useEffect(function () {
    if (isOpen && !sessionInitialized.current) {
      setSessionStartState(_toConsumableArray(selectedOptionIds));
      sessionInitialized.current = true;
      setIsPositioned(false); // Reset positioning flag when opening
      positionDecided.current = null; // Reset position decision
    } else if (!isOpen) {
      sessionInitialized.current = false;
      setIsPositioned(false);
      positionDecided.current = null;
    }
  }, [isOpen, selectedOptionIds]);

  // Handle Apply button click
  var handleApplyClick = useCallback(function () {
    onApply(selectedOptionIds);
  }, [onApply, selectedOptionIds]);

  // Check if Apply button should be enabled
  var shouldEnableApply = useMemo(function () {
    // Enable if there are any current selections
    if (selectedOptionIds.length > 0) {
      return true;
    }

    // Enable if current state is different from session start state (including clearing selections)
    var currentSorted = _toConsumableArray(selectedOptionIds).sort();
    var sessionSorted = _toConsumableArray(sessionStartState).sort();
    var hasChangesFromSessionStart = currentSorted.length !== sessionSorted.length || !currentSorted.every(function (id, index) {
      return id === sessionSorted[index];
    });
    return hasChangesFromSessionStart;
  }, [selectedOptionIds, sessionStartState]);

  // Validate and filter options for safety
  var validateOptions = function validateOptions(options) {
    if (!Array.isArray(options)) return [];
    return options.filter(function (option) {
      if (!option || _typeof(option) !== 'object') return false;
      if (typeof option.id !== 'string' || !option.id.trim()) return false;
      if (typeof option.label !== 'string') return false;
      return true;
    });
  };
  var processedOptions = useMemo(function () {
    var validOptions = validateOptions(filteredOptions);
    if (showSectionHeaders) {
      return validOptions;
    }
    return validOptions.filter(function (option) {
      return !option.isHeader;
    });
  }, [filteredOptions, showSectionHeaders]);

  // Check if there are any selectable options (non-header, non-disabled)
  var hasSelectableOptions = useMemo(function () {
    return processedOptions.some(function (option) {
      return !option.isHeader && !option.disabled;
    });
  }, [processedOptions]);
  var menuId = useMemo(function () {
    return "menu-".concat(testId || 'menu-multiple-select');
  }, [testId]);
  var triggerId = useMemo(function () {
    return "".concat(menuId, "-trigger");
  }, [menuId]);
  var selectableOptions = useMemo(function () {
    return allOptions.filter(function (option) {
      return !option.disabled && !option.isHeader;
    });
  }, [allOptions]);
  var selectedTabCount = useMemo(function () {
    return selectableOptions.filter(function (option) {
      return selectedOptionIds.includes(option.id);
    }).length;
  }, [selectableOptions, selectedOptionIds]);
  var isAllSelected = selectableOptions.length > 0 && selectableOptions.every(function (option) {
    return selectedOptionIds.includes(option.id);
  });
  var segmentedControlTabs = useMemo(function () {
    var mappedTabs = tabs.map(function (tab) {
      return {
        value: tab.value,
        label: tab.label,
        disabled: tab.disabled
      };
    });
    if (!isValidSegmentedControlOptionsLength(mappedTabs)) {
      return undefined;
    }
    return mappedTabs;
  }, [tabs]);

  // Handle menu header actions
  var handleMenuHeaderPrimaryAction = useCallback(function () {
    var _menuHeader$primaryAc;
    menuHeader === null || menuHeader === void 0 || (_menuHeader$primaryAc = menuHeader.primaryAction) === null || _menuHeader$primaryAc === void 0 || _menuHeader$primaryAc.onClick();
  }, [menuHeader]);
  var handleMenuHeaderSecondaryAction = useCallback(function () {
    var _menuHeader$secondary;
    menuHeader === null || menuHeader === void 0 || (_menuHeader$secondary = menuHeader.secondaryAction) === null || _menuHeader$secondary === void 0 || _menuHeader$secondary.onClick();
  }, [menuHeader]);

  // Handle pointer down event to prevent propagation
  var handlePointerDown = useCallback(function (e) {
    e.stopPropagation();
  }, []);

  // Handle click event to prevent propagation
  var handleClick = useCallback(function (e) {
    e.stopPropagation();
  }, []);

  // Handle keyboard event for menu role accessibility
  var handleKeyDown = useCallback(function (e) {
    e.stopPropagation();
  }, []);
  useLayoutEffect(function () {
    var _triggerRef$current$g, _triggerRef$current;
    var menu = menuRef.current;
    if (!isOpen || !parentPosition || !menu) {
      return;
    }

    // Live rect — avoids stale-position jumps when search/tab changes re-trigger
    // this effect after the user has scrolled since opening the menu.
    var rect = (_triggerRef$current$g = triggerRef === null || triggerRef === void 0 || (_triggerRef$current = triggerRef.current) === null || _triggerRef$current === void 0 ? void 0 : _triggerRef$current.getBoundingClientRect()) !== null && _triggerRef$current$g !== void 0 ? _triggerRef$current$g : parentPosition;
    var viewportHeight = window.innerHeight;
    var viewportWidth = window.innerWidth;
    if (positionDecided.current === null) {
      var _dropdownHeight = menu.offsetHeight;
      var spaceBelow = viewportHeight - rect.bottom;
      var spaceAbove = rect.top;
      var shouldPositionAbove = spaceBelow < _dropdownHeight && spaceAbove > spaceBelow;
      positionDecided.current = shouldPositionAbove ? 'top' : 'bottom';
    } else {
      // Sync with rAF's live flip decision so a mid-session direction change isn't overridden.
      var rafDecision = menu.getAttribute('data-position');
      if (rafDecision === 'top' || rafDecision === 'bottom') {
        positionDecided.current = rafDecision;
      }
    }
    var position = positionDecided.current;
    var dropdownHeight = menu.offsetHeight;
    var dropdownWidth = menu.offsetWidth;
    // See src/components/shared/menuPositioningConstants.ts for values and rationale.
    var BELOW_GAP = MENU_BELOW_GAP;
    var ABOVE_GAP = MENU_MULTI_ABOVE_GAP;
    var EDGE = MENU_EDGE_BUFFER;

    // Y: position above or below the trigger; clamp 'top' so the menu never bleeds above the viewport
    var top = position === 'top' ? Math.max(EDGE, rect.top - dropdownHeight - ABOVE_GAP) : rect.bottom + BELOW_GAP;

    // Shift: prevent horizontal viewport clipping
    var x = rect.left;
    if (x + dropdownWidth > viewportWidth - EDGE) x = Math.max(EDGE, viewportWidth - dropdownWidth - EDGE);
    if (x < EDGE) x = EDGE;

    // Set position attribute for styling
    menu.setAttribute('data-position', position);

    // translate3d is compositor-only (GPU) — unlike top/left it never triggers layout,
    // so the dropdown tracks the trigger with zero main-thread cost.
    menu.style.transform = "translate3d(".concat(x, "px, ").concat(top, "px, 0)");
    if (rect.width) {
      menu.style.minWidth = "".concat(rect.width, "px");
    }

    // Mark as positioned
    setIsPositioned(true);
  }, [isOpen, parentPosition, menuRef, filteredOptions.length, showTabs, showSearch, isLoading, triggerRef]);

  // Apply maxHeight to options container when scrollable.
  // Size middleware: cap to available viewport space so the scroll area never
  // extends off-screen, while still respecting the maxHeight prop as an upper bound.
  useLayoutEffect(function () {
    var _triggerRef$current$g2, _triggerRef$current2;
    var optionsContainer = optionsContainerRef.current;
    var menu = menuRef.current;
    if (!optionsContainer || !menu) return;
    if (!isScrollable) {
      optionsContainer.style.maxHeight = 'none';
      return;
    }

    // See src/components/shared/menuPositioningConstants.ts for values and rationale.
    var BELOW_GAP = MENU_BELOW_GAP;
    var ABOVE_GAP = MENU_MULTI_ABOVE_GAP;
    var EDGE = MENU_EDGE_BUFFER;
    var position = positionDecided.current;
    var liveRect = (_triggerRef$current$g2 = triggerRef === null || triggerRef === void 0 || (_triggerRef$current2 = triggerRef.current) === null || _triggerRef$current2 === void 0 ? void 0 : _triggerRef$current2.getBoundingClientRect()) !== null && _triggerRef$current$g2 !== void 0 ? _triggerRef$current$g2 : parentPosition;
    var available = position && liveRect ? Math.max(MENU_MIN_AVAILABLE_SPACE, position === 'top' ? liveRect.top - ABOVE_GAP - EDGE : window.innerHeight - liveRect.bottom - BELOW_GAP - EDGE) : Infinity;

    // Subtract fixed chrome (header, search, tabs, footer with Apply/Cancel) so the
    // total menu height = available — this keeps the Apply button always in view.
    var overhead = menu.offsetHeight - optionsContainer.offsetHeight;
    var rawMax = typeof maxHeight === 'string' ? parseInt(maxHeight, 10) : maxHeight;
    var maxHeightPx = rawMax !== undefined && !isNaN(rawMax) ? rawMax : MENU_DEFAULT_MAX_HEIGHT;
    var nextMaxHeight = "".concat(Math.min(maxHeightPx, Math.max(MENU_MIN_SCROLL_AREA_HEIGHT, available - overhead)), "px");
    if (optionsContainer.style.maxHeight !== nextMaxHeight) {
      optionsContainer.style.maxHeight = nextMaxHeight;
    }
  }, [maxHeight, isScrollable, parentPosition, menuRef, filteredOptions.length, showTabs, showSearch, isLoading, triggerRef]);
  if (!isOpen) return null;

  /**
   * Renders the list of menu options
   * @returns React element for options list
   */
  function renderOptionsList() {
    return /*#__PURE__*/jsx(Fragment, {
      children: processedOptions.map(function (option) {
        if (option.isHeader) {
          return /*#__PURE__*/jsx("div", {
            className: styles.sectionHeader,
            children: option.label
          }, option.id);
        }
        var handlers = itemHandlers === null || itemHandlers === void 0 ? void 0 : itemHandlers[option.id];
        var isSelected = selectedOptionIds.includes(option.id);
        return /*#__PURE__*/jsx(MenuItemWithCheckbox, {
          option: option,
          isSelected: isSelected,
          onToggle: handlers === null || handlers === void 0 ? void 0 : handlers.onToggle
        }, option.id);
      })
    });
  }

  /**
   * Renders content for the options container based on state
   * @returns React element for loading/empty/options state
   */
  function renderMenuContent() {
    if (isLoading) {
      return /*#__PURE__*/jsx("div", {
        className: styles.loadingContainer,
        children: /*#__PURE__*/jsx(Spinner, {
          "aria-label": "Loading"
        })
      });
    }
    if (!hasSelectableOptions && !isLoadingMore) {
      return /*#__PURE__*/jsx("div", {
        className: styles.noResults,
        children: messages.noResults
      });
    }
    return enableInfiniteLoading ? /*#__PURE__*/jsx(InfiniteScroll, {
      onLoadMore: handleLoadMore,
      hasMoreContent: hasMoreData && !isLoadingMore,
      isLoading: isLoadingMore,
      scrollableTargetId: scrollAreaId,
      children: /*#__PURE__*/jsxs(Fragment, {
        children: [renderOptionsList(), isLoadingMore && /*#__PURE__*/jsxs("div", {
          className: styles.loadingContainer,
          role: "status",
          "aria-live": "polite",
          children: [/*#__PURE__*/jsx("span", {
            className: styles.srOnly,
            children: "Loading more options"
          }), /*#__PURE__*/jsx(Spinner, {
            "aria-hidden": "true",
            size: "small"
          })]
        })]
      })
    }, processedOptions.length) : renderOptionsList();
  }
  return /*#__PURE__*/jsx(MenuPortal, {
    children: /*#__PURE__*/jsx("div", {
      ref: menuRef,
      className: styles.dropdown,
      id: menuId,
      role: "menu",
      "aria-labelledby": triggerId,
      "data-testid": testId ? "".concat(testId, "-menu") : undefined,
      "data-menu-dropdown": "true",
      style: {
        visibility: isPositioned ? 'visible' : 'hidden'
      },
      tabIndex: 0,
      onPointerDown: handlePointerDown,
      onClick: handleClick,
      onKeyDown: handleKeyDown,
      children: /*#__PURE__*/jsxs("div", {
        className: classNames(styles.menuContainer, _defineProperty({}, styles.menuContainerScrollable, isScrollable)),
        children: [menuHeader && (menuHeader.title || menuHeader.primaryAction || menuHeader.secondaryAction) && /*#__PURE__*/jsx("div", {
          className: styles.menuHeader,
          children: /*#__PURE__*/jsxs("div", {
            className: styles.menuHeaderContent,
            children: [menuHeader.title && /*#__PURE__*/jsx("h3", {
              className: styles.menuHeaderTitle,
              children: menuHeader.title
            }), /*#__PURE__*/jsxs("div", {
              className: styles.menuHeaderActions,
              children: [menuHeader.primaryAction && /*#__PURE__*/jsx(Button, {
                type: "button",
                variant: "tertiary",
                UNSAFE_className: menuHeader.primaryAction.UNSAFE_className || styles.menuHeaderAction,
                onClick: handleMenuHeaderPrimaryAction,
                disabled: menuHeader.primaryAction.disabled,
                children: menuHeader.primaryAction.label
              }), menuHeader.secondaryAction && /*#__PURE__*/jsx(Button, {
                type: "button",
                variant: "tertiary",
                UNSAFE_className: menuHeader.secondaryAction.UNSAFE_className || styles.menuHeaderAction,
                onClick: handleMenuHeaderSecondaryAction,
                disabled: menuHeader.secondaryAction.disabled,
                children: menuHeader.secondaryAction.label
              })]
            })]
          })
        }), showTabs && segmentedControlTabs && /*#__PURE__*/jsx("div", {
          className: styles.tabContainer,
          children: /*#__PURE__*/jsx(SegmentedControl, {
            options: segmentedControlTabs,
            value: activeTabId && tabs.some(function (tab) {
              return tab.value === activeTabId && !tab.disabled;
            }) ? activeTabId : (_ref2 = (_tabs$find$value = (_tabs$find = tabs.find(function (tab) {
              return !tab.disabled;
            })) === null || _tabs$find === void 0 ? void 0 : _tabs$find.value) !== null && _tabs$find$value !== void 0 ? _tabs$find$value : (_tabs$ = tabs[0]) === null || _tabs$ === void 0 ? void 0 : _tabs$.value) !== null && _ref2 !== void 0 ? _ref2 : '',
            onChange: safeOnTabChange,
            width: "100%",
            "aria-label": "Filter menu options by category"
          })
        }), showSearch && /*#__PURE__*/jsx("div", {
          className: styles.searchContainer,
          children: /*#__PURE__*/jsx(SearchBox, {
            ref: searchRef,
            label: null,
            "aria-label": searchPlaceholder || (placeholders === null || placeholders === void 0 ? void 0 : placeholders.search) || 'Search options',
            value: searchQuery,
            onChange: handleSearchChange,
            onCloseIconClick: handleSearchClear,
            onKeyDown: handleSearchKeyDown,
            placeholder: searchPlaceholder,
            UNSAFE_className: styles.searchBox,
            size: "small"
          })
        }), /*#__PURE__*/jsxs("div", {
          className: styles.header,
          children: [/*#__PURE__*/jsxs("h3", {
            children: [/*#__PURE__*/jsx("span", {
              children: selectedLabel
            }), /*#__PURE__*/jsx("span", {
              className: styles.headerCount,
              children: selectedTabCount
            })]
          }), /*#__PURE__*/jsxs("div", {
            className: styles.headerActions,
            children: [/*#__PURE__*/jsx(Button, {
              type: "button",
              UNSAFE_className: styles.selectAllButton,
              onClick: onSelectAll,
              disabled: isAllSelected,
              children: selectAllText
            }), /*#__PURE__*/jsx(Button, {
              type: "button",
              UNSAFE_className: styles.clearAllButton,
              onClick: onClearAll,
              disabled: selectedTabCount === 0,
              children: clearAllText
            })]
          })]
        }), /*#__PURE__*/jsx("div", {
          id: scrollAreaId,
          ref: optionsContainerRef,
          "data-menu-scroll-area": "true",
          className: isScrollable ? styles.optionsContainerScrollable : styles.optionsContainer,
          children: renderMenuContent()
        }), /*#__PURE__*/jsxs("div", {
          className: styles.footer,
          children: [/*#__PURE__*/jsx(Button, {
            UNSAFE_className: styles.cancelButton,
            onClick: onCancel,
            type: "button",
            children: cancelText
          }), /*#__PURE__*/jsx(Button, {
            UNSAFE_className: styles.applyButton,
            onClick: handleApplyClick,
            type: "button",
            variant: "primary",
            disabled: !shouldEnableApply,
            children: applyText
          })]
        })]
      })
    })
  });
};

export { MenuDropdownContainerMultiple as default };
