import { objectWithoutProperties as _objectWithoutProperties, objectSpread2 as _objectSpread2 } from '../../_virtual/_rollupPluginBabelHelpers.js';
import { componentErrorMessageTitleAliasOptionsWeight } from '@livingdesign/tokens/dist/js/light/regular/components/ErrorMessage';
import 'react';
import { Heading, Body } from '@walmart-web/livingdesign-components';
import { applyCommonProps } from '../utility/commonProps.js';
import styles from './ErrorMessage.module.scss.js';
import { jsxs, jsx } from 'react/jsx-runtime';

var _excluded = ["actions", "children", "media", "title"];
/**
 * Error Messages provide a familiar layout with descriptive content and suggested actions for error scenarios.
 *
 * - {@link https://livingdesign.walmart.com/components/error-message/ Guidelines}
 * - {@link https://livingdesign.walmart.com/components/error-message/react/ React documentation}
 *
 * ### Content
 *
 * #### Children and title
 *
 * The Error Message requires a title and text content. Provide a title using the `title` prop and text content using the `children` prop:
 *
 * ```tsx
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 *
 * #### Actions
 *
 * The Error Message can include buttons to perform actions relevant to the displayed error. Provide button-like elements through the `actions` prop:
 *
 * ```tsx
 * import * as React from "react";
 * import {
 *   Button,
 *   ErrorMessage
 * } from "@walmart-web/livingdesign-components";
 *
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 *   actions={
 *     <Button size="medium" variant="primary" onClick={...}>
 *       Refresh page
 *     </Button>
 *   }
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 * #### Media
 *
 * The Error Message can include media, such as an illustration. Set the `media` prop to display an image with the Error Message:
 *
 *  ```tsx
 * <ErrorMessage
 *   title="Service details aren’t available right now"
 *   media={
 *     <img
 *       alt=""
 *       height="200"
 *       src="/SearchNotFoundCircle.svg"
 *       width="200"
 *     />
 *   }
 * >
 *   Refresh this page to try again.
 * </ErrorMessage>
 * ```
 */
var ErrorMessage = function ErrorMessage(props) {
  var _applyCommonProps = applyCommonProps(props, ErrorMessage),
    actions = _applyCommonProps.actions,
    children = _applyCommonProps.children,
    media = _applyCommonProps.media,
    title = _applyCommonProps.title,
    rest = _objectWithoutProperties(_applyCommonProps, _excluded);
  return /*#__PURE__*/jsxs("div", _objectSpread2(_objectSpread2({
    className: styles.errorMessage
  }, rest), {}, {
    children: [media && /*#__PURE__*/jsx("div", {
      className: styles.media,
      children: media
    }), /*#__PURE__*/jsx(Heading, {
      as: "div",
      weight: componentErrorMessageTitleAliasOptionsWeight,
      UNSAFE_className: styles.title,
      children: title
    }), /*#__PURE__*/jsx(Body, {
      as: "div",
      UNSAFE_className: styles.textLabel,
      children: children
    }), actions && /*#__PURE__*/jsx("div", {
      className: styles.actions,
      children: actions
    })]
  }));
};

export { ErrorMessage };
