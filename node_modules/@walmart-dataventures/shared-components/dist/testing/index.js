'use strict';

function _arrayLikeToArray(r, a) {
  (null == a || a > r.length) && (a = r.length);
  for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
  return n;
}
function _classCallCheck(a, n) {
  if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _defineProperties(e, r) {
  for (var t = 0; t < r.length; t++) {
    var o = r[t];
    o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o);
  }
}
function _createClass(e, r, t) {
  return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", {
    writable: !1
  }), e;
}
function _createForOfIteratorHelper(r, e) {
  var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (!t) {
    if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) {
      t && (r = t);
      var n = 0,
        F = function () {};
      return {
        s: F,
        n: function () {
          return n >= r.length ? {
            done: !0
          } : {
            done: !1,
            value: r[n++]
          };
        },
        e: function (r) {
          throw r;
        },
        f: F
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  var o,
    a = !0,
    u = !1;
  return {
    s: function () {
      t = t.call(r);
    },
    n: function () {
      var r = t.next();
      return a = r.done, r;
    },
    e: function (r) {
      u = !0, o = r;
    },
    f: function () {
      try {
        a || null == t.return || t.return();
      } finally {
        if (u) throw o;
      }
    }
  };
}
function _defineProperty(e, r, t) {
  return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : e[r] = t, e;
}
function _toPrimitive(t, r) {
  if ("object" != typeof t || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r || "default");
    if ("object" != typeof i) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}
function _toPropertyKey(t) {
  var i = _toPrimitive(t, "string");
  return "symbol" == typeof i ? i : i + "";
}
function _unsupportedIterableToArray(r, a) {
  if (r) {
    if ("string" == typeof r) return _arrayLikeToArray(r, a);
    var t = {}.toString.call(r).slice(8, -1);
    return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
  }
}

/**
 * Configurable ResizeObserver mock for JSDOM test environments.
 *
 * Fires synchronously on observe() with default dimensions (200x150).
 * Use triggerResize() helper to simulate resize events in tests.
 */
var ResizeObserverMock = /*#__PURE__*/function () {
  function ResizeObserverMock(callback) {
    _classCallCheck(this, ResizeObserverMock);
    _defineProperty(this, "targets", new Map());
    this.callback = callback;
    ResizeObserverMock.instances.add(this);
  }
  _createClass(ResizeObserverMock, [{
    key: "observe",
    value: function observe(target, _options) {
      // Default dimensions (can be overridden via triggerResize)
      var dimensions = {
        width: 200,
        height: 150
      };
      this.targets.set(target, dimensions);

      // Fire callback immediately with default dimensions
      this.notifyResize(target, dimensions);
    }
  }, {
    key: "unobserve",
    value: function unobserve(target) {
      this.targets["delete"](target);
    }
  }, {
    key: "disconnect",
    value: function disconnect() {
      this.targets.clear();
      ResizeObserverMock.instances["delete"](this);
    }
  }, {
    key: "notifyResize",
    value: function notifyResize(target, dimensions) {
      // Populate box sizes for components that might check these values
      var boxSize = {
        inlineSize: dimensions.width,
        blockSize: dimensions.height
      };
      var entry = {
        target: target,
        contentRect: {
          width: dimensions.width,
          height: dimensions.height,
          top: 0,
          left: 0,
          bottom: dimensions.height,
          right: dimensions.width,
          x: 0,
          y: 0
        },
        borderBoxSize: [boxSize],
        contentBoxSize: [boxSize],
        devicePixelContentBoxSize: [boxSize]
      };
      this.callback([entry], this);
    }

    // Internal method for triggerResize helper
  }], [{
    key: "_triggerForElement",
    value: function _triggerForElement(element, dimensions) {
      var _iterator = _createForOfIteratorHelper(ResizeObserverMock.instances),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var instance = _step.value;
          if (instance.targets.has(element)) {
            instance.targets.set(element, dimensions);
            instance.notifyResize(element, dimensions);
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }

    /**
     * Clears all ResizeObserver instances.
     * Automatically called by setupSharedComponentsTests() in beforeEach hooks.
     * Can also be called manually in test cleanup if needed.
     */
  }, {
    key: "clearAllInstances",
    value: function clearAllInstances() {
      ResizeObserverMock.instances.clear();
    }
  }]);
  return ResizeObserverMock;
}();
// Store instances for triggerResize helper
_defineProperty(ResizeObserverMock, "instances", new Set());
var cleanupRegistered = false;

/**
 * Sets up global ResizeObserver mock for JSDOM test environment.
 * Automatically cleans up instances between tests to prevent memory leaks.
 *
 * Call this in your Jest setupFilesAfterEnv:
 *
 * @example
 * // jest.setup.js
 * import { setupSharedComponentsTests } from '@walmart-dataventures/shared-components/testing';
 * setupSharedComponentsTests();
 */
function setupSharedComponentsTests() {
  if (typeof global.ResizeObserver === 'undefined') {
    global.ResizeObserver = ResizeObserverMock;
  }

  // Automatically clean up instances between tests to prevent memory leaks
  // Only register once to avoid "Hooks cannot be defined inside tests" errors
  if (typeof beforeEach !== 'undefined' && !cleanupRegistered) {
    cleanupRegistered = true;
    beforeEach(function () {
      ResizeObserverMock.clearAllInstances();
    });
  }
}

/**
 * Test helper to manually trigger ResizeObserver callbacks.
 *
 * Use this in tests to simulate element resize events:
 *
 * @example
 * import { triggerResize } from '@walmart-dataventures/shared-components/testing';
 *
 * test('menu repositions on resize', () => {
 *   const { getByRole } = render(<MenuAction />);
 *   const menu = getByRole('menu');
 *
 *   // Simulate menu growing taller
 *   triggerResize(menu, { width: 300, height: 500 });
 *
 *   expect(menu).toHaveStyle({ transform: 'translate(...)' });
 * });
 */
function triggerResize(element, dimensions) {
  // Trigger ResizeObserver callbacks for this element
  ResizeObserverMock._triggerForElement(element, dimensions);
  var rect = {
    width: dimensions.width,
    height: dimensions.height,
    top: 0,
    left: 0,
    bottom: dimensions.height,
    right: dimensions.width,
    x: 0,
    y: 0,
    toJSON: function toJSON() {
      return {};
    }
  };

  // Update getBoundingClientRect without Jest dependency
  element.getBoundingClientRect = function () {
    return rect;
  };
}

exports.ResizeObserverMock = ResizeObserverMock;
exports.setupSharedComponentsTests = setupSharedComponentsTests;
exports.triggerResize = triggerResize;
