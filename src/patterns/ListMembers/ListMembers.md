# ListMembers

**Import:** `import { ListMembers } from "./patterns/ListMembers"`
**Category:** patterns

## Composition

`ListMembers` is part of a compound component. Use together with: `ListMembersList`, `ListMembersItem`.

All pieces import from the same path (`./patterns/ListMembers`). See each sibling's `.md` for its API.

## Props

_(no public props)_