# ListAssociateList

**Import:** `import { ListAssociateList } from "./patterns/ListAssociate"`
**Category:** patterns

## Composition

`ListAssociateList` is part of a compound component. Use together with: `ListAssociate`, `ListAssociateItem`.

All pieces import from the same path (`./patterns/ListAssociate`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)