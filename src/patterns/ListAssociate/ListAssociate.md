# ListAssociate

**Import:** `import { ListAssociate } from "./patterns/ListAssociate"`
**Category:** patterns

## Composition

`ListAssociate` is part of a compound component. Use together with: `ListAssociateList`, `ListAssociateItem`.

All pieces import from the same path (`./patterns/ListAssociate`). See each sibling's `.md` for its API.

## Props

_(no public props)_