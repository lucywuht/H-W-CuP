# HeaderDesktop

**Import:** `import { HeaderDesktop } from "./patterns/Header"`
**Category:** patterns

## Composition

`HeaderDesktop` is part of a compound component. Use together with: `HeaderMobile`, `HeaderMobileTopNav`, `Header`.

All pieces import from the same path (`./patterns/Header`). See each sibling's `.md` for its API.

## Props

_(no public props)_