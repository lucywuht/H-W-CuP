# HeaderMobile

**Import:** `import { HeaderMobile } from "./patterns/Header"`
**Category:** patterns

## Composition

`HeaderMobile` is part of a compound component. Use together with: `HeaderDesktop`, `HeaderMobileTopNav`, `Header`.

All pieces import from the same path (`./patterns/Header`). See each sibling's `.md` for its API.

## Props

_(no public props)_