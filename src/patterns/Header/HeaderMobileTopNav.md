# HeaderMobileTopNav

**Import:** `import { HeaderMobileTopNav } from "./patterns/Header"`
**Category:** patterns

## Composition

`HeaderMobileTopNav` is part of a compound component. Use together with: `HeaderDesktop`, `HeaderMobile`, `Header`.

All pieces import from the same path (`./patterns/Header`). See each sibling's `.md` for its API.

## Props

_(no public props)_