# ListAction

**Import:** `import { ListAction } from "./patterns/ListAction"`
**Category:** patterns

## Composition

`ListAction` is part of a compound component. Use together with: `ListActionList`, `ListActionItem`.

All pieces import from the same path (`./patterns/ListAction`). See each sibling's `.md` for its API.

## Props

_(no public props)_