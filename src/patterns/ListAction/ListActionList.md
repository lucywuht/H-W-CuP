# ListActionList

**Import:** `import { ListActionList } from "./patterns/ListAction"`
**Category:** patterns

## Composition

`ListActionList` is part of a compound component. Use together with: `ListAction`, `ListActionItem`.

All pieces import from the same path (`./patterns/ListAction`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)