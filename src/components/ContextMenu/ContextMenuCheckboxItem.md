# ContextMenuCheckboxItem

**Import:** `import { ContextMenuCheckboxItem } from "./components/ContextMenu"`
**Category:** components

## Composition

`ContextMenuCheckboxItem` is part of a compound component. Use together with: `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuRadioItem`, `ContextMenuLabel`, `ContextMenuShortcut`, `ContextMenuSub`, `ContextMenuSubContent`, `ContextMenuSubTrigger`, `ContextMenuRadioGroup`, `ContextMenu`, `ContextMenuSeparator`.

All pieces import from the same path (`./components/ContextMenu`). See each sibling's `.md` for its API.

## Props

_(no public props)_