# ContextMenu

**Import:** `import { ContextMenu } from "./components/ContextMenu"`
**Category:** components

## Composition

`ContextMenu` is part of a compound component. Use together with: `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuCheckboxItem`, `ContextMenuRadioItem`, `ContextMenuLabel`, `ContextMenuShortcut`, `ContextMenuSub`, `ContextMenuSubContent`, `ContextMenuSubTrigger`, `ContextMenuRadioGroup`, `ContextMenuSeparator`.

All pieces import from the same path (`./components/ContextMenu`). See each sibling's `.md` for its API.

## Props

_(no public props)_