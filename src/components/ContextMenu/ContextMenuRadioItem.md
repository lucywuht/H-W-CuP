# ContextMenuRadioItem

**Import:** `import { ContextMenuRadioItem } from "./components/ContextMenu"`
**Category:** components

## Composition

`ContextMenuRadioItem` is part of a compound component. Use together with: `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuCheckboxItem`, `ContextMenuLabel`, `ContextMenuShortcut`, `ContextMenuSub`, `ContextMenuSubContent`, `ContextMenuSubTrigger`, `ContextMenuRadioGroup`, `ContextMenu`, `ContextMenuSeparator`.

All pieces import from the same path (`./components/ContextMenu`). See each sibling's `.md` for its API.

## Props

_(no public props)_