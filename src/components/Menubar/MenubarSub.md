# MenubarSub

**Import:** `import { MenubarSub } from "./components/Menubar"`
**Category:** components

## Composition

`MenubarSub` is part of a compound component. Use together with: `Menubar`, `MenubarGroup`, `MenubarTrigger`, `MenubarSubTrigger`, `MenubarSubContent`, `MenubarItem`, `MenubarLabel`, `MenubarSeparator`, `MenubarShortcut`, `MenubarMenu`, `MenubarPortal`, `MenubarRadioGroup`, `MenubarContent`, `MenubarCheckboxItem`, `MenubarRadioItem`.

All pieces import from the same path (`./components/Menubar`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)