# ToggleGroup

**Import:** `import { ToggleGroup } from "./components/ToggleGroup"`
**Category:** components

## Composition

`ToggleGroup` is part of a compound component. Use together with: `ToggleGroupItem`.

All pieces import from the same path (`./components/ToggleGroup`). See each sibling's `.md` for its API.

## Props

- `variant`: "default" | "outline"
- `size`: "small" | "medium" | "large" | "sm" | "lg"
- `children`: ReactNode